// Code generated by sqlc-ydb. DO NOT EDIT.
// source: queries.sql
package records

import (
	"context"
	"database/sql"

	"github.com/ydb-platform/ydb-go-sdk/v3/types"
)

// -- name: InsertRecords :exec
func (q *Queries) InsertRecords(ctx context.Context, arg InsertRecordsParams) error {
	_, err := q.db.ExecContext(ctx, ""+
		"DECLARE $owner_id AS Uint64;\n"+
		"DECLARE $rows AS List<Struct<record_id: Utf8, group_id: Utf8, payload: Bytes, attributes: Json>>;\n"+
		"DECLARE $created_at AS Timestamp;\n"+
		"INSERT INTO records (owner_hash, owner_id, record_id, group_id, payload, attributes, created_at, updated_at)\n"+
		"SELECT\n"+
		"    Digest::CityHash(CAST($owner_id AS String)),\n"+
		"    $owner_id,\n"+
		"    r.record_id,\n"+
		"    r.group_id,\n"+
		"    r.payload,\n"+
		"    r.attributes,\n"+
		"    $created_at AS created_at,\n"+
		"    $created_at AS updated_at\n"+
		"FROM AS_TABLE($rows) AS r;",
		sql.Named("owner_id", arg.OwnerID),
		sql.Named("rows", bindInsertRecordsRowsItem(arg.Rows)),
		sql.Named("created_at", arg.CreatedAt),
	)

	return err
}

func bindInsertRecordsRowsItem(values []InsertRecordsRowsItem) types.Value {
	if len(values) == 0 {
		return types.ZeroValue(types.List(types.Struct(
			types.StructField("record_id", types.TypeText),
			types.StructField("group_id", types.TypeText),
			types.StructField("payload", types.TypeBytes),
			types.StructField("attributes", types.TypeJSON),
		)))
	}
	items := make([]types.Value, len(values))
	for i, item := range values {
		items[i] = types.StructValue(
			types.StructFieldValue("record_id", types.TextValue(item.RecordID)),
			types.StructFieldValue("group_id", types.TextValue(item.GroupID)),
			types.StructFieldValue("payload", types.BytesValue(item.Payload)),
			types.StructFieldValue("attributes", types.JSONValue(item.Attributes)),
		)
	}
	return types.ListValue(items...)
}

// -- name: UpdateRecords :exec
func (q *Queries) UpdateRecords(ctx context.Context, arg UpdateRecordsParams) error {
	_, err := q.db.ExecContext(ctx, ""+
		"DECLARE $owner_id AS Uint64;\n"+
		"DECLARE $rows AS List<Struct<record_id: Utf8, group_id: Utf8, payload: Bytes, attributes: Json>>;\n"+
		"DECLARE $updated_at AS Timestamp;\n"+
		"UPDATE records ON\n"+
		"SELECT\n"+
		"    Digest::CityHash(CAST($owner_id AS String)) AS owner_hash,\n"+
		"    r.`record_id` AS `record_id`, `r`.`group_id` AS `group_id`, `r`.`payload` AS `payload`, `r`.`attributes` AS `attributes`,\n"+
		"    $updated_at AS updated_at\n"+
		"FROM AS_TABLE($rows) AS r;",
		sql.Named("owner_id", arg.OwnerID),
		sql.Named("rows", bindUpdateRecordsRowsItem(arg.Rows)),
		sql.Named("updated_at", arg.UpdatedAt),
	)

	return err
}

func bindUpdateRecordsRowsItem(values []UpdateRecordsRowsItem) types.Value {
	if len(values) == 0 {
		return types.ZeroValue(types.List(types.Struct(
			types.StructField("record_id", types.TypeText),
			types.StructField("group_id", types.TypeText),
			types.StructField("payload", types.TypeBytes),
			types.StructField("attributes", types.TypeJSON),
		)))
	}
	items := make([]types.Value, len(values))
	for i, item := range values {
		items[i] = types.StructValue(
			types.StructFieldValue("record_id", types.TextValue(item.RecordID)),
			types.StructFieldValue("group_id", types.TextValue(item.GroupID)),
			types.StructFieldValue("payload", types.BytesValue(item.Payload)),
			types.StructFieldValue("attributes", types.JSONValue(item.Attributes)),
		)
	}
	return types.ListValue(items...)
}

// -- name: ListRecords :many
func (q *Queries) ListRecords(ctx context.Context, arg uint64) ([]ListRecordsRow, error) {
	rows, err := q.db.QueryContext(ctx, ""+
		"DECLARE $owner_id AS Uint64;\n"+
		"SELECT `owner_hash`, `owner_id`, `record_id`, `group_id`, `payload`, `attributes`, `created_at`, `updated_at`\n"+
		"FROM records\n"+
		"WHERE owner_hash = Digest::CityHash(CAST($owner_id AS String))\n"+
		"ORDER BY record_id;",
		sql.Named("owner_id", arg),
	)
	if err != nil {
		return nil, err
	}
	defer rows.Close()

	items := []ListRecordsRow(nil)
	for rows.Next() {
		var row ListRecordsRow
		if err := rows.Scan(
			&row.OwnerHash,
			&row.OwnerID,
			&row.RecordID,
			&row.GroupID,
			&row.Payload,
			&row.Attributes,
			&row.CreatedAt,
			&row.UpdatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, row)
	}

	if err := rows.Err(); err != nil {
		return nil, err
	}

	return items, nil
}

// -- name: FilterRecords :many
func (q *Queries) FilterRecords(ctx context.Context, arg FilterRecordsParams) ([]FilterRecordsRow, error) {
	rows, err := q.db.QueryContext(ctx, ""+
		"DECLARE $owner_id AS Uint64;\n"+
		"DECLARE $record_ids AS List<Utf8>;\n"+
		"DECLARE $group_id AS Utf8;\n"+
		"SELECT owner_hash, owner_id, record_id, group_id, payload, attributes, created_at, updated_at\n"+
		"FROM records\n"+
		"WHERE owner_hash = Digest::CityHash(CAST($owner_id AS String))\n"+
		"    AND record_id IN $record_ids\n"+
		"    AND group_id = $group_id\n"+
		"ORDER BY record_id;",
		sql.Named("owner_id", arg.OwnerID),
		sql.Named("record_ids", bindFilterRecordsRecordIds(arg.RecordIds)),
		sql.Named("group_id", arg.GroupID),
	)
	if err != nil {
		return nil, err
	}
	defer rows.Close()

	items := []FilterRecordsRow(nil)
	for rows.Next() {
		var row FilterRecordsRow
		if err := rows.Scan(
			&row.OwnerHash,
			&row.OwnerID,
			&row.RecordID,
			&row.GroupID,
			&row.Payload,
			&row.Attributes,
			&row.CreatedAt,
			&row.UpdatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, row)
	}

	if err := rows.Err(); err != nil {
		return nil, err
	}

	return items, nil
}

func bindFilterRecordsRecordIds(values []string) types.Value {
	if len(values) == 0 {
		return types.ZeroValue(types.List(types.TypeText))
	}
	items := make([]types.Value, len(values))
	for i, item := range values {
		items[i] = types.TextValue(item)
	}
	return types.ListValue(items...)
}

// -- name: DeleteRecords :exec
func (q *Queries) DeleteRecords(ctx context.Context, arg DeleteRecordsParams) error {
	_, err := q.db.ExecContext(ctx, ""+
		"DECLARE $owner_id AS Uint64;\n"+
		"DECLARE $record_ids AS List<Utf8>;\n"+
		"DECLARE $group_id AS Utf8;\n"+
		"DELETE FROM records ON\n"+
		"SELECT owner_hash, record_id\n"+
		"FROM records\n"+
		"WHERE owner_hash = Digest::CityHash(CAST($owner_id AS String))\n"+
		"    AND record_id IN $record_ids\n"+
		"    AND group_id = $group_id;",
		sql.Named("owner_id", arg.OwnerID),
		sql.Named("record_ids", bindDeleteRecordsRecordIds(arg.RecordIds)),
		sql.Named("group_id", arg.GroupID),
	)

	return err
}

func bindDeleteRecordsRecordIds(values []string) types.Value {
	if len(values) == 0 {
		return types.ZeroValue(types.List(types.TypeText))
	}
	items := make([]types.Value, len(values))
	for i, item := range values {
		items[i] = types.TextValue(item)
	}
	return types.ListValue(items...)
}

// -- name: GetRecord :one
func (q *Queries) GetRecord(ctx context.Context, arg GetRecordKey) (GetRecordRow, error) {
	var row GetRecordRow
	err := q.db.QueryRowContext(ctx, ""+
		"DECLARE $key AS Struct<owner_hash: Uint64, record_id: Utf8>;\n"+
		"SELECT owner_hash, owner_id, record_id, group_id, payload, attributes, created_at, updated_at\n"+
		"FROM records\n"+
		"WHERE owner_hash = $key.owner_hash AND record_id = $key.record_id;",
		sql.Named("key", bindGetRecordKey(arg)),
	).Scan(
		&row.OwnerHash,
		&row.OwnerID,
		&row.RecordID,
		&row.GroupID,
		&row.Payload,
		&row.Attributes,
		&row.CreatedAt,
		&row.UpdatedAt,
	)

	return row, err
}

func bindGetRecordKey(item GetRecordKey) types.Value {
	return types.StructValue(
		types.StructFieldValue("owner_hash", types.Uint64Value(item.OwnerHash)),
		types.StructFieldValue("record_id", types.TextValue(item.RecordID)),
	)
}

// -- name: UpsertRecords :exec
func (q *Queries) UpsertRecords(ctx context.Context, arg UpsertRecordsParams) error {
	_, err := q.db.ExecContext(ctx, ""+
		"DECLARE $owner_id AS Uint64;\n"+
		"DECLARE $rows AS List<Struct<record_id: Utf8, group_id: Utf8, payload: Bytes, attributes: Json>>;\n"+
		"DECLARE $created_at AS Timestamp;\n"+
		"UPSERT INTO records (owner_hash, owner_id, record_id, group_id, payload, attributes, created_at, updated_at)\n"+
		"SELECT\n"+
		"    Digest::CityHash(CAST($owner_id AS String)),\n"+
		"    $owner_id,\n"+
		"    r.record_id,\n"+
		"    r.group_id,\n"+
		"    r.payload,\n"+
		"    r.attributes,\n"+
		"    $created_at,\n"+
		"    $created_at\n"+
		"FROM AS_TABLE($rows) AS r;",
		sql.Named("owner_id", arg.OwnerID),
		sql.Named("rows", bindUpsertRecordsRowsItem(arg.Rows)),
		sql.Named("created_at", arg.CreatedAt),
	)

	return err
}

func bindUpsertRecordsRowsItem(values []UpsertRecordsRowsItem) types.Value {
	if len(values) == 0 {
		return types.ZeroValue(types.List(types.Struct(
			types.StructField("record_id", types.TypeText),
			types.StructField("group_id", types.TypeText),
			types.StructField("payload", types.TypeBytes),
			types.StructField("attributes", types.TypeJSON),
		)))
	}
	items := make([]types.Value, len(values))
	for i, item := range values {
		items[i] = types.StructValue(
			types.StructFieldValue("record_id", types.TextValue(item.RecordID)),
			types.StructFieldValue("group_id", types.TextValue(item.GroupID)),
			types.StructFieldValue("payload", types.BytesValue(item.Payload)),
			types.StructFieldValue("attributes", types.JSONValue(item.Attributes)),
		)
	}
	return types.ListValue(items...)
}

// -- name: FindRecordsByTags :many
func (q *Queries) FindRecordsByTags(ctx context.Context, arg [][]byte) ([]FindRecordsByTagsRow, error) {
	rows, err := q.db.QueryContext(ctx, ""+
		"DECLARE $tags AS List<String>;\n"+
		"SELECT record_id\n"+
		"FROM records\n"+
		"WHERE NOT SetIsDisjoint(ToSet(Yson::ConvertToStringList(attributes)), $tags)\n"+
		"ORDER BY record_id;",
		sql.Named("tags", bindFindRecordsByTagsTags(arg)),
	)
	if err != nil {
		return nil, err
	}
	defer rows.Close()

	items := []FindRecordsByTagsRow(nil)
	for rows.Next() {
		var row FindRecordsByTagsRow
		if err := rows.Scan(
			&row.RecordID,
		); err != nil {
			return nil, err
		}
		items = append(items, row)
	}

	if err := rows.Err(); err != nil {
		return nil, err
	}

	return items, nil
}

func bindFindRecordsByTagsTags(values [][]byte) types.Value {
	if len(values) == 0 {
		return types.ZeroValue(types.List(types.TypeBytes))
	}
	items := make([]types.Value, len(values))
	for i, item := range values {
		items[i] = types.BytesValue(item)
	}
	return types.ListValue(items...)
}

// -- name: ReverseGroupLabel :one
func (q *Queries) ReverseGroupLabel(ctx context.Context, arg *string) (ReverseGroupLabelRow, error) {
	var row ReverseGroupLabelRow
	err := q.db.QueryRowContext(ctx, ""+
		"DECLARE $label AS Utf8?;\n"+
		"SELECT Unicode::Reverse($label) AS reversed_label;",
		sql.Named("label", arg),
	).Scan(
		&row.ReversedLabel,
	)

	return row, err
}
