// Code generated by sqlc-ydb. DO NOT EDIT.
package authors.hibernate;

import tech.ydb.table.values.PrimitiveValue;
import tech.ydb.table.values.PrimitiveType;
import tech.ydb.table.values.OptionalType;

// The caller owns the injected client and its lifecycle.
public final class Queries {
    private final org.hibernate.Session client;

    public Queries(org.hibernate.Session client) {
        this.client = java.util.Objects.requireNonNull(client);
    }

    private static final String getAuthorSql = """
-- name: GetAuthor :one
SELECT id, name, bio FROM authors WHERE id = $author_id;\
""";
    private static final String getAuthorPreparedSql = """
DECLARE $author_id AS Uint64;
-- name: GetAuthor :one
SELECT id, name, bio FROM authors WHERE id = $author_id;\
""";

    public java.util.Optional<GetAuthorRow> getAuthor(long authorId) {
        return client.doReturningWork(_connection -> {
            try (var _prepared = _connection.prepareStatement(getAuthorPreparedSql)) {
                var _statement = _prepared.unwrap(tech.ydb.jdbc.YdbPreparedStatement.class);
                _statement.setObject("author_id", PrimitiveValue.newUint64(authorId));
                try (var _rows = _prepared.executeQuery()) {
                    if (!_rows.next()) return java.util.Optional.empty();
                    long _value0 = _rows.getLong(1);
                    String _value1 = _rows.getString(2);
                    String _value2 = _rows.getString(3);
                    if (_rows.wasNull()) _value2 = null;
                    return java.util.Optional.of(new GetAuthorRow(_value0, _value1, _value2));
                }
            }
        });
    }

    private static final String listAuthorsSql = """
-- name: ListAuthors :many
SELECT id, name, bio FROM authors ORDER BY name;\
""";

    public java.util.List<ListAuthorsRow> listAuthors() {
        return client.doReturningWork(_connection -> {
            try (var _prepared = _connection.prepareStatement(listAuthorsSql)) {
                try (var _rows = _prepared.executeQuery()) {
                    var _items = new java.util.ArrayList<ListAuthorsRow>();
                    while (_rows.next()) {
                        long _value0 = _rows.getLong(1);
                        String _value1 = _rows.getString(2);
                        String _value2 = _rows.getString(3);
                        if (_rows.wasNull()) _value2 = null;
                        _items.add(new ListAuthorsRow(_value0, _value1, _value2));
                    }
                    return _items;
                }
            }
        });
    }

    private static final String getAuthorNameSql = """
-- name: GetAuthorName :one
SELECT name FROM authors WHERE id = $author_id;\
""";
    private static final String getAuthorNamePreparedSql = """
DECLARE $author_id AS Uint64;
-- name: GetAuthorName :one
SELECT name FROM authors WHERE id = $author_id;\
""";

    public java.util.Optional<GetAuthorNameRow> getAuthorName(long authorId) {
        return client.doReturningWork(_connection -> {
            try (var _prepared = _connection.prepareStatement(getAuthorNamePreparedSql)) {
                var _statement = _prepared.unwrap(tech.ydb.jdbc.YdbPreparedStatement.class);
                _statement.setObject("author_id", PrimitiveValue.newUint64(authorId));
                try (var _rows = _prepared.executeQuery()) {
                    if (!_rows.next()) return java.util.Optional.empty();
                    String _value0 = _rows.getString(1);
                    return java.util.Optional.of(new GetAuthorNameRow(_value0));
                }
            }
        });
    }

    private static final String createAuthorSql = """
-- name: CreateAuthor :one
INSERT INTO authors (id, name, bio)
VALUES ($author_id, $author_name, $biography)
RETURNING id, name, bio;\
""";
    private static final String createAuthorPreparedSql = """
DECLARE $author_id AS Uint64;
DECLARE $author_name AS Utf8;
DECLARE $biography AS Optional<Utf8>;
-- name: CreateAuthor :one
INSERT INTO authors (id, name, bio)
VALUES ($author_id, $author_name, $biography)
RETURNING id, name, bio;\
""";

    public java.util.Optional<CreateAuthorRow> createAuthor(long authorId, String authorName, String biography) {
        return client.doReturningWork(_connection -> {
            try (var _prepared = _connection.prepareStatement(createAuthorPreparedSql)) {
                var _statement = _prepared.unwrap(tech.ydb.jdbc.YdbPreparedStatement.class);
                _statement.setObject("author_id", PrimitiveValue.newUint64(authorId));
                _statement.setObject("author_name", PrimitiveValue.newText(authorName));
                _statement.setObject("biography", biography == null ? OptionalType.of(PrimitiveType.Text).emptyValue() : OptionalType.of(PrimitiveType.Text).newValue(PrimitiveValue.newText(biography)));
                try (var _rows = _prepared.executeQuery()) {
                    if (!_rows.next()) return java.util.Optional.empty();
                    long _value0 = _rows.getLong(1);
                    String _value1 = _rows.getString(2);
                    String _value2 = _rows.getString(3);
                    if (_rows.wasNull()) _value2 = null;
                    return java.util.Optional.of(new CreateAuthorRow(_value0, _value1, _value2));
                }
            }
        });
    }

    private static final String upsertAuthorSql = """
-- name: UpsertAuthor :exec
UPSERT INTO authors (id, name, bio)
VALUES ($author_id, $author_name, $biography);\
""";
    private static final String upsertAuthorPreparedSql = """
DECLARE $author_id AS Uint64;
DECLARE $author_name AS Utf8;
DECLARE $biography AS Optional<Utf8>;
-- name: UpsertAuthor :exec
UPSERT INTO authors (id, name, bio)
VALUES ($author_id, $author_name, $biography);\
""";

    public void upsertAuthor(long authorId, String authorName, String biography) {
        client.doReturningWork(_connection -> {
            try (var _prepared = _connection.prepareStatement(upsertAuthorPreparedSql)) {
                var _statement = _prepared.unwrap(tech.ydb.jdbc.YdbPreparedStatement.class);
                _statement.setObject("author_id", PrimitiveValue.newUint64(authorId));
                _statement.setObject("author_name", PrimitiveValue.newText(authorName));
                _statement.setObject("biography", biography == null ? OptionalType.of(PrimitiveType.Text).emptyValue() : OptionalType.of(PrimitiveType.Text).newValue(PrimitiveValue.newText(biography)));
                _prepared.execute();
                return null;
            }
        });
    }

    private static final String deleteAuthorSql = """
-- name: DeleteAuthor :exec
DELETE FROM authors WHERE id = $author_id;\
""";
    private static final String deleteAuthorPreparedSql = """
DECLARE $author_id AS Uint64;
-- name: DeleteAuthor :exec
DELETE FROM authors WHERE id = $author_id;\
""";

    public void deleteAuthor(long authorId) {
        client.doReturningWork(_connection -> {
            try (var _prepared = _connection.prepareStatement(deleteAuthorPreparedSql)) {
                var _statement = _prepared.unwrap(tech.ydb.jdbc.YdbPreparedStatement.class);
                _statement.setObject("author_id", PrimitiveValue.newUint64(authorId));
                _prepared.execute();
                return null;
            }
        });
    }
}
