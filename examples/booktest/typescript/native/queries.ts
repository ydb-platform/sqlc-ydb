// Code generated by sqlc-ydb. DO NOT EDIT.
import type { Query, SQL } from "@ydbjs/query";
import { Int32, Json, Primitive, TimestampType, Uint64, Utf8 } from "@ydbjs/value/primitive";

export type ConfigureQuery = (query: Query) => void;

export type GetAuthorRow = {
  readonly authorId: bigint;
  readonly name: string;
};

export type GetBookRow = {
  readonly bookId: bigint;
  readonly authorId: bigint;
  readonly isbn: string;
  readonly bookType: string;
  readonly title: string;
  readonly publicationYear: number;
  readonly available: bigint;
  readonly tags: string;
};

export type BooksByTitleYearParams = {
  readonly title: string;
  readonly publicationYear: number;
};

export type BooksByTitleYearRow = {
  readonly bookId: bigint;
  readonly authorId: bigint;
  readonly isbn: string;
  readonly bookType: string;
  readonly title: string;
  readonly publicationYear: number;
  readonly available: bigint;
  readonly tags: string;
};

export type BooksByTagsRow = {
  readonly bookId: bigint;
  readonly title: string;
  readonly name: string | null;
  readonly isbn: string;
  readonly tags: string;
};

export type CreateAuthorParams = {
  readonly authorId: bigint;
  readonly name: string;
};

export type CreateAuthorRow = {
  readonly authorId: bigint;
  readonly name: string;
};

export type CreateBookParams = {
  readonly bookId: bigint;
  readonly authorId: bigint;
  readonly isbn: string;
  readonly bookType: string;
  readonly title: string;
  readonly publicationYear: number;
  readonly available: bigint;
  readonly tags: string;
};

export type CreateBookRow = {
  readonly bookId: bigint;
  readonly authorId: bigint;
  readonly isbn: string;
  readonly bookType: string;
  readonly title: string;
  readonly publicationYear: number;
  readonly available: bigint;
  readonly tags: string;
};

export type UpdateBookParams = {
  readonly title: string;
  readonly tags: string;
  readonly bookId: bigint;
};

export type UpdateBookISBNParams = {
  readonly title: string;
  readonly tags: string;
  readonly isbn: string;
  readonly bookId: bigint;
};

export type DeleteAuthorBeforeYearParams = {
  readonly publicationYear: number;
  readonly authorId: bigint;
};

export type SayHelloRow = {
  readonly greeting: string;
};

function _int32(value: unknown, name: string): number { if (typeof value !== "number" || !Number.isInteger(value) || value < -2147483648 || value > 2147483647) throw new RangeError(`${name} is outside YQL Int32 range`); return value; }
function _uint64(value: unknown, name: string): bigint { if (typeof value !== "bigint" || value < 0n || value > 18446744073709551615n) throw new RangeError(`${name} is outside YQL Uint64 range`); return value; }
function _string(value: unknown, name: string): string { if (typeof value !== "string") throw new TypeError(`${name} must be a string`); return value; }
function _timestamp(value: unknown, name: string): bigint { if (typeof value !== "bigint" || value < 0n || value > 4291747199999999n) throw new RangeError(`${name} is outside YQL Timestamp microsecond range`); return value; }
function _json(value: unknown, name: string): string { if (typeof value !== "string") throw new TypeError(`${name} must be JSON text`); try { JSON.parse(value); } catch (error) { throw new TypeError(`${name} must contain valid JSON`, { cause: error }); } return value; }

type _RawValue = { value?: { case?: string; value?: unknown } };
function _rawValue<T>(value: unknown, name: string, expectedCase: string): T { if (value === null || typeof value !== "object") throw new TypeError(`${name} has an unexpected YDB value shape`); const raw = value as _RawValue; if (raw.value?.case !== expectedCase) throw new TypeError(`${name} has an unexpected YDB value shape`); return raw.value.value as T; }
function _rawOptional<T>(value: unknown, name: string, read: (value: unknown) => T): T | null { if (value === null || typeof value !== "object") throw new TypeError(`${name} has an unexpected YDB Optional shape`); const raw = value as _RawValue; return raw.value?.case === "nullFlagValue" ? null : read(value); }

function _decodeGetAuthorRow(row: unknown): GetAuthorRow {
  if (row === null || typeof row !== "object" || Array.isArray(row)) throw new TypeError("GetAuthor: expected an object row");
  const record = row as Record<string, unknown>;
  if (!Object.hasOwn(row, "author_id")) throw new TypeError("GetAuthor: result row is missing column author_id");
  if (!Object.hasOwn(row, "name")) throw new TypeError("GetAuthor: result row is missing column name");
  return {
    authorId: record["author_id"] as bigint,
    name: record["name"] as string,
  };
}

function _decodeGetBookRow(row: unknown): GetBookRow {
  if (row === null || typeof row !== "object" || Array.isArray(row)) throw new TypeError("GetBook: expected an object row");
  const record = row as Record<string, unknown>;
  if (!Object.hasOwn(row, "book_id")) throw new TypeError("GetBook: result row is missing column book_id");
  if (!Object.hasOwn(row, "author_id")) throw new TypeError("GetBook: result row is missing column author_id");
  if (!Object.hasOwn(row, "isbn")) throw new TypeError("GetBook: result row is missing column isbn");
  if (!Object.hasOwn(row, "book_type")) throw new TypeError("GetBook: result row is missing column book_type");
  if (!Object.hasOwn(row, "title")) throw new TypeError("GetBook: result row is missing column title");
  if (!Object.hasOwn(row, "publication_year")) throw new TypeError("GetBook: result row is missing column publication_year");
  if (!Object.hasOwn(row, "available")) throw new TypeError("GetBook: result row is missing column available");
  if (!Object.hasOwn(row, "tags")) throw new TypeError("GetBook: result row is missing column tags");
  return {
    bookId: _rawValue<bigint>(record["book_id"], "GetBook.book_id", "uint64Value"),
    authorId: _rawValue<bigint>(record["author_id"], "GetBook.author_id", "uint64Value"),
    isbn: _rawValue<string>(record["isbn"], "GetBook.isbn", "textValue"),
    bookType: _rawValue<string>(record["book_type"], "GetBook.book_type", "textValue"),
    title: _rawValue<string>(record["title"], "GetBook.title", "textValue"),
    publicationYear: _rawValue<number>(record["publication_year"], "GetBook.publication_year", "int32Value"),
    available: _rawValue<bigint>(record["available"], "GetBook.available", "uint64Value"),
    tags: _rawValue<string>(record["tags"], "GetBook.tags", "textValue"),
  };
}

function _decodeBooksByTitleYearRow(row: unknown): BooksByTitleYearRow {
  if (row === null || typeof row !== "object" || Array.isArray(row)) throw new TypeError("BooksByTitleYear: expected an object row");
  const record = row as Record<string, unknown>;
  if (!Object.hasOwn(row, "book_id")) throw new TypeError("BooksByTitleYear: result row is missing column book_id");
  if (!Object.hasOwn(row, "author_id")) throw new TypeError("BooksByTitleYear: result row is missing column author_id");
  if (!Object.hasOwn(row, "isbn")) throw new TypeError("BooksByTitleYear: result row is missing column isbn");
  if (!Object.hasOwn(row, "book_type")) throw new TypeError("BooksByTitleYear: result row is missing column book_type");
  if (!Object.hasOwn(row, "title")) throw new TypeError("BooksByTitleYear: result row is missing column title");
  if (!Object.hasOwn(row, "publication_year")) throw new TypeError("BooksByTitleYear: result row is missing column publication_year");
  if (!Object.hasOwn(row, "available")) throw new TypeError("BooksByTitleYear: result row is missing column available");
  if (!Object.hasOwn(row, "tags")) throw new TypeError("BooksByTitleYear: result row is missing column tags");
  return {
    bookId: _rawValue<bigint>(record["book_id"], "BooksByTitleYear.book_id", "uint64Value"),
    authorId: _rawValue<bigint>(record["author_id"], "BooksByTitleYear.author_id", "uint64Value"),
    isbn: _rawValue<string>(record["isbn"], "BooksByTitleYear.isbn", "textValue"),
    bookType: _rawValue<string>(record["book_type"], "BooksByTitleYear.book_type", "textValue"),
    title: _rawValue<string>(record["title"], "BooksByTitleYear.title", "textValue"),
    publicationYear: _rawValue<number>(record["publication_year"], "BooksByTitleYear.publication_year", "int32Value"),
    available: _rawValue<bigint>(record["available"], "BooksByTitleYear.available", "uint64Value"),
    tags: _rawValue<string>(record["tags"], "BooksByTitleYear.tags", "textValue"),
  };
}

function _decodeBooksByTagsRow(row: unknown): BooksByTagsRow {
  if (row === null || typeof row !== "object" || Array.isArray(row)) throw new TypeError("BooksByTags: expected an object row");
  const record = row as Record<string, unknown>;
  if (!Object.hasOwn(row, "b.book_id")) throw new TypeError("BooksByTags: result row is missing column b.book_id");
  if (!Object.hasOwn(row, "b.title")) throw new TypeError("BooksByTags: result row is missing column b.title");
  if (!Object.hasOwn(row, "a.name")) throw new TypeError("BooksByTags: result row is missing column a.name");
  if (!Object.hasOwn(row, "b.isbn")) throw new TypeError("BooksByTags: result row is missing column b.isbn");
  if (!Object.hasOwn(row, "b.tags")) throw new TypeError("BooksByTags: result row is missing column b.tags");
  return {
    bookId: _rawValue<bigint>(record["b.book_id"], "BooksByTags.b.book_id", "uint64Value"),
    title: _rawValue<string>(record["b.title"], "BooksByTags.b.title", "textValue"),
    name: _rawOptional(record["a.name"], "BooksByTags.a.name", (value) => _rawValue<string>(value, "BooksByTags.a.name", "textValue")),
    isbn: _rawValue<string>(record["b.isbn"], "BooksByTags.b.isbn", "textValue"),
    tags: _rawValue<string>(record["b.tags"], "BooksByTags.b.tags", "textValue"),
  };
}

function _decodeCreateAuthorRow(row: unknown): CreateAuthorRow {
  if (row === null || typeof row !== "object" || Array.isArray(row)) throw new TypeError("CreateAuthor: expected an object row");
  const record = row as Record<string, unknown>;
  if (!Object.hasOwn(row, "author_id")) throw new TypeError("CreateAuthor: result row is missing column author_id");
  if (!Object.hasOwn(row, "name")) throw new TypeError("CreateAuthor: result row is missing column name");
  return {
    authorId: record["author_id"] as bigint,
    name: record["name"] as string,
  };
}

function _decodeCreateBookRow(row: unknown): CreateBookRow {
  if (row === null || typeof row !== "object" || Array.isArray(row)) throw new TypeError("CreateBook: expected an object row");
  const record = row as Record<string, unknown>;
  if (!Object.hasOwn(row, "book_id")) throw new TypeError("CreateBook: result row is missing column book_id");
  if (!Object.hasOwn(row, "author_id")) throw new TypeError("CreateBook: result row is missing column author_id");
  if (!Object.hasOwn(row, "isbn")) throw new TypeError("CreateBook: result row is missing column isbn");
  if (!Object.hasOwn(row, "book_type")) throw new TypeError("CreateBook: result row is missing column book_type");
  if (!Object.hasOwn(row, "title")) throw new TypeError("CreateBook: result row is missing column title");
  if (!Object.hasOwn(row, "publication_year")) throw new TypeError("CreateBook: result row is missing column publication_year");
  if (!Object.hasOwn(row, "available")) throw new TypeError("CreateBook: result row is missing column available");
  if (!Object.hasOwn(row, "tags")) throw new TypeError("CreateBook: result row is missing column tags");
  return {
    bookId: _rawValue<bigint>(record["book_id"], "CreateBook.book_id", "uint64Value"),
    authorId: _rawValue<bigint>(record["author_id"], "CreateBook.author_id", "uint64Value"),
    isbn: _rawValue<string>(record["isbn"], "CreateBook.isbn", "textValue"),
    bookType: _rawValue<string>(record["book_type"], "CreateBook.book_type", "textValue"),
    title: _rawValue<string>(record["title"], "CreateBook.title", "textValue"),
    publicationYear: _rawValue<number>(record["publication_year"], "CreateBook.publication_year", "int32Value"),
    available: _rawValue<bigint>(record["available"], "CreateBook.available", "uint64Value"),
    tags: _rawValue<string>(record["tags"], "CreateBook.tags", "textValue"),
  };
}

export class Queries {
  readonly #sql: SQL;

  constructor(sql: SQL) {
    if (typeof sql !== "function") throw new TypeError("Queries requires a YDB SQL function");
    this.#sql = sql;
  }

  async getAuthor(authorId: bigint, configure?: ConfigureQuery): Promise<GetAuthorRow | null> {
    const pending = this.#sql<[GetAuthorRow]>`-- name: GetAuthor :one
SELECT author_id, name
FROM authors
WHERE author_id = $author_id;`
      .parameter("author_id", new Uint64(_uint64(authorId, "author_id")));
    configure?.(pending);
    const resultSets = await pending;
    if (!Array.isArray(resultSets) || !Array.isArray(resultSets[0])) throw new TypeError("GetAuthor: expected the first YDB result set to be an array");
    const rows = resultSets[0];
    return rows.length === 0 ? null : _decodeGetAuthorRow(rows[0]);
  }

  async getBook(bookId: bigint, configure?: ConfigureQuery): Promise<GetBookRow | null> {
    const pending = this.#sql<[Record<string, unknown>]>`-- name: GetBook :one
SELECT book_id, author_id, isbn, book_type, title, publication_year, available, tags
FROM books
WHERE book_id = $book_id;`
      .parameter("book_id", new Uint64(_uint64(bookId, "book_id")));
    configure?.(pending);
    const resultSets = await pending.raw();
    if (!Array.isArray(resultSets) || !Array.isArray(resultSets[0])) throw new TypeError("GetBook: expected the first YDB result set to be an array");
    const rows = resultSets[0];
    return rows.length === 0 ? null : _decodeGetBookRow(rows[0]);
  }

  async deleteBook(bookId: bigint, configure?: ConfigureQuery): Promise<void> {
    const pending = this.#sql`-- name: DeleteBook :exec
DELETE FROM books
WHERE book_id = $book_id;`
      .parameter("book_id", new Uint64(_uint64(bookId, "book_id")));
    configure?.(pending);
    await pending;
  }

  async booksByTitleYear(args: BooksByTitleYearParams, configure?: ConfigureQuery): Promise<BooksByTitleYearRow[]> {
    const pending = this.#sql<[Record<string, unknown>]>`-- name: BooksByTitleYear :many
SELECT book_id, author_id, isbn, book_type, title, publication_year, available, tags
FROM books
WHERE title = $title AND publication_year = $publication_year;`
      .parameter("title", new Utf8(_string(args.title, "title")))
      .parameter("publication_year", new Int32(_int32(args.publicationYear, "publication_year")));
    configure?.(pending);
    const resultSets = await pending.raw();
    if (!Array.isArray(resultSets) || !Array.isArray(resultSets[0])) throw new TypeError("BooksByTitleYear: expected the first YDB result set to be an array");
    const rows = resultSets[0];
    return rows.map(_decodeBooksByTitleYearRow);
  }

  async booksByTags(tags: string, configure?: ConfigureQuery): Promise<BooksByTagsRow[]> {
    const pending = this.#sql<[Record<string, unknown>]>`-- name: BooksByTags :many
\x20\x20\x20
SELECT
    b.book_id,
    b.title,
    a.name,
    b.isbn,
    b.tags
FROM books AS b
LEFT JOIN authors AS a ON b.author_id = a.author_id
WHERE NOT SetIsDisjoint(
    ToSet(Yson::ConvertToStringList(b.tags)),
    Yson::ConvertToStringList($tags)
);`
      .parameter("tags", new Json(_json(tags, "tags")));
    configure?.(pending);
    const resultSets = await pending.raw();
    if (!Array.isArray(resultSets) || !Array.isArray(resultSets[0])) throw new TypeError("BooksByTags: expected the first YDB result set to be an array");
    const rows = resultSets[0];
    return rows.map(_decodeBooksByTagsRow);
  }

  async createAuthor(args: CreateAuthorParams, configure?: ConfigureQuery): Promise<CreateAuthorRow | null> {
    const pending = this.#sql<[CreateAuthorRow]>`-- name: CreateAuthor :one
INSERT INTO authors (author_id, name)
VALUES ($author_id, $name)
RETURNING author_id, name;`
      .parameter("author_id", new Uint64(_uint64(args.authorId, "author_id")))
      .parameter("name", new Utf8(_string(args.name, "name")));
    configure?.(pending);
    const resultSets = await pending;
    if (!Array.isArray(resultSets) || !Array.isArray(resultSets[0])) throw new TypeError("CreateAuthor: expected the first YDB result set to be an array");
    const rows = resultSets[0];
    return rows.length === 0 ? null : _decodeCreateAuthorRow(rows[0]);
  }

  async createBook(args: CreateBookParams, configure?: ConfigureQuery): Promise<CreateBookRow | null> {
    const pending = this.#sql<[Record<string, unknown>]>`-- name: CreateBook :one
INSERT INTO books (
    book_id,
    author_id,
    isbn,
    book_type,
    title,
    publication_year,
    available,
    tags
) VALUES (
    $book_id,
    $author_id,
    $isbn,
    $book_type,
    $title,
    $publication_year,
    $available,
    $tags
)
RETURNING book_id, author_id, isbn, book_type, title, publication_year, available, tags;`
      .parameter("book_id", new Uint64(_uint64(args.bookId, "book_id")))
      .parameter("author_id", new Uint64(_uint64(args.authorId, "author_id")))
      .parameter("isbn", new Utf8(_string(args.isbn, "isbn")))
      .parameter("book_type", new Utf8(_string(args.bookType, "book_type")))
      .parameter("title", new Utf8(_string(args.title, "title")))
      .parameter("publication_year", new Int32(_int32(args.publicationYear, "publication_year")))
      .parameter("available", new Primitive({ value: { case: "uint64Value", value: _timestamp(args.available, "available") } }, new TimestampType()))
      .parameter("tags", new Json(_json(args.tags, "tags")));
    configure?.(pending);
    const resultSets = await pending.raw();
    if (!Array.isArray(resultSets) || !Array.isArray(resultSets[0])) throw new TypeError("CreateBook: expected the first YDB result set to be an array");
    const rows = resultSets[0];
    return rows.length === 0 ? null : _decodeCreateBookRow(rows[0]);
  }

  async updateBook(args: UpdateBookParams, configure?: ConfigureQuery): Promise<void> {
    const pending = this.#sql`-- name: UpdateBook :exec
UPDATE books
SET title = $title, tags = $tags
WHERE book_id = $book_id;`
      .parameter("title", new Utf8(_string(args.title, "title")))
      .parameter("tags", new Json(_json(args.tags, "tags")))
      .parameter("book_id", new Uint64(_uint64(args.bookId, "book_id")));
    configure?.(pending);
    await pending;
  }

  async updateBookISBN(args: UpdateBookISBNParams, configure?: ConfigureQuery): Promise<void> {
    const pending = this.#sql`-- name: UpdateBookISBN :exec
UPDATE books
SET title = $title, tags = $tags, isbn = $isbn
WHERE book_id = $book_id;`
      .parameter("title", new Utf8(_string(args.title, "title")))
      .parameter("tags", new Json(_json(args.tags, "tags")))
      .parameter("isbn", new Utf8(_string(args.isbn, "isbn")))
      .parameter("book_id", new Uint64(_uint64(args.bookId, "book_id")));
    configure?.(pending);
    await pending;
  }

  async deleteAuthorBeforeYear(args: DeleteAuthorBeforeYearParams, configure?: ConfigureQuery): Promise<void> {
    const pending = this.#sql`-- name: DeleteAuthorBeforeYear :exec
DELETE FROM books
WHERE publication_year < $publication_year AND author_id = $author_id;`
      .parameter("publication_year", new Int32(_int32(args.publicationYear, "publication_year")))
      .parameter("author_id", new Uint64(_uint64(args.authorId, "author_id")));
    configure?.(pending);
    await pending;
  }

  async sayHello(name: string, configure?: ConfigureQuery): Promise<SayHelloRow | null> {
    const pending = this.#sql<[SayHelloRow]>`-- name: SayHello :one
SELECT "hello "u || $name AS greeting;`
      .parameter("name", new Utf8(_string(name, "name")));
    configure?.(pending);
    const [rows] = await pending;

    return rows[0] ?? null;
  }
}
