// Code generated by sqlc-ydb. DO NOT EDIT.
import type { Query, SQL } from "@ydbjs/query";
import type { JSValue } from "@ydbjs/value";
import { Bytes, Int32, Json, Timestamp, Uint64, Utf8 } from "@ydbjs/value/primitive";

export type ConfigureQuery = (query: Query) => void;

export type GetAuthorRow = {
  readonly author_id: bigint;
  readonly name: string;
};

export type GetBookRow = {
  readonly book_id: bigint;
  readonly author_id: bigint;
  readonly isbn: string;
  readonly book_type: string;
  readonly title: string;
  readonly publication_year: number;
  readonly available: Date;
  readonly tags: JSValue;
};

export type BooksByTitleYearParams = {
  readonly title: string;
  readonly publicationYear: number;
};

export type BooksByTitleYearRow = {
  readonly book_id: bigint;
  readonly author_id: bigint;
  readonly isbn: string;
  readonly book_type: string;
  readonly title: string;
  readonly publication_year: number;
  readonly available: Date;
  readonly tags: JSValue;
};

export type BooksByTagsRow = {
  readonly "b.book_id": bigint;
  readonly "b.title": string;
  readonly "a.name": string | null;
  readonly "b.isbn": string;
  readonly "b.tags": JSValue;
};

export type CreateAuthorParams = {
  readonly authorId: bigint;
  readonly name: string;
};

export type CreateAuthorRow = {
  readonly author_id: bigint;
  readonly name: string;
};

export type CreateBookParams = {
  readonly bookId: bigint;
  readonly authorId: bigint;
  readonly isbn: string;
  readonly bookType: string;
  readonly title: string;
  readonly publicationYear: number;
  readonly available: Date;
  readonly tags: string;
};

export type CreateBookRow = {
  readonly book_id: bigint;
  readonly author_id: bigint;
  readonly isbn: string;
  readonly book_type: string;
  readonly title: string;
  readonly publication_year: number;
  readonly available: Date;
  readonly tags: JSValue;
};

export type UpdateBookParams = {
  readonly title: string;
  readonly tags: string;
  readonly bookId: bigint;
};

export type UpdateBookISBNParams = {
  readonly title: string;
  readonly tags: string;
  readonly isbn: string;
  readonly bookId: bigint;
};

export type DeleteAuthorBeforeYearParams = {
  readonly publicationYear: number;
  readonly authorId: bigint;
};

export type SayHelloRow = {
  readonly greeting: string;
};

export type ListAuthorsWithRecentBooksRow = {
  readonly author_id: bigint;
  readonly name: string;
};

export type ListBooksWithRecentEditionsRow = {
  readonly book_id: bigint;
  readonly author_id: bigint;
  readonly isbn: string;
  readonly book_type: string;
  readonly title: string;
  readonly publication_year: number;
  readonly available: Date;
  readonly tags: JSValue;
};

export type UpdateAuthorAndListBooksParams = {
  readonly authorId: bigint;
  readonly name: string;
};

export type UpdateAuthorAndListBooksRow = {
  readonly book_id: bigint;
  readonly title: string;
};

export type SelectAuthorAndDeleteBooksRow = {
  readonly author_id: bigint;
  readonly name: string;
};

export type ListAuthorBookTitlesRow = {
  readonly "a.author_id": bigint;
  readonly "a.name": string;
  readonly titles_json: JSValue;
};

export type InspectBookTextRow = {
  readonly base32: Uint8Array;
  readonly alphabetic: boolean;
  readonly host: Uint8Array | null;
  readonly square_root: number;
  readonly yson_string: boolean;
  readonly pattern_found: boolean;
};

export class Queries {
  readonly #sql: SQL;

  constructor(sql: SQL) {
    if (typeof sql !== "function") throw new TypeError("Queries requires a YDB SQL function");
    this.#sql = sql;
  }

  // -- name: GetAuthor :one
  async getAuthor(authorId: bigint, configure?: ConfigureQuery): Promise<GetAuthorRow | null> {
    const stmt = this.#sql<[GetAuthorRow]>(
      "SELECT author_id, name\n" +
      "FROM authors\n" +
      "WHERE author_id = $author_id;"
    )
      .parameter("author_id", new Uint64(authorId));
    configure?.(stmt);
    const [rows] = await stmt;

    return rows[0] ?? null;
  }

  // -- name: GetBook :one
  async getBook(bookId: bigint, configure?: ConfigureQuery): Promise<GetBookRow | null> {
    const stmt = this.#sql<[GetBookRow]>(
      "SELECT book_id, author_id, isbn, book_type, title, publication_year, available, tags\n" +
      "FROM books\n" +
      "WHERE book_id = $book_id;"
    )
      .parameter("book_id", new Uint64(bookId));
    configure?.(stmt);
    const [rows] = await stmt;

    return rows[0] ?? null;
  }

  // -- name: DeleteBook :exec
  async deleteBook(bookId: bigint, configure?: ConfigureQuery): Promise<void> {
    const stmt = this.#sql(
      "DELETE FROM books\n" +
      "WHERE book_id = $book_id;"
    )
      .parameter("book_id", new Uint64(bookId));
    configure?.(stmt);
    await stmt;
  }

  // -- name: BooksByTitleYear :many
  async booksByTitleYear(args: BooksByTitleYearParams, configure?: ConfigureQuery): Promise<BooksByTitleYearRow[]> {
    const stmt = this.#sql<[BooksByTitleYearRow]>(
      "SELECT book_id, author_id, isbn, book_type, title, publication_year, available, tags\n" +
      "FROM books\n" +
      "WHERE title = $title AND publication_year = $publication_year;"
    )
      .parameter("title", new Utf8(args.title))
      .parameter("publication_year", new Int32(args.publicationYear));
    configure?.(stmt);
    const [rows] = await stmt;

    return rows;
  }

  // -- name: BooksByTags :many
  async booksByTags(tags: string, configure?: ConfigureQuery): Promise<BooksByTagsRow[]> {
    const stmt = this.#sql<[BooksByTagsRow]>(
      "DECLARE $tags AS Json;\n" +
      "SELECT\n" +
      "    b.book_id,\n" +
      "    b.title,\n" +
      "    a.name,\n" +
      "    b.isbn,\n" +
      "    b.tags\n" +
      "FROM books AS b\n" +
      "LEFT JOIN authors AS a ON b.author_id = a.author_id\n" +
      "WHERE NOT SetIsDisjoint(\n" +
      "    ToSet(Yson::ConvertToStringList(b.tags)),\n" +
      "    Yson::ConvertToStringList($tags)\n" +
      ");"
    );
    // Keep explicit DECLARE statements; the SDK otherwise prepends duplicates.
    Object.defineProperty(stmt, "text", { value: stmt.text, writable: false });
    stmt
      .parameter("tags", new Json(tags));
    configure?.(stmt);
    const [rows] = await stmt;

    return rows;
  }

  // -- name: CreateAuthor :one
  async createAuthor(args: CreateAuthorParams, configure?: ConfigureQuery): Promise<CreateAuthorRow | null> {
    const stmt = this.#sql<[CreateAuthorRow]>(
      "INSERT INTO authors (author_id, name)\n" +
      "VALUES ($author_id, $name)\n" +
      "RETURNING author_id, name;"
    )
      .parameter("author_id", new Uint64(args.authorId))
      .parameter("name", new Utf8(args.name));
    configure?.(stmt);
    const [rows] = await stmt;

    return rows[0] ?? null;
  }

  // -- name: CreateBook :one
  async createBook(args: CreateBookParams, configure?: ConfigureQuery): Promise<CreateBookRow | null> {
    const stmt = this.#sql<[CreateBookRow]>(
      "INSERT INTO books (\n" +
      "    book_id,\n" +
      "    author_id,\n" +
      "    isbn,\n" +
      "    book_type,\n" +
      "    title,\n" +
      "    publication_year,\n" +
      "    available,\n" +
      "    tags\n" +
      ") VALUES (\n" +
      "    $book_id,\n" +
      "    $author_id,\n" +
      "    $isbn,\n" +
      "    $book_type,\n" +
      "    $title,\n" +
      "    $publication_year,\n" +
      "    $available,\n" +
      "    $tags\n" +
      ")\n" +
      "RETURNING book_id, author_id, isbn, book_type, title, publication_year, available, tags;"
    )
      .parameter("book_id", new Uint64(args.bookId))
      .parameter("author_id", new Uint64(args.authorId))
      .parameter("isbn", new Utf8(args.isbn))
      .parameter("book_type", new Utf8(args.bookType))
      .parameter("title", new Utf8(args.title))
      .parameter("publication_year", new Int32(args.publicationYear))
      .parameter("available", new Timestamp(args.available))
      .parameter("tags", new Json(args.tags));
    configure?.(stmt);
    const [rows] = await stmt;

    return rows[0] ?? null;
  }

  // -- name: UpdateBook :exec
  async updateBook(args: UpdateBookParams, configure?: ConfigureQuery): Promise<void> {
    const stmt = this.#sql(
      "UPDATE books\n" +
      "SET title = $title, tags = $tags\n" +
      "WHERE book_id = $book_id;"
    )
      .parameter("title", new Utf8(args.title))
      .parameter("tags", new Json(args.tags))
      .parameter("book_id", new Uint64(args.bookId));
    configure?.(stmt);
    await stmt;
  }

  // -- name: UpdateBookISBN :exec
  async updateBookISBN(args: UpdateBookISBNParams, configure?: ConfigureQuery): Promise<void> {
    const stmt = this.#sql(
      "UPDATE books\n" +
      "SET title = $title, tags = $tags, isbn = $isbn\n" +
      "WHERE book_id = $book_id;"
    )
      .parameter("title", new Utf8(args.title))
      .parameter("tags", new Json(args.tags))
      .parameter("isbn", new Utf8(args.isbn))
      .parameter("book_id", new Uint64(args.bookId));
    configure?.(stmt);
    await stmt;
  }

  // -- name: DeleteAuthorBeforeYear :exec
  async deleteAuthorBeforeYear(args: DeleteAuthorBeforeYearParams, configure?: ConfigureQuery): Promise<void> {
    const stmt = this.#sql(
      "DELETE FROM books\n" +
      "WHERE publication_year < $publication_year AND author_id = $author_id;"
    )
      .parameter("publication_year", new Int32(args.publicationYear))
      .parameter("author_id", new Uint64(args.authorId));
    configure?.(stmt);
    await stmt;
  }

  // -- name: SayHello :one
  async sayHello(name: string, configure?: ConfigureQuery): Promise<SayHelloRow | null> {
    const stmt = this.#sql<[SayHelloRow]>(
      "SELECT \"hello \"u || $name AS greeting;"
    )
      .parameter("name", new Utf8(name));
    configure?.(stmt);
    const [rows] = await stmt;

    return rows[0] ?? null;
  }

  // -- name: ListAuthorsWithRecentBooks :many
  async listAuthorsWithRecentBooks(sinceYear: number, configure?: ConfigureQuery): Promise<ListAuthorsWithRecentBooksRow[]> {
    const stmt = this.#sql<[ListAuthorsWithRecentBooksRow]>(
      "SELECT a.author_id, a.name\n" +
      "FROM authors AS a\n" +
      "WHERE a.author_id IN (\n" +
      "    SELECT b.author_id FROM books AS b WHERE b.publication_year >= $since_year\n" +
      ")\n" +
      "ORDER BY a.author_id;"
    )
      .parameter("since_year", new Int32(sinceYear));
    configure?.(stmt);
    const [rows] = await stmt;

    return rows;
  }

  // -- name: ListBooksWithRecentEditions :many
  async listBooksWithRecentEditions(sinceYear: number, configure?: ConfigureQuery): Promise<ListBooksWithRecentEditionsRow[]> {
    const stmt = this.#sql<[ListBooksWithRecentEditionsRow]>(
      "DECLARE $since_year AS Int32;\n" +
      "SELECT b.book_id, b.author_id, b.isbn, b.book_type, b.title, b.publication_year, b.available, b.tags\n" +
      "FROM books AS b\n" +
      "WHERE (b.author_id, b.book_type) IN (\n" +
      "    SELECT (recent.author_id, recent.book_type)\n" +
      "    FROM books AS recent\n" +
      "    WHERE recent.publication_year >= $since_year\n" +
      ")\n" +
      "ORDER BY b.book_id;"
    );
    // Keep explicit DECLARE statements; the SDK otherwise prepends duplicates.
    Object.defineProperty(stmt, "text", { value: stmt.text, writable: false });
    stmt
      .parameter("since_year", new Int32(sinceYear));
    configure?.(stmt);
    const [rows] = await stmt;

    return rows;
  }

  // -- name: DeleteBooksByAuthorName :exec
  async deleteBooksByAuthorName(authorName: string, configure?: ConfigureQuery): Promise<void> {
    const stmt = this.#sql(
      "DELETE FROM books\n" +
      "WHERE author_id IN (SELECT author_id FROM authors WHERE name = $author_name);"
    )
      .parameter("author_name", new Utf8(authorName));
    configure?.(stmt);
    await stmt;
  }

  // -- name: DeleteAuthorWithBooks :exec
  async deleteAuthorWithBooks(authorId: bigint, configure?: ConfigureQuery): Promise<void> {
    const stmt = this.#sql(
      "DECLARE $author_id AS Uint64;\n" +
      "DELETE FROM books WHERE author_id = $author_id;\n" +
      "DELETE FROM authors WHERE author_id = $author_id;"
    );
    // Keep explicit DECLARE statements; the SDK otherwise prepends duplicates.
    Object.defineProperty(stmt, "text", { value: stmt.text, writable: false });
    stmt
      .parameter("author_id", new Uint64(authorId));
    configure?.(stmt);
    await stmt;
  }

  // -- name: UpdateAuthorAndListBooks :many
  async updateAuthorAndListBooks(args: UpdateAuthorAndListBooksParams, configure?: ConfigureQuery): Promise<UpdateAuthorAndListBooksRow[]> {
    const stmt = this.#sql<[UpdateAuthorAndListBooksRow]>(
      "DECLARE $author_id AS Uint64;\n" +
      "DECLARE $name AS Utf8;\n" +
      "UPDATE authors SET name = $name WHERE author_id = $author_id;\n" +
      "SELECT book_id, title FROM books WHERE author_id = $author_id ORDER BY book_id;"
    );
    // Keep explicit DECLARE statements; the SDK otherwise prepends duplicates.
    Object.defineProperty(stmt, "text", { value: stmt.text, writable: false });
    stmt
      .parameter("author_id", new Uint64(args.authorId))
      .parameter("name", new Utf8(args.name));
    configure?.(stmt);
    const [rows] = await stmt;

    return rows;
  }

  // -- name: SelectAuthorAndDeleteBooks :one
  async selectAuthorAndDeleteBooks(authorId: bigint, configure?: ConfigureQuery): Promise<SelectAuthorAndDeleteBooksRow | null> {
    const stmt = this.#sql<[SelectAuthorAndDeleteBooksRow]>(
      "DECLARE $author_id AS Uint64;\n" +
      "SELECT author_id, name FROM authors WHERE author_id = $author_id;\n" +
      "DELETE FROM books WHERE author_id = $author_id;"
    );
    // Keep explicit DECLARE statements; the SDK otherwise prepends duplicates.
    Object.defineProperty(stmt, "text", { value: stmt.text, writable: false });
    stmt
      .parameter("author_id", new Uint64(authorId));
    configure?.(stmt);
    const [rows] = await stmt;

    return rows[0] ?? null;
  }

  // -- name: ListAuthorBookTitles :many
  async listAuthorBookTitles(sinceYear: number, configure?: ConfigureQuery): Promise<ListAuthorBookTitlesRow[]> {
    const stmt = this.#sql<[ListAuthorBookTitlesRow]>(
      "DECLARE $since_year AS Int32;\n" +
      "$recent = (SELECT author_id, title FROM books WHERE publication_year >= $since_year);\n" +
      "$grouped = (\n" +
      "    SELECT author_id, AGGREGATE_LIST(title, 100u) AS titles\n" +
      "    FROM $recent\n" +
      "    GROUP BY author_id\n" +
      ");\n" +
      "SELECT a.author_id, a.name, Yson::SerializeJson(Json::From(g.titles)) AS titles_json\n" +
      "FROM (SELECT author_id, name FROM authors) AS a\n" +
      "JOIN $grouped AS g ON a.author_id = g.author_id\n" +
      "ORDER BY a.author_id;"
    );
    // Keep explicit DECLARE statements; the SDK otherwise prepends duplicates.
    Object.defineProperty(stmt, "text", { value: stmt.text, writable: false });
    stmt
      .parameter("since_year", new Int32(sinceYear));
    configure?.(stmt);
    const [rows] = await stmt;

    return rows;
  }

  // -- name: InspectBookText :one
  async inspectBookText(text: Uint8Array, configure?: ConfigureQuery): Promise<InspectBookTextRow | null> {
    const stmt = this.#sql<[InspectBookTextRow]>(
      "DECLARE $text AS String;\n" +
      "SELECT\n" +
      "    String::Base32Encode($text) AS base32,\n" +
      "    Unicode::IsAlpha(\"Book\"u) AS alphabetic,\n" +
      "    Url::GetHost(\"https://example.org/books\") AS host,\n" +
      "    Math::Sqrt(9.0) AS square_root,\n" +
      "    Yson::IsString(Yson::From($text)) AS yson_string,\n" +
      "    Pire::Grep(\"book\")($text) AS pattern_found;"
    );
    // Keep explicit DECLARE statements; the SDK otherwise prepends duplicates.
    Object.defineProperty(stmt, "text", { value: stmt.text, writable: false });
    stmt
      .parameter("text", new Bytes(text));
    configure?.(stmt);
    const [rows] = await stmt;

    return rows[0] ?? null;
  }
}
