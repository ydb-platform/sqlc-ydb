// Code generated by sqlc-ydb. DO NOT EDIT.
package ondeck.nativeapi

import tech.ydb.query.QueryTransaction
import tech.ydb.query.tools.SessionRetryContext
import tech.ydb.query.tools.QueryReader
import tech.ydb.common.transaction.TxMode
import tech.ydb.table.query.Params
import tech.ydb.table.values.PrimitiveValue
import tech.ydb.table.values.PrimitiveType
import tech.ydb.table.values.OptionalType

// The caller owns the injected executor and its lifecycle.
class Queries {
    private val client: SessionRetryContext?
    private val transaction: QueryTransaction?

    constructor(client: SessionRetryContext) {
        this.client = client
        this.transaction = null
    }

    constructor(transaction: QueryTransaction) {
        this.client = null
        this.transaction = transaction
    }

    // -- name: ListCities :many
    fun listCities(): List<ListCitiesRow> {
        val _params = Params.create()
        val _query = if (transaction != null) {
            QueryReader.readFrom(transaction.createQuery(
                "SELECT slug, name\n" +
                "FROM city\n" +
                "ORDER BY name;", _params)).join().getValue()
        } else {
            client!!.supplyResult { _session ->
                QueryReader.readFrom(_session.createQuery(
                "SELECT slug, name\n" +
                "FROM city\n" +
                "ORDER BY name;", TxMode.SERIALIZABLE_RW, _params))
            }.join().getValue()
        }
        kotlin.check(_query.getResultSetCount() == 1) { "Expected one result set" }
        val _rows = _query.getResultSet(0)
        val _items = ArrayList<ListCitiesRow>()
        while (_rows.next()) {
            val _value0: String = _rows.getColumn(0).getText()
            val _value1: String = _rows.getColumn(1).getText()
            _items.add(ListCitiesRow(_value0, _value1))
        }
        return _items
    }

    // -- name: GetCity :one
    fun getCity(slug: String): GetCityRow? {
        val _params = Params.create()
        _params.put("\$slug", PrimitiveValue.newText(slug))
        val _query = if (transaction != null) {
            QueryReader.readFrom(transaction.createQuery(
                "SELECT slug, name\n" +
                "FROM city\n" +
                "WHERE slug = \$slug;", _params)).join().getValue()
        } else {
            client!!.supplyResult { _session ->
                QueryReader.readFrom(_session.createQuery(
                "SELECT slug, name\n" +
                "FROM city\n" +
                "WHERE slug = \$slug;", TxMode.SERIALIZABLE_RW, _params))
            }.join().getValue()
        }
        kotlin.check(_query.getResultSetCount() == 1) { "Expected one result set" }
        val _rows = _query.getResultSet(0)
        if (!_rows.next()) return null
        val _value0: String = _rows.getColumn(0).getText()
        val _value1: String = _rows.getColumn(1).getText()
        return GetCityRow(_value0, _value1)
    }

    // -- name: CreateCity :one
    fun createCity(name: String, slug: String): CreateCityRow? {
        val _params = Params.create()
        _params.put("\$name", PrimitiveValue.newText(name))
        _params.put("\$slug", PrimitiveValue.newText(slug))
        val _query = if (transaction != null) {
            QueryReader.readFrom(transaction.createQuery(
                "INSERT INTO city (\n" +
                "    name,\n" +
                "    slug\n" +
                ") VALUES (\n" +
                "    \$name,\n" +
                "    \$slug\n" +
                ") RETURNING slug, name;", _params)).join().getValue()
        } else {
            client!!.supplyResult { _session ->
                QueryReader.readFrom(_session.createQuery(
                "INSERT INTO city (\n" +
                "    name,\n" +
                "    slug\n" +
                ") VALUES (\n" +
                "    \$name,\n" +
                "    \$slug\n" +
                ") RETURNING slug, name;", TxMode.SERIALIZABLE_RW, _params))
            }.join().getValue()
        }
        kotlin.check(_query.getResultSetCount() == 1) { "Expected one result set" }
        val _rows = _query.getResultSet(0)
        if (!_rows.next()) return null
        val _value0: String = _rows.getColumn(0).getText()
        val _value1: String = _rows.getColumn(1).getText()
        return CreateCityRow(_value0, _value1)
    }

    // -- name: UpdateCityName :exec
    fun updateCityName(name: String, slug: String): Unit {
        val _params = Params.create()
        _params.put("\$name", PrimitiveValue.newText(name))
        _params.put("\$slug", PrimitiveValue.newText(slug))
        if (transaction != null) {
            transaction.createQuery(
                "UPDATE city\n" +
                "SET name = \$name\n" +
                "WHERE slug = \$slug;", _params).execute().join().getStatus().expectSuccess()
        } else {
            client!!.supplyResult { _session ->
                _session.createQuery(
                "UPDATE city\n" +
                "SET name = \$name\n" +
                "WHERE slug = \$slug;", TxMode.SERIALIZABLE_RW, _params).execute()
            }.join().getStatus().expectSuccess()
        }
    }

    // -- name: ListVenues :many
    fun listVenues(city: String): List<ListVenuesRow> {
        val _params = Params.create()
        _params.put("\$city", PrimitiveValue.newText(city))
        val _query = if (transaction != null) {
            QueryReader.readFrom(transaction.createQuery(
                "SELECT id, slug, name, city, status, statuses, spotify_playlist, songkick_id, tags, created_at\n" +
                "FROM venue\n" +
                "WHERE city = \$city\n" +
                "ORDER BY name;", _params)).join().getValue()
        } else {
            client!!.supplyResult { _session ->
                QueryReader.readFrom(_session.createQuery(
                "SELECT id, slug, name, city, status, statuses, spotify_playlist, songkick_id, tags, created_at\n" +
                "FROM venue\n" +
                "WHERE city = \$city\n" +
                "ORDER BY name;", TxMode.SERIALIZABLE_RW, _params))
            }.join().getValue()
        }
        kotlin.check(_query.getResultSetCount() == 1) { "Expected one result set" }
        val _rows = _query.getResultSet(0)
        val _items = ArrayList<ListVenuesRow>()
        while (_rows.next()) {
            val _value0: Long = _rows.getColumn(0).getUint64()
            val _value1: String = _rows.getColumn(1).getText()
            val _value2: String = _rows.getColumn(2).getText()
            val _value3: String = _rows.getColumn(3).getText()
            val _value4: String = _rows.getColumn(4).getText()
            val _value5: String? = if (_rows.getColumn(5).isOptionalItemPresent()) _rows.getColumn(5).getOptionalItem().getJson() else null
            val _value6: String = _rows.getColumn(6).getText()
            val _value7: String? = if (_rows.getColumn(7).isOptionalItemPresent()) _rows.getColumn(7).getOptionalItem().getText() else null
            val _value8: String? = if (_rows.getColumn(8).isOptionalItemPresent()) _rows.getColumn(8).getOptionalItem().getJson() else null
            val _value9: java.time.Instant? = if (_rows.getColumn(9).isOptionalItemPresent()) _rows.getColumn(9).getOptionalItem().getTimestamp() else null
            _items.add(ListVenuesRow(_value0, _value1, _value2, _value3, _value4, _value5, _value6, _value7, _value8, _value9))
        }
        return _items
    }

    // -- name: DeleteVenue :exec
    fun deleteVenue(slug: String): Unit {
        val _params = Params.create()
        _params.put("\$slug", PrimitiveValue.newText(slug))
        if (transaction != null) {
            transaction.createQuery(
                "DELETE FROM venue\n" +
                "WHERE slug = \$slug AND slug = \$slug;", _params).execute().join().getStatus().expectSuccess()
        } else {
            client!!.supplyResult { _session ->
                _session.createQuery(
                "DELETE FROM venue\n" +
                "WHERE slug = \$slug AND slug = \$slug;", TxMode.SERIALIZABLE_RW, _params).execute()
            }.join().getStatus().expectSuccess()
        }
    }

    // -- name: GetVenue :one
    fun getVenue(slug: String, city: String): GetVenueRow? {
        val _params = Params.create()
        _params.put("\$slug", PrimitiveValue.newText(slug))
        _params.put("\$city", PrimitiveValue.newText(city))
        val _query = if (transaction != null) {
            QueryReader.readFrom(transaction.createQuery(
                "SELECT id, slug, name, city, status, statuses, spotify_playlist, songkick_id, tags, created_at\n" +
                "FROM venue\n" +
                "WHERE slug = \$slug AND city = \$city;", _params)).join().getValue()
        } else {
            client!!.supplyResult { _session ->
                QueryReader.readFrom(_session.createQuery(
                "SELECT id, slug, name, city, status, statuses, spotify_playlist, songkick_id, tags, created_at\n" +
                "FROM venue\n" +
                "WHERE slug = \$slug AND city = \$city;", TxMode.SERIALIZABLE_RW, _params))
            }.join().getValue()
        }
        kotlin.check(_query.getResultSetCount() == 1) { "Expected one result set" }
        val _rows = _query.getResultSet(0)
        if (!_rows.next()) return null
        val _value0: Long = _rows.getColumn(0).getUint64()
        val _value1: String = _rows.getColumn(1).getText()
        val _value2: String = _rows.getColumn(2).getText()
        val _value3: String = _rows.getColumn(3).getText()
        val _value4: String = _rows.getColumn(4).getText()
        val _value5: String? = if (_rows.getColumn(5).isOptionalItemPresent()) _rows.getColumn(5).getOptionalItem().getJson() else null
        val _value6: String = _rows.getColumn(6).getText()
        val _value7: String? = if (_rows.getColumn(7).isOptionalItemPresent()) _rows.getColumn(7).getOptionalItem().getText() else null
        val _value8: String? = if (_rows.getColumn(8).isOptionalItemPresent()) _rows.getColumn(8).getOptionalItem().getJson() else null
        val _value9: java.time.Instant? = if (_rows.getColumn(9).isOptionalItemPresent()) _rows.getColumn(9).getOptionalItem().getTimestamp() else null
        return GetVenueRow(_value0, _value1, _value2, _value3, _value4, _value5, _value6, _value7, _value8, _value9)
    }

    // -- name: CreateVenue :one
    fun createVenue(id: Long, slug: String, name: String, city: String, createdAt: java.time.Instant?, spotifyPlaylist: String, status: String, statuses: String?, tags: String?): CreateVenueRow? {
        val _params = Params.create()
        _params.put("\$id", PrimitiveValue.newUint64(id))
        _params.put("\$slug", PrimitiveValue.newText(slug))
        _params.put("\$name", PrimitiveValue.newText(name))
        _params.put("\$city", PrimitiveValue.newText(city))
        _params.put("\$created_at", if (createdAt == null) OptionalType.of(PrimitiveType.Timestamp).emptyValue() else OptionalType.of(PrimitiveType.Timestamp).newValue(PrimitiveValue.newTimestamp(createdAt)))
        _params.put("\$spotify_playlist", PrimitiveValue.newText(spotifyPlaylist))
        _params.put("\$status", PrimitiveValue.newText(status))
        _params.put("\$statuses", if (statuses == null) OptionalType.of(PrimitiveType.Json).emptyValue() else OptionalType.of(PrimitiveType.Json).newValue(PrimitiveValue.newJson(statuses)))
        _params.put("\$tags", if (tags == null) OptionalType.of(PrimitiveType.Json).emptyValue() else OptionalType.of(PrimitiveType.Json).newValue(PrimitiveValue.newJson(tags)))
        val _query = if (transaction != null) {
            QueryReader.readFrom(transaction.createQuery(
                "INSERT INTO venue (\n" +
                "    id,\n" +
                "    slug,\n" +
                "    name,\n" +
                "    city,\n" +
                "    created_at,\n" +
                "    spotify_playlist,\n" +
                "    status,\n" +
                "    statuses,\n" +
                "    tags\n" +
                ") VALUES (\n" +
                "    \$id,\n" +
                "    \$slug,\n" +
                "    \$name,\n" +
                "    \$city,\n" +
                "    \$created_at,\n" +
                "    \$spotify_playlist,\n" +
                "    \$status,\n" +
                "    \$statuses,\n" +
                "    \$tags\n" +
                ") RETURNING id;", _params)).join().getValue()
        } else {
            client!!.supplyResult { _session ->
                QueryReader.readFrom(_session.createQuery(
                "INSERT INTO venue (\n" +
                "    id,\n" +
                "    slug,\n" +
                "    name,\n" +
                "    city,\n" +
                "    created_at,\n" +
                "    spotify_playlist,\n" +
                "    status,\n" +
                "    statuses,\n" +
                "    tags\n" +
                ") VALUES (\n" +
                "    \$id,\n" +
                "    \$slug,\n" +
                "    \$name,\n" +
                "    \$city,\n" +
                "    \$created_at,\n" +
                "    \$spotify_playlist,\n" +
                "    \$status,\n" +
                "    \$statuses,\n" +
                "    \$tags\n" +
                ") RETURNING id;", TxMode.SERIALIZABLE_RW, _params))
            }.join().getValue()
        }
        kotlin.check(_query.getResultSetCount() == 1) { "Expected one result set" }
        val _rows = _query.getResultSet(0)
        if (!_rows.next()) return null
        val _value0: Long = _rows.getColumn(0).getUint64()
        return CreateVenueRow(_value0)
    }

    // -- name: UpdateVenueName :one
    fun updateVenueName(name: String, slug: String): UpdateVenueNameRow? {
        val _params = Params.create()
        _params.put("\$name", PrimitiveValue.newText(name))
        _params.put("\$slug", PrimitiveValue.newText(slug))
        val _query = if (transaction != null) {
            QueryReader.readFrom(transaction.createQuery(
                "UPDATE venue\n" +
                "SET name = \$name\n" +
                "WHERE slug = \$slug\n" +
                "RETURNING id;", _params)).join().getValue()
        } else {
            client!!.supplyResult { _session ->
                QueryReader.readFrom(_session.createQuery(
                "UPDATE venue\n" +
                "SET name = \$name\n" +
                "WHERE slug = \$slug\n" +
                "RETURNING id;", TxMode.SERIALIZABLE_RW, _params))
            }.join().getValue()
        }
        kotlin.check(_query.getResultSetCount() == 1) { "Expected one result set" }
        val _rows = _query.getResultSet(0)
        if (!_rows.next()) return null
        val _value0: Long = _rows.getColumn(0).getUint64()
        return UpdateVenueNameRow(_value0)
    }

    // -- name: VenueCountByCity :many
    fun venueCountByCity(): List<VenueCountByCityRow> {
        val _params = Params.create()
        val _query = if (transaction != null) {
            QueryReader.readFrom(transaction.createQuery(
                "SELECT\n" +
                "    city,\n" +
                "    COUNT(*) AS venue_count\n" +
                "FROM venue\n" +
                "GROUP BY city\n" +
                "ORDER BY city;", _params)).join().getValue()
        } else {
            client!!.supplyResult { _session ->
                QueryReader.readFrom(_session.createQuery(
                "SELECT\n" +
                "    city,\n" +
                "    COUNT(*) AS venue_count\n" +
                "FROM venue\n" +
                "GROUP BY city\n" +
                "ORDER BY city;", TxMode.SERIALIZABLE_RW, _params))
            }.join().getValue()
        }
        kotlin.check(_query.getResultSetCount() == 1) { "Expected one result set" }
        val _rows = _query.getResultSet(0)
        val _items = ArrayList<VenueCountByCityRow>()
        while (_rows.next()) {
            val _value0: String = _rows.getColumn(0).getText()
            val _value1: Long = _rows.getColumn(1).getUint64()
            _items.add(VenueCountByCityRow(_value0, _value1))
        }
        return _items
    }
}
