// Code generated by sqlc-ydb. DO NOT EDIT.
import type { Query, SQL } from "@ydbjs/query";
import type { JSValue } from "@ydbjs/value";
import { Json, JsonType, Timestamp, TimestampType, Uint64, Utf8 } from "@ydbjs/value/primitive";
import { Optional } from "@ydbjs/value/optional";

export type ConfigureQuery = (query: Query) => void;

export type ListCitiesRow = {
  readonly slug: string;
  readonly name: string;
};

export type GetCityRow = {
  readonly slug: string;
  readonly name: string;
};

export type CreateCityParams = {
  readonly name: string;
  readonly slug: string;
};

export type CreateCityRow = {
  readonly slug: string;
  readonly name: string;
};

export type UpdateCityNameParams = {
  readonly name: string;
  readonly slug: string;
};

export type ListVenuesRow = {
  readonly id: bigint;
  readonly slug: string;
  readonly name: string;
  readonly city: string;
  readonly status: string;
  readonly statuses: JSValue;
  readonly spotify_playlist: string;
  readonly songkick_id: string | null;
  readonly tags: JSValue;
  readonly created_at: Date | null;
};

export type GetVenueParams = {
  readonly slug: string;
  readonly city: string;
};

export type GetVenueRow = {
  readonly id: bigint;
  readonly slug: string;
  readonly name: string;
  readonly city: string;
  readonly status: string;
  readonly statuses: JSValue;
  readonly spotify_playlist: string;
  readonly songkick_id: string | null;
  readonly tags: JSValue;
  readonly created_at: Date | null;
};

export type CreateVenueParams = {
  readonly id: bigint;
  readonly slug: string;
  readonly name: string;
  readonly city: string;
  readonly createdAt: Date | null;
  readonly spotifyPlaylist: string;
  readonly status: string;
  readonly statuses: string | null;
  readonly tags: string | null;
};

export type CreateVenueRow = {
  readonly id: bigint;
};

export type UpdateVenueNameParams = {
  readonly name: string;
  readonly slug: string;
};

export type UpdateVenueNameRow = {
  readonly id: bigint;
};

export type VenueCountByCityRow = {
  readonly city: string;
  readonly venue_count: bigint;
};

export class Queries {
  readonly #sql: SQL;

  constructor(sql: SQL) {
    if (typeof sql !== "function") throw new TypeError("Queries requires a YDB SQL function");
    this.#sql = sql;
  }

  async listCities(configure?: ConfigureQuery): Promise<ListCitiesRow[]> {
    const stmt = this.#sql<[ListCitiesRow]>`-- name: ListCities :many
      SELECT slug, name
      FROM city
      ORDER BY name;`;
    configure?.(stmt);
    const [rows] = await stmt;

    return rows;
  }

  async getCity(slug: string, configure?: ConfigureQuery): Promise<GetCityRow | null> {
    const stmt = this.#sql<[GetCityRow]>`-- name: GetCity :one
      SELECT slug, name
      FROM city
      WHERE slug = $slug;`
      .parameter("slug", new Utf8(slug));
    configure?.(stmt);
    const [rows] = await stmt;

    return rows[0] ?? null;
  }

  async createCity(args: CreateCityParams, configure?: ConfigureQuery): Promise<CreateCityRow | null> {
    const stmt = this.#sql<[CreateCityRow]>`-- name: CreateCity :one
      INSERT INTO city (
          name,
          slug
      ) VALUES (
          $name,
          $slug
      ) RETURNING slug, name;`
      .parameter("name", new Utf8(args.name))
      .parameter("slug", new Utf8(args.slug));
    configure?.(stmt);
    const [rows] = await stmt;

    return rows[0] ?? null;
  }

  async updateCityName(args: UpdateCityNameParams, configure?: ConfigureQuery): Promise<void> {
    const stmt = this.#sql`-- name: UpdateCityName :exec
      UPDATE city
      SET name = $name
      WHERE slug = $slug;`
      .parameter("name", new Utf8(args.name))
      .parameter("slug", new Utf8(args.slug));
    configure?.(stmt);
    await stmt;
  }

  async listVenues(city: string, configure?: ConfigureQuery): Promise<ListVenuesRow[]> {
    const stmt = this.#sql<[ListVenuesRow]>`-- name: ListVenues :many
      SELECT id, slug, name, city, status, statuses, spotify_playlist, songkick_id, tags, created_at
      FROM venue
      WHERE city = $city
      ORDER BY name;`
      .parameter("city", new Utf8(city));
    configure?.(stmt);
    const [rows] = await stmt;

    return rows;
  }

  async deleteVenue(slug: string, configure?: ConfigureQuery): Promise<void> {
    const stmt = this.#sql`-- name: DeleteVenue :exec
      DELETE FROM venue
      WHERE slug = $slug AND slug = $slug;`
      .parameter("slug", new Utf8(slug));
    configure?.(stmt);
    await stmt;
  }

  async getVenue(args: GetVenueParams, configure?: ConfigureQuery): Promise<GetVenueRow | null> {
    const stmt = this.#sql<[GetVenueRow]>`-- name: GetVenue :one
      SELECT id, slug, name, city, status, statuses, spotify_playlist, songkick_id, tags, created_at
      FROM venue
      WHERE slug = $slug AND city = $city;`
      .parameter("slug", new Utf8(args.slug))
      .parameter("city", new Utf8(args.city));
    configure?.(stmt);
    const [rows] = await stmt;

    return rows[0] ?? null;
  }

  async createVenue(args: CreateVenueParams, configure?: ConfigureQuery): Promise<CreateVenueRow | null> {
    const stmt = this.#sql<[CreateVenueRow]>`-- name: CreateVenue :one
      INSERT INTO venue (
          id,
          slug,
          name,
          city,
          created_at,
          spotify_playlist,
          status,
          statuses,
          tags
      ) VALUES (
          $id,
          $slug,
          $name,
          $city,
          $created_at,
          $spotify_playlist,
          $status,
          $statuses,
          $tags
      ) RETURNING id;`
      .parameter("id", new Uint64(args.id))
      .parameter("slug", new Utf8(args.slug))
      .parameter("name", new Utf8(args.name))
      .parameter("city", new Utf8(args.city))
      .parameter("created_at", new Optional(args.createdAt === null ? null : new Timestamp(args.createdAt), new TimestampType()))
      .parameter("spotify_playlist", new Utf8(args.spotifyPlaylist))
      .parameter("status", new Utf8(args.status))
      .parameter("statuses", new Optional(args.statuses === null ? null : new Json(args.statuses), new JsonType()))
      .parameter("tags", new Optional(args.tags === null ? null : new Json(args.tags), new JsonType()));
    configure?.(stmt);
    const [rows] = await stmt;

    return rows[0] ?? null;
  }

  async updateVenueName(args: UpdateVenueNameParams, configure?: ConfigureQuery): Promise<UpdateVenueNameRow | null> {
    const stmt = this.#sql<[UpdateVenueNameRow]>`-- name: UpdateVenueName :one
      UPDATE venue
      SET name = $name
      WHERE slug = $slug
      RETURNING id;`
      .parameter("name", new Utf8(args.name))
      .parameter("slug", new Utf8(args.slug));
    configure?.(stmt);
    const [rows] = await stmt;

    return rows[0] ?? null;
  }

  async venueCountByCity(configure?: ConfigureQuery): Promise<VenueCountByCityRow[]> {
    const stmt = this.#sql<[VenueCountByCityRow]>`-- name: VenueCountByCity :many
      SELECT
          city,
          COUNT(*) AS venue_count
      FROM venue
      GROUP BY city
      ORDER BY city;`;
    configure?.(stmt);
    const [rows] = await stmt;

    return rows;
  }
}
