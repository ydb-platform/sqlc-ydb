// Code generated by sqlc-ydb. DO NOT EDIT.
import type { Query, SQL } from "@ydbjs/query";
import type { Type, Value } from "@ydbjs/value";
import { Json, JsonType, Primitive, TimestampType, Uint64, Utf8 } from "@ydbjs/value/primitive";
import { Optional } from "@ydbjs/value/optional";

export type ConfigureQuery = (query: Query) => void;

export interface ListCitiesRow {
  readonly slug: string;
  readonly name: string;
}

export interface GetCityRow {
  readonly slug: string;
  readonly name: string;
}

export interface CreateCityParams {
  readonly name: string;
  readonly slug: string;
}

export interface CreateCityRow {
  readonly slug: string;
  readonly name: string;
}

export interface UpdateCityNameParams {
  readonly name: string;
  readonly slug: string;
}

export interface ListVenuesRow {
  readonly id: bigint;
  readonly slug: string;
  readonly name: string;
  readonly city: string;
  readonly status: string;
  readonly statuses: string | null;
  readonly spotifyPlaylist: string;
  readonly songkickId: string | null;
  readonly tags: string | null;
  readonly createdAt: bigint | null;
}

export interface GetVenueParams {
  readonly slug: string;
  readonly city: string;
}

export interface GetVenueRow {
  readonly id: bigint;
  readonly slug: string;
  readonly name: string;
  readonly city: string;
  readonly status: string;
  readonly statuses: string | null;
  readonly spotifyPlaylist: string;
  readonly songkickId: string | null;
  readonly tags: string | null;
  readonly createdAt: bigint | null;
}

export interface CreateVenueParams {
  readonly id: bigint;
  readonly slug: string;
  readonly name: string;
  readonly city: string;
  readonly createdAt: bigint | null;
  readonly spotifyPlaylist: string;
  readonly status: string;
  readonly statuses: string | null;
  readonly tags: string | null;
}

export interface CreateVenueRow {
  readonly id: bigint;
}

export interface UpdateVenueNameParams {
  readonly name: string;
  readonly slug: string;
}

export interface UpdateVenueNameRow {
  readonly id: bigint;
}

export interface VenueCountByCityRow {
  readonly city: string;
  readonly venueCount: bigint;
}

function _uint64(value: unknown, name: string): bigint { if (typeof value !== "bigint" || value < 0n || value > 18446744073709551615n) throw new RangeError(`${name} is outside YQL Uint64 range`); return value; }
function _string(value: unknown, name: string): string { if (typeof value !== "string") throw new TypeError(`${name} must be a string`); return value; }
function _timestamp(value: unknown, name: string): bigint { if (typeof value !== "bigint" || value < 0n || value > 4291747199999999n) throw new RangeError(`${name} is outside YQL Timestamp microsecond range`); return value; }
function _json(value: unknown, name: string): string { if (typeof value !== "string") throw new TypeError(`${name} must be JSON text`); try { JSON.parse(value); } catch (error) { throw new TypeError(`${name} must contain valid JSON`, { cause: error }); } return value; }
function _optional<T>(value: T | null | undefined, name: string, itemType: Type, makeValue: (item: T) => Value): Value { if (value === undefined) throw new TypeError(`${name} must not be undefined; use null for an empty Optional`); return new Optional(value === null ? null : makeValue(value), itemType); }

type _RawValue = { value?: { case?: string; value?: unknown } };
function _rawValue<T>(value: unknown, name: string, expectedCase: string): T { if (value === null || typeof value !== "object") throw new TypeError(`${name} has an unexpected YDB value shape`); const raw = value as _RawValue; if (raw.value?.case !== expectedCase) throw new TypeError(`${name} has an unexpected YDB value shape`); return raw.value.value as T; }
function _rawOptional<T>(value: unknown, name: string, read: (value: unknown) => T): T | null { if (value === null || typeof value !== "object") throw new TypeError(`${name} has an unexpected YDB Optional shape`); const raw = value as _RawValue; return raw.value?.case === "nullFlagValue" ? null : read(value); }

function _decodeListCitiesRow(row: unknown): ListCitiesRow {
  if (row === null || typeof row !== "object" || Array.isArray(row)) throw new TypeError("ListCities: expected an object row");
  const record = row as Record<string, unknown>;
  if (!Object.hasOwn(row, "slug")) throw new TypeError("ListCities: result row is missing column slug");
  if (!Object.hasOwn(row, "name")) throw new TypeError("ListCities: result row is missing column name");
  return {
    slug: record["slug"] as string,
    name: record["name"] as string,
  };
}

function _decodeGetCityRow(row: unknown): GetCityRow {
  if (row === null || typeof row !== "object" || Array.isArray(row)) throw new TypeError("GetCity: expected an object row");
  const record = row as Record<string, unknown>;
  if (!Object.hasOwn(row, "slug")) throw new TypeError("GetCity: result row is missing column slug");
  if (!Object.hasOwn(row, "name")) throw new TypeError("GetCity: result row is missing column name");
  return {
    slug: record["slug"] as string,
    name: record["name"] as string,
  };
}

function _decodeCreateCityRow(row: unknown): CreateCityRow {
  if (row === null || typeof row !== "object" || Array.isArray(row)) throw new TypeError("CreateCity: expected an object row");
  const record = row as Record<string, unknown>;
  if (!Object.hasOwn(row, "slug")) throw new TypeError("CreateCity: result row is missing column slug");
  if (!Object.hasOwn(row, "name")) throw new TypeError("CreateCity: result row is missing column name");
  return {
    slug: record["slug"] as string,
    name: record["name"] as string,
  };
}

function _decodeListVenuesRow(row: unknown): ListVenuesRow {
  if (row === null || typeof row !== "object" || Array.isArray(row)) throw new TypeError("ListVenues: expected an object row");
  const record = row as Record<string, unknown>;
  if (!Object.hasOwn(row, "id")) throw new TypeError("ListVenues: result row is missing column id");
  if (!Object.hasOwn(row, "slug")) throw new TypeError("ListVenues: result row is missing column slug");
  if (!Object.hasOwn(row, "name")) throw new TypeError("ListVenues: result row is missing column name");
  if (!Object.hasOwn(row, "city")) throw new TypeError("ListVenues: result row is missing column city");
  if (!Object.hasOwn(row, "status")) throw new TypeError("ListVenues: result row is missing column status");
  if (!Object.hasOwn(row, "statuses")) throw new TypeError("ListVenues: result row is missing column statuses");
  if (!Object.hasOwn(row, "spotify_playlist")) throw new TypeError("ListVenues: result row is missing column spotify_playlist");
  if (!Object.hasOwn(row, "songkick_id")) throw new TypeError("ListVenues: result row is missing column songkick_id");
  if (!Object.hasOwn(row, "tags")) throw new TypeError("ListVenues: result row is missing column tags");
  if (!Object.hasOwn(row, "created_at")) throw new TypeError("ListVenues: result row is missing column created_at");
  return {
    id: _rawValue<bigint>(record["id"], "ListVenues.id", "uint64Value"),
    slug: _rawValue<string>(record["slug"], "ListVenues.slug", "textValue"),
    name: _rawValue<string>(record["name"], "ListVenues.name", "textValue"),
    city: _rawValue<string>(record["city"], "ListVenues.city", "textValue"),
    status: _rawValue<string>(record["status"], "ListVenues.status", "textValue"),
    statuses: _rawOptional(record["statuses"], "ListVenues.statuses", (value) => _rawValue<string>(value, "ListVenues.statuses", "textValue")),
    spotifyPlaylist: _rawValue<string>(record["spotify_playlist"], "ListVenues.spotify_playlist", "textValue"),
    songkickId: _rawOptional(record["songkick_id"], "ListVenues.songkick_id", (value) => _rawValue<string>(value, "ListVenues.songkick_id", "textValue")),
    tags: _rawOptional(record["tags"], "ListVenues.tags", (value) => _rawValue<string>(value, "ListVenues.tags", "textValue")),
    createdAt: _rawOptional(record["created_at"], "ListVenues.created_at", (value) => _rawValue<bigint>(value, "ListVenues.created_at", "uint64Value")),
  };
}

function _decodeGetVenueRow(row: unknown): GetVenueRow {
  if (row === null || typeof row !== "object" || Array.isArray(row)) throw new TypeError("GetVenue: expected an object row");
  const record = row as Record<string, unknown>;
  if (!Object.hasOwn(row, "id")) throw new TypeError("GetVenue: result row is missing column id");
  if (!Object.hasOwn(row, "slug")) throw new TypeError("GetVenue: result row is missing column slug");
  if (!Object.hasOwn(row, "name")) throw new TypeError("GetVenue: result row is missing column name");
  if (!Object.hasOwn(row, "city")) throw new TypeError("GetVenue: result row is missing column city");
  if (!Object.hasOwn(row, "status")) throw new TypeError("GetVenue: result row is missing column status");
  if (!Object.hasOwn(row, "statuses")) throw new TypeError("GetVenue: result row is missing column statuses");
  if (!Object.hasOwn(row, "spotify_playlist")) throw new TypeError("GetVenue: result row is missing column spotify_playlist");
  if (!Object.hasOwn(row, "songkick_id")) throw new TypeError("GetVenue: result row is missing column songkick_id");
  if (!Object.hasOwn(row, "tags")) throw new TypeError("GetVenue: result row is missing column tags");
  if (!Object.hasOwn(row, "created_at")) throw new TypeError("GetVenue: result row is missing column created_at");
  return {
    id: _rawValue<bigint>(record["id"], "GetVenue.id", "uint64Value"),
    slug: _rawValue<string>(record["slug"], "GetVenue.slug", "textValue"),
    name: _rawValue<string>(record["name"], "GetVenue.name", "textValue"),
    city: _rawValue<string>(record["city"], "GetVenue.city", "textValue"),
    status: _rawValue<string>(record["status"], "GetVenue.status", "textValue"),
    statuses: _rawOptional(record["statuses"], "GetVenue.statuses", (value) => _rawValue<string>(value, "GetVenue.statuses", "textValue")),
    spotifyPlaylist: _rawValue<string>(record["spotify_playlist"], "GetVenue.spotify_playlist", "textValue"),
    songkickId: _rawOptional(record["songkick_id"], "GetVenue.songkick_id", (value) => _rawValue<string>(value, "GetVenue.songkick_id", "textValue")),
    tags: _rawOptional(record["tags"], "GetVenue.tags", (value) => _rawValue<string>(value, "GetVenue.tags", "textValue")),
    createdAt: _rawOptional(record["created_at"], "GetVenue.created_at", (value) => _rawValue<bigint>(value, "GetVenue.created_at", "uint64Value")),
  };
}

function _decodeCreateVenueRow(row: unknown): CreateVenueRow {
  if (row === null || typeof row !== "object" || Array.isArray(row)) throw new TypeError("CreateVenue: expected an object row");
  const record = row as Record<string, unknown>;
  if (!Object.hasOwn(row, "id")) throw new TypeError("CreateVenue: result row is missing column id");
  return {
    id: record["id"] as bigint,
  };
}

function _decodeUpdateVenueNameRow(row: unknown): UpdateVenueNameRow {
  if (row === null || typeof row !== "object" || Array.isArray(row)) throw new TypeError("UpdateVenueName: expected an object row");
  const record = row as Record<string, unknown>;
  if (!Object.hasOwn(row, "id")) throw new TypeError("UpdateVenueName: result row is missing column id");
  return {
    id: record["id"] as bigint,
  };
}

function _decodeVenueCountByCityRow(row: unknown): VenueCountByCityRow {
  if (row === null || typeof row !== "object" || Array.isArray(row)) throw new TypeError("VenueCountByCity: expected an object row");
  const record = row as Record<string, unknown>;
  if (!Object.hasOwn(row, "city")) throw new TypeError("VenueCountByCity: result row is missing column city");
  if (!Object.hasOwn(row, "venue_count")) throw new TypeError("VenueCountByCity: result row is missing column venue_count");
  return {
    city: record["city"] as string,
    venueCount: record["venue_count"] as bigint,
  };
}

export class Queries {
  readonly #sql: SQL;

  constructor(sql: SQL) {
    if (typeof sql !== "function") throw new TypeError("Queries requires a YDB SQL function");
    this.#sql = sql;
  }

  async listCities(configure?: ConfigureQuery): Promise<ListCitiesRow[]> {
    const pending = this.#sql<[ListCitiesRow]>`-- name: ListCities :many
SELECT slug, name
FROM city
ORDER BY name;`;
    configure?.(pending);
    const resultSets = await pending;
    if (!Array.isArray(resultSets) || !Array.isArray(resultSets[0])) throw new TypeError("ListCities: expected the first YDB result set to be an array");
    const rows = resultSets[0];
    return rows.map(_decodeListCitiesRow);
  }

  async getCity(slug: string, configure?: ConfigureQuery): Promise<GetCityRow | null> {
    const pending = this.#sql<[GetCityRow]>`-- name: GetCity :one
SELECT slug, name
FROM city
WHERE slug = $slug;`
      .parameter("slug", new Utf8(_string(slug, "slug")));
    configure?.(pending);
    const resultSets = await pending;
    if (!Array.isArray(resultSets) || !Array.isArray(resultSets[0])) throw new TypeError("GetCity: expected the first YDB result set to be an array");
    const rows = resultSets[0];
    return rows.length === 0 ? null : _decodeGetCityRow(rows[0]);
  }

  async createCity(args: CreateCityParams, configure?: ConfigureQuery): Promise<CreateCityRow | null> {
    const pending = this.#sql<[CreateCityRow]>`-- name: CreateCity :one
INSERT INTO city (
    name,
    slug
) VALUES (
    $name,
    $slug
) RETURNING slug, name;`
      .parameter("name", new Utf8(_string(args.name, "name")))
      .parameter("slug", new Utf8(_string(args.slug, "slug")));
    configure?.(pending);
    const resultSets = await pending;
    if (!Array.isArray(resultSets) || !Array.isArray(resultSets[0])) throw new TypeError("CreateCity: expected the first YDB result set to be an array");
    const rows = resultSets[0];
    return rows.length === 0 ? null : _decodeCreateCityRow(rows[0]);
  }

  async updateCityName(args: UpdateCityNameParams, configure?: ConfigureQuery): Promise<void> {
    const pending = this.#sql`-- name: UpdateCityName :exec
UPDATE city
SET name = $name
WHERE slug = $slug;`
      .parameter("name", new Utf8(_string(args.name, "name")))
      .parameter("slug", new Utf8(_string(args.slug, "slug")));
    configure?.(pending);
    await pending;
  }

  async listVenues(city: string, configure?: ConfigureQuery): Promise<ListVenuesRow[]> {
    const pending = this.#sql<[Record<string, unknown>]>`-- name: ListVenues :many
SELECT id, slug, name, city, status, statuses, spotify_playlist, songkick_id, tags, created_at
FROM venue
WHERE city = $city
ORDER BY name;`
      .parameter("city", new Utf8(_string(city, "city")));
    configure?.(pending);
    const resultSets = await pending.raw();
    if (!Array.isArray(resultSets) || !Array.isArray(resultSets[0])) throw new TypeError("ListVenues: expected the first YDB result set to be an array");
    const rows = resultSets[0];
    return rows.map(_decodeListVenuesRow);
  }

  async deleteVenue(slug: string, configure?: ConfigureQuery): Promise<void> {
    const pending = this.#sql`-- name: DeleteVenue :exec
DELETE FROM venue
WHERE slug = $slug AND slug = $slug;`
      .parameter("slug", new Utf8(_string(slug, "slug")));
    configure?.(pending);
    await pending;
  }

  async getVenue(args: GetVenueParams, configure?: ConfigureQuery): Promise<GetVenueRow | null> {
    const pending = this.#sql<[Record<string, unknown>]>`-- name: GetVenue :one
SELECT id, slug, name, city, status, statuses, spotify_playlist, songkick_id, tags, created_at
FROM venue
WHERE slug = $slug AND city = $city;`
      .parameter("slug", new Utf8(_string(args.slug, "slug")))
      .parameter("city", new Utf8(_string(args.city, "city")));
    configure?.(pending);
    const resultSets = await pending.raw();
    if (!Array.isArray(resultSets) || !Array.isArray(resultSets[0])) throw new TypeError("GetVenue: expected the first YDB result set to be an array");
    const rows = resultSets[0];
    return rows.length === 0 ? null : _decodeGetVenueRow(rows[0]);
  }

  async createVenue(args: CreateVenueParams, configure?: ConfigureQuery): Promise<CreateVenueRow | null> {
    const pending = this.#sql<[CreateVenueRow]>`-- name: CreateVenue :one
INSERT INTO venue (
    id,
    slug,
    name,
    city,
    created_at,
    spotify_playlist,
    status,
    statuses,
    tags
) VALUES (
    $id,
    $slug,
    $name,
    $city,
    $created_at,
    $spotify_playlist,
    $status,
    $statuses,
    $tags
) RETURNING id;`
      .parameter("id", new Uint64(_uint64(args.id, "id")))
      .parameter("slug", new Utf8(_string(args.slug, "slug")))
      .parameter("name", new Utf8(_string(args.name, "name")))
      .parameter("city", new Utf8(_string(args.city, "city")))
      .parameter("created_at", _optional(args.createdAt, "created_at", new TimestampType(), (item) => new Primitive({ value: { case: "uint64Value", value: _timestamp(item, "created_at") } }, new TimestampType())))
      .parameter("spotify_playlist", new Utf8(_string(args.spotifyPlaylist, "spotify_playlist")))
      .parameter("status", new Utf8(_string(args.status, "status")))
      .parameter("statuses", _optional(args.statuses, "statuses", new JsonType(), (item) => new Json(_json(item, "statuses"))))
      .parameter("tags", _optional(args.tags, "tags", new JsonType(), (item) => new Json(_json(item, "tags"))));
    configure?.(pending);
    const resultSets = await pending;
    if (!Array.isArray(resultSets) || !Array.isArray(resultSets[0])) throw new TypeError("CreateVenue: expected the first YDB result set to be an array");
    const rows = resultSets[0];
    return rows.length === 0 ? null : _decodeCreateVenueRow(rows[0]);
  }

  async updateVenueName(args: UpdateVenueNameParams, configure?: ConfigureQuery): Promise<UpdateVenueNameRow | null> {
    const pending = this.#sql<[UpdateVenueNameRow]>`-- name: UpdateVenueName :one
UPDATE venue
SET name = $name
WHERE slug = $slug
RETURNING id;`
      .parameter("name", new Utf8(_string(args.name, "name")))
      .parameter("slug", new Utf8(_string(args.slug, "slug")));
    configure?.(pending);
    const resultSets = await pending;
    if (!Array.isArray(resultSets) || !Array.isArray(resultSets[0])) throw new TypeError("UpdateVenueName: expected the first YDB result set to be an array");
    const rows = resultSets[0];
    return rows.length === 0 ? null : _decodeUpdateVenueNameRow(rows[0]);
  }

  async venueCountByCity(configure?: ConfigureQuery): Promise<VenueCountByCityRow[]> {
    const pending = this.#sql<[VenueCountByCityRow]>`-- name: VenueCountByCity :many
SELECT
    city,
    COUNT(*) AS venue_count
FROM venue
GROUP BY city
ORDER BY city;`;
    configure?.(pending);
    const resultSets = await pending;
    if (!Array.isArray(resultSets) || !Array.isArray(resultSets[0])) throw new TypeError("VenueCountByCity: expected the first YDB result set to be an array");
    const rows = resultSets[0];
    return rows.map(_decodeVenueCountByCityRow);
  }
}
