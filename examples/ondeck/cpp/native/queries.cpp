// Code generated by sqlc-ydb. DO NOT EDIT.
#include "queries.hpp"

#include <ydb-cpp-sdk/client/params/params.h>
#include <ydb-cpp-sdk/client/result/result.h>
#include <ydb-cpp-sdk/client/types/status/status.h>

#include <stdexcept>
#include <utility>

namespace ondeck::native {

// -- name: ListCities :many
std::vector<ListCitiesRow> Queries::ListCities() const {
    std::optional<NYdb::TResultSet> sqlc_result_set;
    const auto sqlc_execute = [&](NYdb::NQuery::TSession sqlc_session, const NYdb::NQuery::TTxControl& sqlc_tx) -> NYdb::TStatus {
        auto sqlc_result = sqlc_session.ExecuteQuery(
            R"sql(
                SELECT slug, name
                FROM city
                ORDER BY name;
            )sql",
            sqlc_tx,
            this->execute_settings_
        ).GetValueSync();
        if (sqlc_result.IsSuccess()) {
            if (sqlc_result.GetResultSets().size() != 1) {
                throw std::runtime_error("expected exactly one result set");
            }
            sqlc_result_set = sqlc_result.GetResultSet(0);
        }
        return sqlc_result;
    };
    const auto sqlc_status = this->transaction_ != nullptr
        ? sqlc_execute(this->transaction_->GetSession(), NYdb::NQuery::TTxControl::Tx(*this->transaction_))
        : this->client_->RetryQuerySync([&](NYdb::NQuery::TSession sqlc_session) -> NYdb::TStatus {
            return sqlc_execute(
                std::move(sqlc_session),
                NYdb::NQuery::TTxControl::BeginTx(this->tx_settings_).CommitTx()
            );
        }, this->retry_settings_);
    NYdb::NStatusHelpers::ThrowOnError(sqlc_status);
    if (!sqlc_result_set) {
        throw std::runtime_error("ListCities: successful query returned no result set");
    }
    NYdb::TResultSetParser sqlc_parser(*sqlc_result_set);
    std::vector<ListCitiesRow> sqlc_rows;
    sqlc_rows.reserve(sqlc_result_set->RowsCount());
    while (sqlc_parser.TryNextRow()) {
        sqlc_rows.push_back(ListCitiesRow{
            sqlc_parser.ColumnParser("slug").GetUtf8(),
            sqlc_parser.ColumnParser("name").GetUtf8(),
        });
    }
    return sqlc_rows;
}

// -- name: GetCity :one
std::optional<GetCityRow> Queries::GetCity(const std::string& slug) const {
    std::optional<NYdb::TResultSet> sqlc_result_set;
    const auto sqlc_execute = [&](NYdb::NQuery::TSession sqlc_session, const NYdb::NQuery::TTxControl& sqlc_tx) -> NYdb::TStatus {
        auto sqlc_params = NYdb::TParamsBuilder()
            .AddParam("$slug").Utf8(slug).Build()
            .Build();
        auto sqlc_result = sqlc_session.ExecuteQuery(
            R"sql(
                SELECT slug, name
                FROM city
                WHERE slug = $slug;
            )sql",
            sqlc_tx,
            sqlc_params,
            this->execute_settings_
        ).GetValueSync();
        if (sqlc_result.IsSuccess()) {
            if (sqlc_result.GetResultSets().size() != 1) {
                throw std::runtime_error("expected exactly one result set");
            }
            sqlc_result_set = sqlc_result.GetResultSet(0);
        }
        return sqlc_result;
    };
    const auto sqlc_status = this->transaction_ != nullptr
        ? sqlc_execute(this->transaction_->GetSession(), NYdb::NQuery::TTxControl::Tx(*this->transaction_))
        : this->client_->RetryQuerySync([&](NYdb::NQuery::TSession sqlc_session) -> NYdb::TStatus {
            return sqlc_execute(
                std::move(sqlc_session),
                NYdb::NQuery::TTxControl::BeginTx(this->tx_settings_).CommitTx()
            );
        }, this->retry_settings_);
    NYdb::NStatusHelpers::ThrowOnError(sqlc_status);
    if (!sqlc_result_set) {
        throw std::runtime_error("GetCity: successful query returned no result set");
    }
    NYdb::TResultSetParser sqlc_parser(*sqlc_result_set);
    if (!sqlc_parser.TryNextRow()) {
        return std::nullopt;
    }
    GetCityRow sqlc_row{
        sqlc_parser.ColumnParser("slug").GetUtf8(),
        sqlc_parser.ColumnParser("name").GetUtf8(),
    };
    return sqlc_row;
}

// -- name: CreateCity :one
std::optional<CreateCityRow> Queries::CreateCity(const std::string& name, const std::string& slug) const {
    std::optional<NYdb::TResultSet> sqlc_result_set;
    const auto sqlc_execute = [&](NYdb::NQuery::TSession sqlc_session, const NYdb::NQuery::TTxControl& sqlc_tx) -> NYdb::TStatus {
        auto sqlc_params = NYdb::TParamsBuilder()
            .AddParam("$name").Utf8(name).Build()
            .AddParam("$slug").Utf8(slug).Build()
            .Build();
        auto sqlc_result = sqlc_session.ExecuteQuery(
            R"sql(
                INSERT INTO city (
                    name,
                    slug
                ) VALUES (
                    $name,
                    $slug
                ) RETURNING slug, name;
            )sql",
            sqlc_tx,
            sqlc_params,
            this->execute_settings_
        ).GetValueSync();
        if (sqlc_result.IsSuccess()) {
            if (sqlc_result.GetResultSets().size() != 1) {
                throw std::runtime_error("expected exactly one result set");
            }
            sqlc_result_set = sqlc_result.GetResultSet(0);
        }
        return sqlc_result;
    };
    const auto sqlc_status = this->transaction_ != nullptr
        ? sqlc_execute(this->transaction_->GetSession(), NYdb::NQuery::TTxControl::Tx(*this->transaction_))
        : this->client_->RetryQuerySync([&](NYdb::NQuery::TSession sqlc_session) -> NYdb::TStatus {
            return sqlc_execute(
                std::move(sqlc_session),
                NYdb::NQuery::TTxControl::BeginTx(this->tx_settings_).CommitTx()
            );
        }, this->retry_settings_);
    NYdb::NStatusHelpers::ThrowOnError(sqlc_status);
    if (!sqlc_result_set) {
        throw std::runtime_error("CreateCity: successful query returned no result set");
    }
    NYdb::TResultSetParser sqlc_parser(*sqlc_result_set);
    if (!sqlc_parser.TryNextRow()) {
        return std::nullopt;
    }
    CreateCityRow sqlc_row{
        sqlc_parser.ColumnParser("slug").GetUtf8(),
        sqlc_parser.ColumnParser("name").GetUtf8(),
    };
    return sqlc_row;
}

// -- name: UpdateCityName :exec
void Queries::UpdateCityName(const std::string& name, const std::string& slug) const {
    const auto sqlc_execute = [&](NYdb::NQuery::TSession sqlc_session, const NYdb::NQuery::TTxControl& sqlc_tx) -> NYdb::TStatus {
        auto sqlc_params = NYdb::TParamsBuilder()
            .AddParam("$name").Utf8(name).Build()
            .AddParam("$slug").Utf8(slug).Build()
            .Build();
        auto sqlc_result = sqlc_session.ExecuteQuery(
            R"sql(
                UPDATE city
                SET name = $name
                WHERE slug = $slug;
            )sql",
            sqlc_tx,
            sqlc_params,
            this->execute_settings_
        ).GetValueSync();
        return sqlc_result;
    };
    const auto sqlc_status = this->transaction_ != nullptr
        ? sqlc_execute(this->transaction_->GetSession(), NYdb::NQuery::TTxControl::Tx(*this->transaction_))
        : this->client_->RetryQuerySync([&](NYdb::NQuery::TSession sqlc_session) -> NYdb::TStatus {
            return sqlc_execute(
                std::move(sqlc_session),
                NYdb::NQuery::TTxControl::BeginTx(this->tx_settings_).CommitTx()
            );
        }, this->retry_settings_);
    NYdb::NStatusHelpers::ThrowOnError(sqlc_status);
}

// -- name: ListVenues :many
std::vector<ListVenuesRow> Queries::ListVenues(const std::string& city) const {
    std::optional<NYdb::TResultSet> sqlc_result_set;
    const auto sqlc_execute = [&](NYdb::NQuery::TSession sqlc_session, const NYdb::NQuery::TTxControl& sqlc_tx) -> NYdb::TStatus {
        auto sqlc_params = NYdb::TParamsBuilder()
            .AddParam("$city").Utf8(city).Build()
            .Build();
        auto sqlc_result = sqlc_session.ExecuteQuery(
            R"sql(
                SELECT id, slug, name, city, status, statuses, spotify_playlist, songkick_id, tags, created_at
                FROM venue
                WHERE city = $city
                ORDER BY name;
            )sql",
            sqlc_tx,
            sqlc_params,
            this->execute_settings_
        ).GetValueSync();
        if (sqlc_result.IsSuccess()) {
            if (sqlc_result.GetResultSets().size() != 1) {
                throw std::runtime_error("expected exactly one result set");
            }
            sqlc_result_set = sqlc_result.GetResultSet(0);
        }
        return sqlc_result;
    };
    const auto sqlc_status = this->transaction_ != nullptr
        ? sqlc_execute(this->transaction_->GetSession(), NYdb::NQuery::TTxControl::Tx(*this->transaction_))
        : this->client_->RetryQuerySync([&](NYdb::NQuery::TSession sqlc_session) -> NYdb::TStatus {
            return sqlc_execute(
                std::move(sqlc_session),
                NYdb::NQuery::TTxControl::BeginTx(this->tx_settings_).CommitTx()
            );
        }, this->retry_settings_);
    NYdb::NStatusHelpers::ThrowOnError(sqlc_status);
    if (!sqlc_result_set) {
        throw std::runtime_error("ListVenues: successful query returned no result set");
    }
    NYdb::TResultSetParser sqlc_parser(*sqlc_result_set);
    std::vector<ListVenuesRow> sqlc_rows;
    sqlc_rows.reserve(sqlc_result_set->RowsCount());
    while (sqlc_parser.TryNextRow()) {
        sqlc_rows.push_back(ListVenuesRow{
            sqlc_parser.ColumnParser("id").GetUint64(),
            sqlc_parser.ColumnParser("slug").GetUtf8(),
            sqlc_parser.ColumnParser("name").GetUtf8(),
            sqlc_parser.ColumnParser("city").GetUtf8(),
            sqlc_parser.ColumnParser("status").GetUtf8(),
            sqlc_parser.ColumnParser("statuses").GetOptionalJson(),
            sqlc_parser.ColumnParser("spotify_playlist").GetUtf8(),
            sqlc_parser.ColumnParser("songkick_id").GetOptionalUtf8(),
            sqlc_parser.ColumnParser("tags").GetOptionalJson(),
            sqlc_parser.ColumnParser("created_at").GetOptionalTimestamp(),
        });
    }
    return sqlc_rows;
}

// -- name: DeleteVenue :exec
void Queries::DeleteVenue(const std::string& slug) const {
    const auto sqlc_execute = [&](NYdb::NQuery::TSession sqlc_session, const NYdb::NQuery::TTxControl& sqlc_tx) -> NYdb::TStatus {
        auto sqlc_params = NYdb::TParamsBuilder()
            .AddParam("$slug").Utf8(slug).Build()
            .Build();
        auto sqlc_result = sqlc_session.ExecuteQuery(
            R"sql(
                DELETE FROM venue
                WHERE slug = $slug AND slug = $slug;
            )sql",
            sqlc_tx,
            sqlc_params,
            this->execute_settings_
        ).GetValueSync();
        return sqlc_result;
    };
    const auto sqlc_status = this->transaction_ != nullptr
        ? sqlc_execute(this->transaction_->GetSession(), NYdb::NQuery::TTxControl::Tx(*this->transaction_))
        : this->client_->RetryQuerySync([&](NYdb::NQuery::TSession sqlc_session) -> NYdb::TStatus {
            return sqlc_execute(
                std::move(sqlc_session),
                NYdb::NQuery::TTxControl::BeginTx(this->tx_settings_).CommitTx()
            );
        }, this->retry_settings_);
    NYdb::NStatusHelpers::ThrowOnError(sqlc_status);
}

// -- name: GetVenue :one
std::optional<GetVenueRow> Queries::GetVenue(const std::string& slug, const std::string& city) const {
    std::optional<NYdb::TResultSet> sqlc_result_set;
    const auto sqlc_execute = [&](NYdb::NQuery::TSession sqlc_session, const NYdb::NQuery::TTxControl& sqlc_tx) -> NYdb::TStatus {
        auto sqlc_params = NYdb::TParamsBuilder()
            .AddParam("$slug").Utf8(slug).Build()
            .AddParam("$city").Utf8(city).Build()
            .Build();
        auto sqlc_result = sqlc_session.ExecuteQuery(
            R"sql(
                SELECT id, slug, name, city, status, statuses, spotify_playlist, songkick_id, tags, created_at
                FROM venue
                WHERE slug = $slug AND city = $city;
            )sql",
            sqlc_tx,
            sqlc_params,
            this->execute_settings_
        ).GetValueSync();
        if (sqlc_result.IsSuccess()) {
            if (sqlc_result.GetResultSets().size() != 1) {
                throw std::runtime_error("expected exactly one result set");
            }
            sqlc_result_set = sqlc_result.GetResultSet(0);
        }
        return sqlc_result;
    };
    const auto sqlc_status = this->transaction_ != nullptr
        ? sqlc_execute(this->transaction_->GetSession(), NYdb::NQuery::TTxControl::Tx(*this->transaction_))
        : this->client_->RetryQuerySync([&](NYdb::NQuery::TSession sqlc_session) -> NYdb::TStatus {
            return sqlc_execute(
                std::move(sqlc_session),
                NYdb::NQuery::TTxControl::BeginTx(this->tx_settings_).CommitTx()
            );
        }, this->retry_settings_);
    NYdb::NStatusHelpers::ThrowOnError(sqlc_status);
    if (!sqlc_result_set) {
        throw std::runtime_error("GetVenue: successful query returned no result set");
    }
    NYdb::TResultSetParser sqlc_parser(*sqlc_result_set);
    if (!sqlc_parser.TryNextRow()) {
        return std::nullopt;
    }
    GetVenueRow sqlc_row{
        sqlc_parser.ColumnParser("id").GetUint64(),
        sqlc_parser.ColumnParser("slug").GetUtf8(),
        sqlc_parser.ColumnParser("name").GetUtf8(),
        sqlc_parser.ColumnParser("city").GetUtf8(),
        sqlc_parser.ColumnParser("status").GetUtf8(),
        sqlc_parser.ColumnParser("statuses").GetOptionalJson(),
        sqlc_parser.ColumnParser("spotify_playlist").GetUtf8(),
        sqlc_parser.ColumnParser("songkick_id").GetOptionalUtf8(),
        sqlc_parser.ColumnParser("tags").GetOptionalJson(),
        sqlc_parser.ColumnParser("created_at").GetOptionalTimestamp(),
    };
    return sqlc_row;
}

// -- name: CreateVenue :one
std::optional<CreateVenueRow> Queries::CreateVenue(std::uint64_t id, const std::string& slug, const std::string& name, const std::string& city, const std::optional<TInstant>& created_at, const std::string& spotify_playlist, const std::string& status, const std::optional<std::string>& statuses, const std::optional<std::string>& tags) const {
    std::optional<NYdb::TResultSet> sqlc_result_set;
    const auto sqlc_execute = [&](NYdb::NQuery::TSession sqlc_session, const NYdb::NQuery::TTxControl& sqlc_tx) -> NYdb::TStatus {
        auto sqlc_params = NYdb::TParamsBuilder()
            .AddParam("$id").Uint64(id).Build()
            .AddParam("$slug").Utf8(slug).Build()
            .AddParam("$name").Utf8(name).Build()
            .AddParam("$city").Utf8(city).Build()
            .AddParam("$created_at").OptionalTimestamp(created_at).Build()
            .AddParam("$spotify_playlist").Utf8(spotify_playlist).Build()
            .AddParam("$status").Utf8(status).Build()
            .AddParam("$statuses").OptionalJson(statuses).Build()
            .AddParam("$tags").OptionalJson(tags).Build()
            .Build();
        auto sqlc_result = sqlc_session.ExecuteQuery(
            R"sql(
                INSERT INTO venue (
                    id,
                    slug,
                    name,
                    city,
                    created_at,
                    spotify_playlist,
                    status,
                    statuses,
                    tags
                ) VALUES (
                    $id,
                    $slug,
                    $name,
                    $city,
                    $created_at,
                    $spotify_playlist,
                    $status,
                    $statuses,
                    $tags
                ) RETURNING id;
            )sql",
            sqlc_tx,
            sqlc_params,
            this->execute_settings_
        ).GetValueSync();
        if (sqlc_result.IsSuccess()) {
            if (sqlc_result.GetResultSets().size() != 1) {
                throw std::runtime_error("expected exactly one result set");
            }
            sqlc_result_set = sqlc_result.GetResultSet(0);
        }
        return sqlc_result;
    };
    const auto sqlc_status = this->transaction_ != nullptr
        ? sqlc_execute(this->transaction_->GetSession(), NYdb::NQuery::TTxControl::Tx(*this->transaction_))
        : this->client_->RetryQuerySync([&](NYdb::NQuery::TSession sqlc_session) -> NYdb::TStatus {
            return sqlc_execute(
                std::move(sqlc_session),
                NYdb::NQuery::TTxControl::BeginTx(this->tx_settings_).CommitTx()
            );
        }, this->retry_settings_);
    NYdb::NStatusHelpers::ThrowOnError(sqlc_status);
    if (!sqlc_result_set) {
        throw std::runtime_error("CreateVenue: successful query returned no result set");
    }
    NYdb::TResultSetParser sqlc_parser(*sqlc_result_set);
    if (!sqlc_parser.TryNextRow()) {
        return std::nullopt;
    }
    CreateVenueRow sqlc_row{
        sqlc_parser.ColumnParser("id").GetUint64(),
    };
    return sqlc_row;
}

// -- name: UpdateVenueName :one
std::optional<UpdateVenueNameRow> Queries::UpdateVenueName(const std::string& name, const std::string& slug) const {
    std::optional<NYdb::TResultSet> sqlc_result_set;
    const auto sqlc_execute = [&](NYdb::NQuery::TSession sqlc_session, const NYdb::NQuery::TTxControl& sqlc_tx) -> NYdb::TStatus {
        auto sqlc_params = NYdb::TParamsBuilder()
            .AddParam("$name").Utf8(name).Build()
            .AddParam("$slug").Utf8(slug).Build()
            .Build();
        auto sqlc_result = sqlc_session.ExecuteQuery(
            R"sql(
                UPDATE venue
                SET name = $name
                WHERE slug = $slug
                RETURNING id;
            )sql",
            sqlc_tx,
            sqlc_params,
            this->execute_settings_
        ).GetValueSync();
        if (sqlc_result.IsSuccess()) {
            if (sqlc_result.GetResultSets().size() != 1) {
                throw std::runtime_error("expected exactly one result set");
            }
            sqlc_result_set = sqlc_result.GetResultSet(0);
        }
        return sqlc_result;
    };
    const auto sqlc_status = this->transaction_ != nullptr
        ? sqlc_execute(this->transaction_->GetSession(), NYdb::NQuery::TTxControl::Tx(*this->transaction_))
        : this->client_->RetryQuerySync([&](NYdb::NQuery::TSession sqlc_session) -> NYdb::TStatus {
            return sqlc_execute(
                std::move(sqlc_session),
                NYdb::NQuery::TTxControl::BeginTx(this->tx_settings_).CommitTx()
            );
        }, this->retry_settings_);
    NYdb::NStatusHelpers::ThrowOnError(sqlc_status);
    if (!sqlc_result_set) {
        throw std::runtime_error("UpdateVenueName: successful query returned no result set");
    }
    NYdb::TResultSetParser sqlc_parser(*sqlc_result_set);
    if (!sqlc_parser.TryNextRow()) {
        return std::nullopt;
    }
    UpdateVenueNameRow sqlc_row{
        sqlc_parser.ColumnParser("id").GetUint64(),
    };
    return sqlc_row;
}

// -- name: VenueCountByCity :many
std::vector<VenueCountByCityRow> Queries::VenueCountByCity() const {
    std::optional<NYdb::TResultSet> sqlc_result_set;
    const auto sqlc_execute = [&](NYdb::NQuery::TSession sqlc_session, const NYdb::NQuery::TTxControl& sqlc_tx) -> NYdb::TStatus {
        auto sqlc_result = sqlc_session.ExecuteQuery(
            R"sql(
                SELECT
                    city,
                    COUNT(*) AS venue_count
                FROM venue
                GROUP BY city
                ORDER BY city;
            )sql",
            sqlc_tx,
            this->execute_settings_
        ).GetValueSync();
        if (sqlc_result.IsSuccess()) {
            if (sqlc_result.GetResultSets().size() != 1) {
                throw std::runtime_error("expected exactly one result set");
            }
            sqlc_result_set = sqlc_result.GetResultSet(0);
        }
        return sqlc_result;
    };
    const auto sqlc_status = this->transaction_ != nullptr
        ? sqlc_execute(this->transaction_->GetSession(), NYdb::NQuery::TTxControl::Tx(*this->transaction_))
        : this->client_->RetryQuerySync([&](NYdb::NQuery::TSession sqlc_session) -> NYdb::TStatus {
            return sqlc_execute(
                std::move(sqlc_session),
                NYdb::NQuery::TTxControl::BeginTx(this->tx_settings_).CommitTx()
            );
        }, this->retry_settings_);
    NYdb::NStatusHelpers::ThrowOnError(sqlc_status);
    if (!sqlc_result_set) {
        throw std::runtime_error("VenueCountByCity: successful query returned no result set");
    }
    NYdb::TResultSetParser sqlc_parser(*sqlc_result_set);
    std::vector<VenueCountByCityRow> sqlc_rows;
    sqlc_rows.reserve(sqlc_result_set->RowsCount());
    while (sqlc_parser.TryNextRow()) {
        sqlc_rows.push_back(VenueCountByCityRow{
            sqlc_parser.ColumnParser("city").GetUtf8(),
            sqlc_parser.ColumnParser("venue_count").GetUint64(),
        });
    }
    return sqlc_rows;
}

}  // namespace ondeck::native
