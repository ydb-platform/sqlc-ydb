// Code generated by sqlc-ydb. DO NOT EDIT.
#include "queries.hpp"

namespace ondeck::userver {

// -- name: ListCities :many
std::vector<ListCitiesRow> Queries::ListCities() const {
    const auto sqlc_query = ::userver::ydb::Query{
        R"sql(
            SELECT slug, name
            FROM city
            ORDER BY name;
        )sql",
        ::userver::ydb::Query::Name{"ListCities"},
        ::userver::ydb::Query::LogMode::kNameOnly,
    };
    auto sqlc_response =
        this->transaction_ != nullptr
        ? this->transaction_->Execute(this->execute_settings_, sqlc_query)
        : this->client_->ExecuteQuery(this->operation_settings_, sqlc_query);
    auto sqlc_cursor = sqlc_response.GetSingleCursor();
    std::vector<ListCitiesRow> sqlc_rows;
    sqlc_rows.reserve(sqlc_cursor.size());
    for (auto sqlc_row : sqlc_cursor) {
        sqlc_rows.push_back(ListCitiesRow{
            sqlc_row.Get<::userver::ydb::Utf8>("slug"),
            sqlc_row.Get<::userver::ydb::Utf8>("name"),
        });
    }
    return sqlc_rows;
}

// -- name: GetCity :one
std::optional<GetCityRow> Queries::GetCity(const ::userver::ydb::Utf8& slug) const {
    const auto sqlc_query = ::userver::ydb::Query{
        R"sql(
            SELECT slug, name
            FROM city
            WHERE slug = $slug;
        )sql",
        ::userver::ydb::Query::Name{"GetCity"},
        ::userver::ydb::Query::LogMode::kNameOnly,
    };
    auto sqlc_response =
        this->transaction_ != nullptr
        ? this->transaction_->Execute(this->execute_settings_, sqlc_query, "$slug", slug)
        : this->client_->ExecuteQuery(this->operation_settings_, sqlc_query, "$slug", slug);
    auto sqlc_cursor = sqlc_response.GetSingleCursor();
    if (sqlc_cursor.empty()) {
        return std::nullopt;
    }
    auto sqlc_row = sqlc_cursor.GetFirstRow();
    return GetCityRow{
        sqlc_row.Get<::userver::ydb::Utf8>("slug"),
        sqlc_row.Get<::userver::ydb::Utf8>("name"),
    };
}

// -- name: CreateCity :one
std::optional<CreateCityRow> Queries::CreateCity(const ::userver::ydb::Utf8& name, const ::userver::ydb::Utf8& slug) const {
    const auto sqlc_query = ::userver::ydb::Query{
        R"sql(
            INSERT INTO city (
                name,
                slug
            ) VALUES (
                $name,
                $slug
            ) RETURNING slug, name;
        )sql",
        ::userver::ydb::Query::Name{"CreateCity"},
        ::userver::ydb::Query::LogMode::kNameOnly,
    };
    auto sqlc_response =
        this->transaction_ != nullptr
        ? this->transaction_->Execute(this->execute_settings_, sqlc_query, "$name", name, "$slug", slug)
        : this->client_->ExecuteQuery(this->operation_settings_, sqlc_query, "$name", name, "$slug", slug);
    auto sqlc_cursor = sqlc_response.GetSingleCursor();
    if (sqlc_cursor.empty()) {
        return std::nullopt;
    }
    auto sqlc_row = sqlc_cursor.GetFirstRow();
    return CreateCityRow{
        sqlc_row.Get<::userver::ydb::Utf8>("slug"),
        sqlc_row.Get<::userver::ydb::Utf8>("name"),
    };
}

// -- name: UpdateCityName :exec
void Queries::UpdateCityName(const ::userver::ydb::Utf8& name, const ::userver::ydb::Utf8& slug) const {
    const auto sqlc_query = ::userver::ydb::Query{
        R"sql(
            UPDATE city
            SET name = $name
            WHERE slug = $slug;
        )sql",
        ::userver::ydb::Query::Name{"UpdateCityName"},
        ::userver::ydb::Query::LogMode::kNameOnly,
    };
    static_cast<void>(
        this->transaction_ != nullptr
        ? this->transaction_->Execute(this->execute_settings_, sqlc_query, "$name", name, "$slug", slug)
        : this->client_->ExecuteQuery(this->operation_settings_, sqlc_query, "$name", name, "$slug", slug)
    );
}

// -- name: ListVenues :many
std::vector<ListVenuesRow> Queries::ListVenues(const ::userver::ydb::Utf8& city) const {
    const auto sqlc_query = ::userver::ydb::Query{
        R"sql(
            SELECT id, slug, name, city, status, statuses, spotify_playlist, songkick_id, tags, created_at
            FROM venue
            WHERE city = $city
            ORDER BY name;
        )sql",
        ::userver::ydb::Query::Name{"ListVenues"},
        ::userver::ydb::Query::LogMode::kNameOnly,
    };
    auto sqlc_response =
        this->transaction_ != nullptr
        ? this->transaction_->Execute(this->execute_settings_, sqlc_query, "$city", city)
        : this->client_->ExecuteQuery(this->operation_settings_, sqlc_query, "$city", city);
    auto sqlc_cursor = sqlc_response.GetSingleCursor();
    std::vector<ListVenuesRow> sqlc_rows;
    sqlc_rows.reserve(sqlc_cursor.size());
    for (auto sqlc_row : sqlc_cursor) {
        sqlc_rows.push_back(ListVenuesRow{
            sqlc_row.Get<std::uint64_t>("id"),
            sqlc_row.Get<::userver::ydb::Utf8>("slug"),
            sqlc_row.Get<::userver::ydb::Utf8>("name"),
            sqlc_row.Get<::userver::ydb::Utf8>("city"),
            sqlc_row.Get<::userver::ydb::Utf8>("status"),
            sqlc_row.Get<std::optional<::userver::formats::json::Value>>("statuses"),
            sqlc_row.Get<::userver::ydb::Utf8>("spotify_playlist"),
            sqlc_row.Get<std::optional<::userver::ydb::Utf8>>("songkick_id"),
            sqlc_row.Get<std::optional<::userver::formats::json::Value>>("tags"),
            sqlc_row.Get<std::optional<std::chrono::system_clock::time_point>>("created_at"),
        });
    }
    return sqlc_rows;
}

// -- name: DeleteVenue :exec
void Queries::DeleteVenue(const ::userver::ydb::Utf8& slug) const {
    const auto sqlc_query = ::userver::ydb::Query{
        R"sql(
            DELETE FROM venue
            WHERE slug = $slug AND slug = $slug;
        )sql",
        ::userver::ydb::Query::Name{"DeleteVenue"},
        ::userver::ydb::Query::LogMode::kNameOnly,
    };
    static_cast<void>(
        this->transaction_ != nullptr
        ? this->transaction_->Execute(this->execute_settings_, sqlc_query, "$slug", slug)
        : this->client_->ExecuteQuery(this->operation_settings_, sqlc_query, "$slug", slug)
    );
}

// -- name: GetVenue :one
std::optional<GetVenueRow> Queries::GetVenue(const ::userver::ydb::Utf8& slug, const ::userver::ydb::Utf8& city) const {
    const auto sqlc_query = ::userver::ydb::Query{
        R"sql(
            SELECT id, slug, name, city, status, statuses, spotify_playlist, songkick_id, tags, created_at
            FROM venue
            WHERE slug = $slug AND city = $city;
        )sql",
        ::userver::ydb::Query::Name{"GetVenue"},
        ::userver::ydb::Query::LogMode::kNameOnly,
    };
    auto sqlc_response =
        this->transaction_ != nullptr
        ? this->transaction_->Execute(this->execute_settings_, sqlc_query, "$slug", slug, "$city", city)
        : this->client_->ExecuteQuery(this->operation_settings_, sqlc_query, "$slug", slug, "$city", city);
    auto sqlc_cursor = sqlc_response.GetSingleCursor();
    if (sqlc_cursor.empty()) {
        return std::nullopt;
    }
    auto sqlc_row = sqlc_cursor.GetFirstRow();
    return GetVenueRow{
        sqlc_row.Get<std::uint64_t>("id"),
        sqlc_row.Get<::userver::ydb::Utf8>("slug"),
        sqlc_row.Get<::userver::ydb::Utf8>("name"),
        sqlc_row.Get<::userver::ydb::Utf8>("city"),
        sqlc_row.Get<::userver::ydb::Utf8>("status"),
        sqlc_row.Get<std::optional<::userver::formats::json::Value>>("statuses"),
        sqlc_row.Get<::userver::ydb::Utf8>("spotify_playlist"),
        sqlc_row.Get<std::optional<::userver::ydb::Utf8>>("songkick_id"),
        sqlc_row.Get<std::optional<::userver::formats::json::Value>>("tags"),
        sqlc_row.Get<std::optional<std::chrono::system_clock::time_point>>("created_at"),
    };
}

// -- name: CreateVenue :one
std::optional<CreateVenueRow> Queries::CreateVenue(std::uint64_t id, const ::userver::ydb::Utf8& slug, const ::userver::ydb::Utf8& name, const ::userver::ydb::Utf8& city, const std::optional<std::chrono::system_clock::time_point>& created_at, const ::userver::ydb::Utf8& spotify_playlist, const ::userver::ydb::Utf8& status, const std::optional<::userver::formats::json::Value>& statuses, const std::optional<::userver::formats::json::Value>& tags) const {
    const auto sqlc_query = ::userver::ydb::Query{
        R"sql(
            INSERT INTO venue (
                id,
                slug,
                name,
                city,
                created_at,
                spotify_playlist,
                status,
                statuses,
                tags
            ) VALUES (
                $id,
                $slug,
                $name,
                $city,
                $created_at,
                $spotify_playlist,
                $status,
                $statuses,
                $tags
            ) RETURNING id;
        )sql",
        ::userver::ydb::Query::Name{"CreateVenue"},
        ::userver::ydb::Query::LogMode::kNameOnly,
    };
    auto sqlc_response =
        this->transaction_ != nullptr
        ? this->transaction_->Execute(this->execute_settings_, sqlc_query, "$id", id, "$slug", slug, "$name", name, "$city", city, "$created_at", created_at, "$spotify_playlist", spotify_playlist, "$status", status, "$statuses", statuses, "$tags", tags)
        : this->client_->ExecuteQuery(this->operation_settings_, sqlc_query, "$id", id, "$slug", slug, "$name", name, "$city", city, "$created_at", created_at, "$spotify_playlist", spotify_playlist, "$status", status, "$statuses", statuses, "$tags", tags);
    auto sqlc_cursor = sqlc_response.GetSingleCursor();
    if (sqlc_cursor.empty()) {
        return std::nullopt;
    }
    auto sqlc_row = sqlc_cursor.GetFirstRow();
    return CreateVenueRow{
        sqlc_row.Get<std::uint64_t>("id"),
    };
}

// -- name: UpdateVenueName :one
std::optional<UpdateVenueNameRow> Queries::UpdateVenueName(const ::userver::ydb::Utf8& name, const ::userver::ydb::Utf8& slug) const {
    const auto sqlc_query = ::userver::ydb::Query{
        R"sql(
            UPDATE venue
            SET name = $name
            WHERE slug = $slug
            RETURNING id;
        )sql",
        ::userver::ydb::Query::Name{"UpdateVenueName"},
        ::userver::ydb::Query::LogMode::kNameOnly,
    };
    auto sqlc_response =
        this->transaction_ != nullptr
        ? this->transaction_->Execute(this->execute_settings_, sqlc_query, "$name", name, "$slug", slug)
        : this->client_->ExecuteQuery(this->operation_settings_, sqlc_query, "$name", name, "$slug", slug);
    auto sqlc_cursor = sqlc_response.GetSingleCursor();
    if (sqlc_cursor.empty()) {
        return std::nullopt;
    }
    auto sqlc_row = sqlc_cursor.GetFirstRow();
    return UpdateVenueNameRow{
        sqlc_row.Get<std::uint64_t>("id"),
    };
}

// -- name: VenueCountByCity :many
std::vector<VenueCountByCityRow> Queries::VenueCountByCity() const {
    const auto sqlc_query = ::userver::ydb::Query{
        R"sql(
            SELECT
                city,
                COUNT(*) AS venue_count
            FROM venue
            GROUP BY city
            ORDER BY city;
        )sql",
        ::userver::ydb::Query::Name{"VenueCountByCity"},
        ::userver::ydb::Query::LogMode::kNameOnly,
    };
    auto sqlc_response =
        this->transaction_ != nullptr
        ? this->transaction_->Execute(this->execute_settings_, sqlc_query)
        : this->client_->ExecuteQuery(this->operation_settings_, sqlc_query);
    auto sqlc_cursor = sqlc_response.GetSingleCursor();
    std::vector<VenueCountByCityRow> sqlc_rows;
    sqlc_rows.reserve(sqlc_cursor.size());
    for (auto sqlc_row : sqlc_cursor) {
        sqlc_rows.push_back(VenueCountByCityRow{
            sqlc_row.Get<::userver::ydb::Utf8>("city"),
            sqlc_row.Get<std::uint64_t>("venue_count"),
        });
    }
    return sqlc_rows;
}

}  // namespace ondeck::userver
