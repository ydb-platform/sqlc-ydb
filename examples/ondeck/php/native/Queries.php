<?php
// Code generated by sqlc-ydb. DO NOT EDIT.
declare(strict_types=1);

namespace Ondeck\Native;

require_once __DIR__ . '/YdbRuntime.php';

use Closure;
use UnexpectedValueException;
use Ydb\Table\ExecuteQueryResult;
use Ydb\Type\PrimitiveTypeId;
use YdbPlatform\Ydb\Session;
use YdbPlatform\Ydb\Table;

final class Queries
{
    public const LIST_CITIES_SQL = <<<'SQLC_YDB_YQL'
-- name: ListCities :many
SELECT slug, name
FROM city
ORDER BY name;
SQLC_YDB_YQL;

    public const GET_CITY_SQL = <<<'SQLC_YDB_YQL'
-- name: GetCity :one
SELECT slug, name
FROM city
WHERE slug = $slug;
SQLC_YDB_YQL;

    public const CREATE_CITY_SQL = <<<'SQLC_YDB_YQL'
-- name: CreateCity :one
INSERT INTO city (
    name,
    slug
) VALUES (
    $name,
    $slug
) RETURNING slug, name;
SQLC_YDB_YQL;

    public const UPDATE_CITY_NAME_SQL = <<<'SQLC_YDB_YQL'
-- name: UpdateCityName :exec
UPDATE city
SET name = $name
WHERE slug = $slug;
SQLC_YDB_YQL;

    public const LIST_VENUES_SQL = <<<'SQLC_YDB_YQL'
-- name: ListVenues :many
SELECT id, slug, name, city, status, statuses, spotify_playlist, songkick_id, tags, created_at
FROM venue
WHERE city = $city
ORDER BY name;
SQLC_YDB_YQL;

    public const DELETE_VENUE_SQL = <<<'SQLC_YDB_YQL'
-- name: DeleteVenue :exec
DELETE FROM venue
WHERE slug = $slug AND slug = $slug;
SQLC_YDB_YQL;

    public const GET_VENUE_SQL = <<<'SQLC_YDB_YQL'
-- name: GetVenue :one
SELECT id, slug, name, city, status, statuses, spotify_playlist, songkick_id, tags, created_at
FROM venue
WHERE slug = $slug AND city = $city;
SQLC_YDB_YQL;

    public const CREATE_VENUE_SQL = <<<'SQLC_YDB_YQL'
-- name: CreateVenue :one
INSERT INTO venue (
    id,
    slug,
    name,
    city,
    created_at,
    spotify_playlist,
    status,
    statuses,
    tags
) VALUES (
    $id,
    $slug,
    $name,
    $city,
    $created_at,
    $spotify_playlist,
    $status,
    $statuses,
    $tags
) RETURNING id;
SQLC_YDB_YQL;

    public const UPDATE_VENUE_NAME_SQL = <<<'SQLC_YDB_YQL'
-- name: UpdateVenueName :one
UPDATE venue
SET name = $name
WHERE slug = $slug
RETURNING id;
SQLC_YDB_YQL;

    public const VENUE_COUNT_BY_CITY_SQL = <<<'SQLC_YDB_YQL'
-- name: VenueCountByCity :many
SELECT
    city,
    COUNT(*) AS venue_count
FROM venue
GROUP BY city
ORDER BY city;
SQLC_YDB_YQL;

    public function __construct(private readonly Table $table)
    {
        if (PHP_INT_SIZE !== 8) {
            throw new \LogicException('sqlc-ydb generated PHP code requires a 64-bit PHP runtime');
        }
    }

    /** @return list<ListCitiesRow> */
    public function listCities(): array
    {
        $parameters = [
        ];
        $result = $this->table->retrySession(function (Session $session) use ($parameters): ExecuteQueryResult {
            $query = $session->newQuery(self::LIST_CITIES_SQL)
                ->parameters($parameters)
                ->beginTx('serializable_read_write');
            return (new YdbRawExecutor($this->table))->execute($session, $query);
        }, false);
        $rows = $this->decodeRows(
            $result,
            'ListCities',
            [
                ['slug', PrimitiveTypeId::UTF8, false],
                ['name', PrimitiveTypeId::UTF8, false],
            ],
            static fn($items): ListCitiesRow => new ListCitiesRow(
                YdbValueCodec::utf8($items->offsetGet(0), 'ListCities.slug'),
                YdbValueCodec::utf8($items->offsetGet(1), 'ListCities.name'),
            ),
        );
        return $rows;
    }

    public function getCity(string $slug): ?GetCityRow
    {
        $parameters = [
            '$slug' => YdbValueCodec::typedUtf8($slug, 'slug'),
        ];
        $result = $this->table->retrySession(function (Session $session) use ($parameters): ExecuteQueryResult {
            $query = $session->newQuery(self::GET_CITY_SQL)
                ->parameters($parameters)
                ->beginTx('serializable_read_write');
            return (new YdbRawExecutor($this->table))->execute($session, $query);
        }, false);
        $rows = $this->decodeRows(
            $result,
            'GetCity',
            [
                ['slug', PrimitiveTypeId::UTF8, false],
                ['name', PrimitiveTypeId::UTF8, false],
            ],
            static fn($items): GetCityRow => new GetCityRow(
                YdbValueCodec::utf8($items->offsetGet(0), 'GetCity.slug'),
                YdbValueCodec::utf8($items->offsetGet(1), 'GetCity.name'),
            ),
        );
        return $rows[0] ?? null;
    }

    public function createCity(CreateCityParams $params): ?CreateCityRow
    {
        $parameters = [
            '$name' => YdbValueCodec::typedUtf8($params->name, 'name'),
            '$slug' => YdbValueCodec::typedUtf8($params->slug, 'slug'),
        ];
        $result = $this->table->retrySession(function (Session $session) use ($parameters): ExecuteQueryResult {
            $query = $session->newQuery(self::CREATE_CITY_SQL)
                ->parameters($parameters)
                ->beginTx('serializable_read_write');
            return (new YdbRawExecutor($this->table))->execute($session, $query);
        }, false);
        $rows = $this->decodeRows(
            $result,
            'CreateCity',
            [
                ['slug', PrimitiveTypeId::UTF8, false],
                ['name', PrimitiveTypeId::UTF8, false],
            ],
            static fn($items): CreateCityRow => new CreateCityRow(
                YdbValueCodec::utf8($items->offsetGet(0), 'CreateCity.slug'),
                YdbValueCodec::utf8($items->offsetGet(1), 'CreateCity.name'),
            ),
        );
        return $rows[0] ?? null;
    }

    public function updateCityName(UpdateCityNameParams $params): void
    {
        $parameters = [
            '$name' => YdbValueCodec::typedUtf8($params->name, 'name'),
            '$slug' => YdbValueCodec::typedUtf8($params->slug, 'slug'),
        ];
        $result = $this->table->retrySession(function (Session $session) use ($parameters): ExecuteQueryResult {
            $query = $session->newQuery(self::UPDATE_CITY_NAME_SQL)
                ->parameters($parameters)
                ->beginTx('serializable_read_write');
            return (new YdbRawExecutor($this->table))->execute($session, $query);
        }, false);
    }

    /** @return list<ListVenuesRow> */
    public function listVenues(string $city): array
    {
        $parameters = [
            '$city' => YdbValueCodec::typedUtf8($city, 'city'),
        ];
        $result = $this->table->retrySession(function (Session $session) use ($parameters): ExecuteQueryResult {
            $query = $session->newQuery(self::LIST_VENUES_SQL)
                ->parameters($parameters)
                ->beginTx('serializable_read_write');
            return (new YdbRawExecutor($this->table))->execute($session, $query);
        }, false);
        $rows = $this->decodeRows(
            $result,
            'ListVenues',
            [
                ['id', PrimitiveTypeId::UINT64, false],
                ['slug', PrimitiveTypeId::UTF8, false],
                ['name', PrimitiveTypeId::UTF8, false],
                ['city', PrimitiveTypeId::UTF8, false],
                ['status', PrimitiveTypeId::UTF8, false],
                ['statuses', PrimitiveTypeId::JSON, true],
                ['spotify_playlist', PrimitiveTypeId::UTF8, false],
                ['songkick_id', PrimitiveTypeId::UTF8, true],
                ['tags', PrimitiveTypeId::JSON, true],
                ['created_at', PrimitiveTypeId::TIMESTAMP, true],
            ],
            static fn($items): ListVenuesRow => new ListVenuesRow(
                YdbValueCodec::uint64($items->offsetGet(0), 'ListVenues.id'),
                YdbValueCodec::utf8($items->offsetGet(1), 'ListVenues.slug'),
                YdbValueCodec::utf8($items->offsetGet(2), 'ListVenues.name'),
                YdbValueCodec::utf8($items->offsetGet(3), 'ListVenues.city'),
                YdbValueCodec::utf8($items->offsetGet(4), 'ListVenues.status'),
                YdbValueCodec::optionalJson($items->offsetGet(5), 'ListVenues.statuses'),
                YdbValueCodec::utf8($items->offsetGet(6), 'ListVenues.spotify_playlist'),
                YdbValueCodec::optionalUtf8($items->offsetGet(7), 'ListVenues.songkick_id'),
                YdbValueCodec::optionalJson($items->offsetGet(8), 'ListVenues.tags'),
                YdbValueCodec::optionalTimestamp($items->offsetGet(9), 'ListVenues.created_at'),
            ),
        );
        return $rows;
    }

    public function deleteVenue(string $slug): void
    {
        $parameters = [
            '$slug' => YdbValueCodec::typedUtf8($slug, 'slug'),
        ];
        $result = $this->table->retrySession(function (Session $session) use ($parameters): ExecuteQueryResult {
            $query = $session->newQuery(self::DELETE_VENUE_SQL)
                ->parameters($parameters)
                ->beginTx('serializable_read_write');
            return (new YdbRawExecutor($this->table))->execute($session, $query);
        }, false);
    }

    public function getVenue(GetVenueParams $params): ?GetVenueRow
    {
        $parameters = [
            '$slug' => YdbValueCodec::typedUtf8($params->slug, 'slug'),
            '$city' => YdbValueCodec::typedUtf8($params->city, 'city'),
        ];
        $result = $this->table->retrySession(function (Session $session) use ($parameters): ExecuteQueryResult {
            $query = $session->newQuery(self::GET_VENUE_SQL)
                ->parameters($parameters)
                ->beginTx('serializable_read_write');
            return (new YdbRawExecutor($this->table))->execute($session, $query);
        }, false);
        $rows = $this->decodeRows(
            $result,
            'GetVenue',
            [
                ['id', PrimitiveTypeId::UINT64, false],
                ['slug', PrimitiveTypeId::UTF8, false],
                ['name', PrimitiveTypeId::UTF8, false],
                ['city', PrimitiveTypeId::UTF8, false],
                ['status', PrimitiveTypeId::UTF8, false],
                ['statuses', PrimitiveTypeId::JSON, true],
                ['spotify_playlist', PrimitiveTypeId::UTF8, false],
                ['songkick_id', PrimitiveTypeId::UTF8, true],
                ['tags', PrimitiveTypeId::JSON, true],
                ['created_at', PrimitiveTypeId::TIMESTAMP, true],
            ],
            static fn($items): GetVenueRow => new GetVenueRow(
                YdbValueCodec::uint64($items->offsetGet(0), 'GetVenue.id'),
                YdbValueCodec::utf8($items->offsetGet(1), 'GetVenue.slug'),
                YdbValueCodec::utf8($items->offsetGet(2), 'GetVenue.name'),
                YdbValueCodec::utf8($items->offsetGet(3), 'GetVenue.city'),
                YdbValueCodec::utf8($items->offsetGet(4), 'GetVenue.status'),
                YdbValueCodec::optionalJson($items->offsetGet(5), 'GetVenue.statuses'),
                YdbValueCodec::utf8($items->offsetGet(6), 'GetVenue.spotify_playlist'),
                YdbValueCodec::optionalUtf8($items->offsetGet(7), 'GetVenue.songkick_id'),
                YdbValueCodec::optionalJson($items->offsetGet(8), 'GetVenue.tags'),
                YdbValueCodec::optionalTimestamp($items->offsetGet(9), 'GetVenue.created_at'),
            ),
        );
        return $rows[0] ?? null;
    }

    public function createVenue(CreateVenueParams $params): ?CreateVenueRow
    {
        $parameters = [
            '$id' => YdbValueCodec::typedUint64($params->id, 'id'),
            '$slug' => YdbValueCodec::typedUtf8($params->slug, 'slug'),
            '$name' => YdbValueCodec::typedUtf8($params->name, 'name'),
            '$city' => YdbValueCodec::typedUtf8($params->city, 'city'),
            '$created_at' => YdbValueCodec::typedOptionalTimestamp($params->createdAt, 'created_at'),
            '$spotify_playlist' => YdbValueCodec::typedUtf8($params->spotifyPlaylist, 'spotify_playlist'),
            '$status' => YdbValueCodec::typedUtf8($params->status, 'status'),
            '$statuses' => YdbValueCodec::typedOptionalJson($params->statuses, 'statuses'),
            '$tags' => YdbValueCodec::typedOptionalJson($params->tags, 'tags'),
        ];
        $result = $this->table->retrySession(function (Session $session) use ($parameters): ExecuteQueryResult {
            $query = $session->newQuery(self::CREATE_VENUE_SQL)
                ->parameters($parameters)
                ->beginTx('serializable_read_write');
            return (new YdbRawExecutor($this->table))->execute($session, $query);
        }, false);
        $rows = $this->decodeRows(
            $result,
            'CreateVenue',
            [
                ['id', PrimitiveTypeId::UINT64, false],
            ],
            static fn($items): CreateVenueRow => new CreateVenueRow(
                YdbValueCodec::uint64($items->offsetGet(0), 'CreateVenue.id'),
            ),
        );
        return $rows[0] ?? null;
    }

    public function updateVenueName(UpdateVenueNameParams $params): ?UpdateVenueNameRow
    {
        $parameters = [
            '$name' => YdbValueCodec::typedUtf8($params->name, 'name'),
            '$slug' => YdbValueCodec::typedUtf8($params->slug, 'slug'),
        ];
        $result = $this->table->retrySession(function (Session $session) use ($parameters): ExecuteQueryResult {
            $query = $session->newQuery(self::UPDATE_VENUE_NAME_SQL)
                ->parameters($parameters)
                ->beginTx('serializable_read_write');
            return (new YdbRawExecutor($this->table))->execute($session, $query);
        }, false);
        $rows = $this->decodeRows(
            $result,
            'UpdateVenueName',
            [
                ['id', PrimitiveTypeId::UINT64, false],
            ],
            static fn($items): UpdateVenueNameRow => new UpdateVenueNameRow(
                YdbValueCodec::uint64($items->offsetGet(0), 'UpdateVenueName.id'),
            ),
        );
        return $rows[0] ?? null;
    }

    /** @return list<VenueCountByCityRow> */
    public function venueCountByCity(): array
    {
        $parameters = [
        ];
        $result = $this->table->retrySession(function (Session $session) use ($parameters): ExecuteQueryResult {
            $query = $session->newQuery(self::VENUE_COUNT_BY_CITY_SQL)
                ->parameters($parameters)
                ->beginTx('serializable_read_write');
            return (new YdbRawExecutor($this->table))->execute($session, $query);
        }, false);
        $rows = $this->decodeRows(
            $result,
            'VenueCountByCity',
            [
                ['city', PrimitiveTypeId::UTF8, false],
                ['venue_count', PrimitiveTypeId::UINT64, false],
            ],
            static fn($items): VenueCountByCityRow => new VenueCountByCityRow(
                YdbValueCodec::utf8($items->offsetGet(0), 'VenueCountByCity.city'),
                YdbValueCodec::uint64($items->offsetGet(1), 'VenueCountByCity.venue_count'),
            ),
        );
        return $rows;
    }

    /**
     * @param array<int, array{0: string, 1: int, 2: bool}> $expectedColumns
     * @return list<object>
     */
    private function decodeRows(ExecuteQueryResult $result, string $query, array $expectedColumns, Closure $decode): array
    {
        $sets = $result->getResultSets();
        if (count($sets) !== 1) {
            throw new UnexpectedValueException(sprintf('%s: expected one YDB result set, got %d', $query, count($sets)));
        }
        $set = $sets->offsetGet(0);
        $columns = $set->getColumns();
        if (count($columns) !== count($expectedColumns)) {
            throw new UnexpectedValueException(sprintf('%s: expected %d result columns, got %d', $query, count($expectedColumns), count($columns)));
        }
        foreach ($expectedColumns as $index => [$name, $typeId, $optional]) {
            $column = $columns->offsetGet($index);
            if ($column->getName() !== $name) {
                throw new UnexpectedValueException(sprintf('%s: result column %d is %s, expected %s', $query, $index, $column->getName(), $name));
            }
            YdbValueCodec::assertType($column->getType(), $typeId, $optional, $query . '.' . $name);
        }
        $rows = [];
        foreach ($set->getRows() as $rowIndex => $row) {
            $items = $row->getItems();
            if (count($items) !== count($expectedColumns)) {
                throw new UnexpectedValueException(sprintf('%s: row %d has %d values, expected %d', $query, $rowIndex, count($items), count($expectedColumns)));
            }
            $rows[] = $decode($items);
        }
        return $rows;
    }
}
