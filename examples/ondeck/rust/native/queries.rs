// Code generated by sqlc-ydb. DO NOT EDIT.

use super::models::*;

pub const LIST_CITIES: &str = r"-- name: ListCities :many
SELECT slug, name
FROM city
ORDER BY name;";

pub const GET_CITY: &str = r"-- name: GetCity :one
DECLARE $slug AS Utf8;
SELECT slug, name
FROM city
WHERE slug = $slug;";

pub const CREATE_CITY: &str = r"-- name: CreateCity :one
DECLARE $name AS Utf8;
DECLARE $slug AS Utf8;
INSERT INTO city (
    name,
    slug
) VALUES (
    $name,
    $slug
) RETURNING slug, name;";

pub const UPDATE_CITY_NAME: &str = r"-- name: UpdateCityName :exec
DECLARE $name AS Utf8;
DECLARE $slug AS Utf8;
UPDATE city
SET name = $name
WHERE slug = $slug;";

pub const LIST_VENUES: &str = r"-- name: ListVenues :many
DECLARE $city AS Utf8;
SELECT id, slug, name, city, status, statuses, spotify_playlist, songkick_id, tags, created_at
FROM venue
WHERE city = $city
ORDER BY name;";

pub const DELETE_VENUE: &str = r"-- name: DeleteVenue :exec
DECLARE $slug AS Utf8;
DELETE FROM venue
WHERE slug = $slug AND slug = $slug;";

pub const GET_VENUE: &str = r"-- name: GetVenue :one
DECLARE $slug AS Utf8;
DECLARE $city AS Utf8;
SELECT id, slug, name, city, status, statuses, spotify_playlist, songkick_id, tags, created_at
FROM venue
WHERE slug = $slug AND city = $city;";

pub const CREATE_VENUE: &str = r"-- name: CreateVenue :one
DECLARE $id AS Uint64;
DECLARE $slug AS Utf8;
DECLARE $name AS Utf8;
DECLARE $city AS Utf8;
DECLARE $created_at AS Optional<Timestamp>;
DECLARE $spotify_playlist AS Utf8;
DECLARE $status AS Utf8;
DECLARE $statuses AS Optional<Json>;
DECLARE $tags AS Optional<Json>;
INSERT INTO venue (
    id,
    slug,
    name,
    city,
    created_at,
    spotify_playlist,
    status,
    statuses,
    tags
) VALUES (
    $id,
    $slug,
    $name,
    $city,
    $created_at,
    $spotify_playlist,
    $status,
    $statuses,
    $tags
) RETURNING id;";

pub const UPDATE_VENUE_NAME: &str = r"-- name: UpdateVenueName :one
DECLARE $name AS Utf8;
DECLARE $slug AS Utf8;
UPDATE venue
SET name = $name
WHERE slug = $slug
RETURNING id;";

pub const VENUE_COUNT_BY_CITY: &str = r"-- name: VenueCountByCity :many
SELECT
    city,
    COUNT(*) AS venue_count
FROM venue
GROUP BY city
ORDER BY city;";

#[derive(Default)]
struct JsonParam(String);

impl From<JsonParam> for ydb::Value {
    fn from(value: JsonParam) -> Self {
        ydb::Value::Json(value.0)
    }
}

struct TimestampParam(std::time::SystemTime);

impl Default for TimestampParam {
    fn default() -> Self {
        Self(std::time::SystemTime::UNIX_EPOCH)
    }
}

impl From<TimestampParam> for ydb::Value {
    fn from(value: TimestampParam) -> Self {
        ydb::Value::Timestamp(value.0)
    }
}

pub struct Queries<'a> {
    client: &'a mut ydb::QueryClient,
}

impl<'a> Queries<'a> {
    pub fn new(client: &'a mut ydb::QueryClient) -> Self {
        Self { client }
    }

    pub async fn list_cities(&mut self) -> ydb::YdbResult<Vec<ListCitiesRow>> {
        let call = self.client.query_result_set(LIST_CITIES);
        let result_set = call.await?;
        let mut rows = Vec::new();
        for mut row in result_set.rows() {
            rows.push(ListCitiesRow {
                slug: row.remove_field(0)?.try_into()?,
                name: row.remove_field(1)?.try_into()?,
            });
        }
        Ok(rows)
    }

    pub async fn get_city(&mut self, slug: String) -> ydb::YdbResult<GetCityRow> {
        let call = self.client.query_result_set(GET_CITY)
            .param("$slug", slug);
        let result_set = call.await?;
        let mut row = result_set.rows().next().ok_or(ydb::YdbError::NoRows)?;
        Ok(GetCityRow {
            slug: row.remove_field(0)?.try_into()?,
            name: row.remove_field(1)?.try_into()?,
        })
    }

    pub async fn create_city(&mut self, name: String, slug: String) -> ydb::YdbResult<CreateCityRow> {
        let call = self.client.query_result_set(CREATE_CITY)
            .param("$name", name)
            .param("$slug", slug);
        let result_set = call.await?;
        let mut row = result_set.rows().next().ok_or(ydb::YdbError::NoRows)?;
        Ok(CreateCityRow {
            slug: row.remove_field(0)?.try_into()?,
            name: row.remove_field(1)?.try_into()?,
        })
    }

    pub async fn update_city_name(&mut self, name: String, slug: String) -> ydb::YdbResult<()> {
        let call = self.client.exec(UPDATE_CITY_NAME)
            .param("$name", name)
            .param("$slug", slug);
        call.await
    }

    pub async fn list_venues(&mut self, city: String) -> ydb::YdbResult<Vec<ListVenuesRow>> {
        let call = self.client.query_result_set(LIST_VENUES)
            .param("$city", city);
        let result_set = call.await?;
        let mut rows = Vec::new();
        for mut row in result_set.rows() {
            rows.push(ListVenuesRow {
                id: row.remove_field(0)?.try_into()?,
                slug: row.remove_field(1)?.try_into()?,
                name: row.remove_field(2)?.try_into()?,
                city: row.remove_field(3)?.try_into()?,
                status: row.remove_field(4)?.try_into()?,
                statuses: row.remove_field(5)?.try_into()?,
                spotify_playlist: row.remove_field(6)?.try_into()?,
                songkick_id: row.remove_field(7)?.try_into()?,
                tags: row.remove_field(8)?.try_into()?,
                created_at: row.remove_field(9)?.try_into()?,
            });
        }
        Ok(rows)
    }

    pub async fn delete_venue(&mut self, slug: String) -> ydb::YdbResult<()> {
        let call = self.client.exec(DELETE_VENUE)
            .param("$slug", slug);
        call.await
    }

    pub async fn get_venue(&mut self, slug: String, city: String) -> ydb::YdbResult<GetVenueRow> {
        let call = self.client.query_result_set(GET_VENUE)
            .param("$slug", slug)
            .param("$city", city);
        let result_set = call.await?;
        let mut row = result_set.rows().next().ok_or(ydb::YdbError::NoRows)?;
        Ok(GetVenueRow {
            id: row.remove_field(0)?.try_into()?,
            slug: row.remove_field(1)?.try_into()?,
            name: row.remove_field(2)?.try_into()?,
            city: row.remove_field(3)?.try_into()?,
            status: row.remove_field(4)?.try_into()?,
            statuses: row.remove_field(5)?.try_into()?,
            spotify_playlist: row.remove_field(6)?.try_into()?,
            songkick_id: row.remove_field(7)?.try_into()?,
            tags: row.remove_field(8)?.try_into()?,
            created_at: row.remove_field(9)?.try_into()?,
        })
    }

    pub async fn create_venue(&mut self, id: u64, slug: String, name: String, city: String, created_at: Option<std::time::SystemTime>, spotify_playlist: String, status: String, statuses: Option<String>, tags: Option<String>) -> ydb::YdbResult<CreateVenueRow> {
        let call = self.client.query_result_set(CREATE_VENUE)
            .param("$id", id)
            .param("$slug", slug)
            .param("$name", name)
            .param("$city", city)
            .param("$created_at", created_at.map(TimestampParam))
            .param("$spotify_playlist", spotify_playlist)
            .param("$status", status)
            .param("$statuses", statuses.map(JsonParam))
            .param("$tags", tags.map(JsonParam));
        let result_set = call.await?;
        let mut row = result_set.rows().next().ok_or(ydb::YdbError::NoRows)?;
        Ok(CreateVenueRow {
            id: row.remove_field(0)?.try_into()?,
        })
    }

    pub async fn update_venue_name(&mut self, name: String, slug: String) -> ydb::YdbResult<UpdateVenueNameRow> {
        let call = self.client.query_result_set(UPDATE_VENUE_NAME)
            .param("$name", name)
            .param("$slug", slug);
        let result_set = call.await?;
        let mut row = result_set.rows().next().ok_or(ydb::YdbError::NoRows)?;
        Ok(UpdateVenueNameRow {
            id: row.remove_field(0)?.try_into()?,
        })
    }

    pub async fn venue_count_by_city(&mut self) -> ydb::YdbResult<Vec<VenueCountByCityRow>> {
        let call = self.client.query_result_set(VENUE_COUNT_BY_CITY);
        let result_set = call.await?;
        let mut rows = Vec::new();
        for mut row in result_set.rows() {
            rows.push(VenueCountByCityRow {
                city: row.remove_field(0)?.try_into()?,
                venue_count: row.remove_field(1)?.try_into()?,
            });
        }
        Ok(rows)
    }

}
