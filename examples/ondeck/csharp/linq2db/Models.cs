// Code generated by sqlc-ydb. DO NOT EDIT.
#nullable enable
using System;

namespace Ondeck.Linq2DB;
public sealed record City(
    string Slug,
    string Name
);

public sealed record Venue(
    ulong ID,
    string Status,
    string? Statuses,
    string Slug,
    string Name,
    string City,
    string SpotifyPlaylist,
    string? SongkickID,
    string? Tags,
    DateTime? CreatedAt
);

public sealed record ListCitiesRow(
    string Slug,
    string Name
);

public sealed record GetCityRow(
    string Slug,
    string Name
);

public sealed record CreateCityParams(
    string Name,
    string Slug
);

public sealed record CreateCityRow(
    string Slug,
    string Name
);

public sealed record UpdateCityNameParams(
    string Name,
    string Slug
);

public sealed record ListVenuesRow(
    ulong ID,
    string Slug,
    string Name,
    string City,
    string Status,
    string? Statuses,
    string SpotifyPlaylist,
    string? SongkickID,
    string? Tags,
    DateTime? CreatedAt
);

public sealed record GetVenueParams(
    string Slug,
    string City
);

public sealed record GetVenueRow(
    ulong ID,
    string Slug,
    string Name,
    string City,
    string Status,
    string? Statuses,
    string SpotifyPlaylist,
    string? SongkickID,
    string? Tags,
    DateTime? CreatedAt
);

public sealed record CreateVenueParams(
    ulong ID,
    string Slug,
    string Name,
    string City,
    DateTime? CreatedAt,
    string SpotifyPlaylist,
    string Status,
    string? Statuses,
    string? Tags
);

public sealed record CreateVenueRow(
    ulong ID
);

public sealed record UpdateVenueNameParams(
    string Name,
    string Slug
);

public sealed record UpdateVenueNameRow(
    ulong ID
);

public sealed record VenueCountByCityRow(
    string City,
    ulong VenueCount
);
