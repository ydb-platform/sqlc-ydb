// Code generated by sqlc-ydb. DO NOT EDIT.
#nullable enable
using System;

namespace Jets.Linq2DB;
public sealed record Pilots(
    int ID,
    string Name
);

public sealed record Jets(
    int ID,
    int PilotID,
    int Age,
    string Name,
    string Color
);

public sealed record Languages(
    int ID,
    string Language
);

public sealed record PilotLanguages(
    int PilotID,
    int LanguageID
);

public sealed record CountPilotsRow(
    ulong PilotCount
);

public sealed record ListPilotsRow(
    int ID,
    string Name
);
