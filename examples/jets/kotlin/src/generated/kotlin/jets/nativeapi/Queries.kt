// Code generated by sqlc-ydb. DO NOT EDIT.
package jets.nativeapi

import tech.ydb.query.QueryTransaction
import tech.ydb.query.tools.SessionRetryContext
import tech.ydb.query.tools.QueryReader
import tech.ydb.common.transaction.TxMode
import tech.ydb.table.query.Params
import tech.ydb.table.values.PrimitiveValue
import tech.ydb.table.values.PrimitiveType
import tech.ydb.table.values.OptionalType

// The caller owns the injected executor and its lifecycle.
class Queries {
    private val client: SessionRetryContext?
    private val transaction: QueryTransaction?

    constructor(client: SessionRetryContext) {
        this.client = client
        this.transaction = null
    }

    constructor(transaction: QueryTransaction) {
        this.client = null
        this.transaction = transaction
    }

    // -- name: CountPilots :one
    fun countPilots(): CountPilotsRow? {
        val _params = Params.create()
        val _query = if (transaction != null) {
            QueryReader.readFrom(transaction.createQuery(
                "SELECT COUNT(*) AS pilot_count FROM pilots;", _params)).join().getValue()
        } else {
            client!!.supplyResult { _session ->
                QueryReader.readFrom(_session.createQuery(
                "SELECT COUNT(*) AS pilot_count FROM pilots;", TxMode.SERIALIZABLE_RW, _params))
            }.join().getValue()
        }
        kotlin.check(_query.getResultSetCount() == 1) { "Expected one result set" }
        val _rows = _query.getResultSet(0)
        if (!_rows.next()) return null
        val _value0: Long = _rows.getColumn(0).getUint64()
        return CountPilotsRow(_value0)
    }

    // -- name: ListPilots :many
    fun listPilots(): List<ListPilotsRow> {
        val _params = Params.create()
        val _query = if (transaction != null) {
            QueryReader.readFrom(transaction.createQuery(
                "SELECT id, name FROM pilots ORDER BY id LIMIT 5;", _params)).join().getValue()
        } else {
            client!!.supplyResult { _session ->
                QueryReader.readFrom(_session.createQuery(
                "SELECT id, name FROM pilots ORDER BY id LIMIT 5;", TxMode.SERIALIZABLE_RW, _params))
            }.join().getValue()
        }
        kotlin.check(_query.getResultSetCount() == 1) { "Expected one result set" }
        val _rows = _query.getResultSet(0)
        val _items = ArrayList<ListPilotsRow>()
        while (_rows.next()) {
            val _value0: Int = _rows.getColumn(0).getInt32()
            val _value1: String = _rows.getColumn(1).getText()
            _items.add(ListPilotsRow(_value0, _value1))
        }
        return _items
    }

    // -- name: DeletePilot :exec
    fun deletePilot(pilotId: Int): Unit {
        val _params = Params.create()
        _params.put("\$pilot_id", PrimitiveValue.newInt32(pilotId))
        if (transaction != null) {
            transaction.createQuery(
                "DELETE FROM pilots WHERE id = \$pilot_id;", _params).execute().join().getStatus().expectSuccess()
        } else {
            client!!.supplyResult { _session ->
                _session.createQuery(
                "DELETE FROM pilots WHERE id = \$pilot_id;", TxMode.SERIALIZABLE_RW, _params).execute()
            }.join().getStatus().expectSuccess()
        }
    }
}
