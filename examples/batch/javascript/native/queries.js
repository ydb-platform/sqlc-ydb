// Code generated by sqlc-ydb. DO NOT EDIT.
import { Int32, Json, JsonType, Primitive, TimestampType, Uint64, Utf8 } from "@ydbjs/value/primitive";
import { Optional } from "@ydbjs/value/optional";

export const GET_AUTHOR_SQL = `-- name: GetAuthor :one
DECLARE $author_id AS Uint64;
SELECT author_id, name, biography FROM authors
WHERE author_id = $author_id;`;
const _GET_AUTHOR_SQL_EXEC = `-- name: GetAuthor :one
\x20\x20\x20
SELECT author_id, name, biography FROM authors
WHERE author_id = $author_id;`;

export const DELETE_BOOK_EXEC_RESULT_SQL = `-- name: DeleteBookExecResult :exec
DECLARE $book_id AS Uint64;
DELETE FROM books
WHERE book_id = $book_id;`;
const _DELETE_BOOK_EXEC_RESULT_SQL_EXEC = `-- name: DeleteBookExecResult :exec
\x20\x20\x20
DELETE FROM books
WHERE book_id = $book_id;`;

export const DELETE_BOOK_SQL = `-- name: DeleteBook :exec
DECLARE $book_id AS Uint64;
DELETE FROM books
WHERE book_id = $book_id;`;
const _DELETE_BOOK_SQL_EXEC = `-- name: DeleteBook :exec
\x20\x20\x20
DELETE FROM books
WHERE book_id = $book_id;`;

export const DELETE_BOOK_NAMED_FUNC_SQL = `-- name: DeleteBookNamedFunc :exec
DECLARE $book_id AS Uint64;
DELETE FROM books
WHERE book_id = $book_id;`;
const _DELETE_BOOK_NAMED_FUNC_SQL_EXEC = `-- name: DeleteBookNamedFunc :exec
\x20\x20\x20
DELETE FROM books
WHERE book_id = $book_id;`;

export const DELETE_BOOK_NAMED_SIGN_SQL = `-- name: DeleteBookNamedSign :exec
DECLARE $book_id AS Uint64;
DELETE FROM books
WHERE book_id = $book_id;`;
const _DELETE_BOOK_NAMED_SIGN_SQL_EXEC = `-- name: DeleteBookNamedSign :exec
\x20\x20\x20
DELETE FROM books
WHERE book_id = $book_id;`;

export const BOOKS_BY_YEAR_SQL = `-- name: BooksByYear :many
DECLARE $year AS Int32;
SELECT book_id, author_id, isbn, book_type, title, year, available, tags
FROM books
WHERE year = $year;`;
const _BOOKS_BY_YEAR_SQL_EXEC = `-- name: BooksByYear :many
\x20\x20\x20
SELECT book_id, author_id, isbn, book_type, title, year, available, tags
FROM books
WHERE year = $year;`;

export const CREATE_AUTHOR_SQL = `-- name: CreateAuthor :one
DECLARE $author_id AS Uint64;
DECLARE $name AS Utf8;
DECLARE $biography AS Optional<Json>;
INSERT INTO authors (author_id, name, biography)
VALUES ($author_id, $name, $biography)
RETURNING author_id, name, biography;`;
const _CREATE_AUTHOR_SQL_EXEC = `-- name: CreateAuthor :one
\x20\x20\x20
\x20\x20\x20
\x20\x20\x20
INSERT INTO authors (author_id, name, biography)
VALUES ($author_id, $name, $biography)
RETURNING author_id, name, biography;`;

export const CREATE_BOOK_SQL = `-- name: CreateBook :one
DECLARE $book_id AS Uint64;
DECLARE $author_id AS Uint64;
DECLARE $isbn AS Utf8;
DECLARE $book_type AS Utf8;
DECLARE $title AS Utf8;
DECLARE $year AS Int32;
DECLARE $available AS Timestamp;
DECLARE $tags AS Json;
INSERT INTO books (book_id, author_id, isbn, book_type, title, year, available, tags)
VALUES ($book_id, $author_id, $isbn, $book_type, $title, $year, $available, $tags)
RETURNING book_id, author_id, isbn, book_type, title, year, available, tags;`;
const _CREATE_BOOK_SQL_EXEC = `-- name: CreateBook :one
\x20\x20\x20
\x20\x20\x20
\x20\x20\x20
\x20\x20\x20
\x20\x20\x20
\x20\x20\x20
\x20\x20\x20
\x20\x20\x20
INSERT INTO books (book_id, author_id, isbn, book_type, title, year, available, tags)
VALUES ($book_id, $author_id, $isbn, $book_type, $title, $year, $available, $tags)
RETURNING book_id, author_id, isbn, book_type, title, year, available, tags;`;

export const UPDATE_BOOK_SQL = `-- name: UpdateBook :exec
DECLARE $title AS Utf8;
DECLARE $tags AS Json;
DECLARE $book_id AS Uint64;
UPDATE books
SET title = $title, tags = $tags
WHERE book_id = $book_id;`;
const _UPDATE_BOOK_SQL_EXEC = `-- name: UpdateBook :exec
\x20\x20\x20
\x20\x20\x20
\x20\x20\x20
UPDATE books
SET title = $title, tags = $tags
WHERE book_id = $book_id;`;

export const GET_BIOGRAPHY_SQL = `-- name: GetBiography :one
DECLARE $author_id AS Uint64;
SELECT biography FROM authors
WHERE author_id = $author_id;`;
const _GET_BIOGRAPHY_SQL_EXEC = `-- name: GetBiography :one
\x20\x20\x20
SELECT biography FROM authors
WHERE author_id = $author_id;`;

function _int32(value, name) { if (!Number.isInteger(value) || value < -2147483648 || value > 2147483647) throw new RangeError(`${name} is outside YQL Int32 range`); return value; }
function _uint64(value, name) { if (typeof value !== "bigint" || value < 0n || value > 18446744073709551615n) throw new RangeError(`${name} is outside YQL Uint64 range`); return value; }
function _string(value, name) { if (typeof value !== "string") throw new TypeError(`${name} must be a string`); return value; }
function _timestamp(value, name) { if (typeof value !== "bigint" || value < 0n || value > 4291747199999999n) throw new RangeError(`${name} is outside YQL Timestamp microsecond range`); return value; }
function _json(value, name) { if (typeof value !== "string") throw new TypeError(`${name} must be JSON text`); try { JSON.parse(value); } catch (error) { throw new TypeError(`${name} must contain valid JSON`, { cause: error }); } return value; }
function _optional(value, name, itemType, makeValue) { if (value === undefined) throw new TypeError(`${name} must not be undefined; use null for an empty Optional`); return new Optional(value === null ? null : makeValue(value), itemType); }

function _rawValue(value, name, expectedCase) { if (value === null || typeof value !== "object" || value.value?.case !== expectedCase) throw new TypeError(`${name} has an unexpected YDB value shape`); return value.value.value; }
function _rawOptional(value, name, read) { if (value === null || typeof value !== "object") throw new TypeError(`${name} has an unexpected YDB Optional shape`); return value.value?.case === "nullFlagValue" ? null : read(value); }

function decodeGetAuthorRow(row) {
  if (row === null || typeof row !== "object" || Array.isArray(row)) throw new TypeError("GetAuthor: expected an object row");
  if (!Object.hasOwn(row, "author_id")) throw new TypeError("GetAuthor: result row is missing column author_id");
  if (!Object.hasOwn(row, "name")) throw new TypeError("GetAuthor: result row is missing column name");
  if (!Object.hasOwn(row, "biography")) throw new TypeError("GetAuthor: result row is missing column biography");
  return {
    authorId: _rawValue(row["author_id"], "GetAuthor.author_id", "uint64Value"),
    name: _rawValue(row["name"], "GetAuthor.name", "textValue"),
    biography: _rawOptional(row["biography"], "GetAuthor.biography", (value) => _rawValue(value, "GetAuthor.biography", "textValue")),
  };
}

function decodeBooksByYearRow(row) {
  if (row === null || typeof row !== "object" || Array.isArray(row)) throw new TypeError("BooksByYear: expected an object row");
  if (!Object.hasOwn(row, "book_id")) throw new TypeError("BooksByYear: result row is missing column book_id");
  if (!Object.hasOwn(row, "author_id")) throw new TypeError("BooksByYear: result row is missing column author_id");
  if (!Object.hasOwn(row, "isbn")) throw new TypeError("BooksByYear: result row is missing column isbn");
  if (!Object.hasOwn(row, "book_type")) throw new TypeError("BooksByYear: result row is missing column book_type");
  if (!Object.hasOwn(row, "title")) throw new TypeError("BooksByYear: result row is missing column title");
  if (!Object.hasOwn(row, "year")) throw new TypeError("BooksByYear: result row is missing column year");
  if (!Object.hasOwn(row, "available")) throw new TypeError("BooksByYear: result row is missing column available");
  if (!Object.hasOwn(row, "tags")) throw new TypeError("BooksByYear: result row is missing column tags");
  return {
    bookId: _rawValue(row["book_id"], "BooksByYear.book_id", "uint64Value"),
    authorId: _rawValue(row["author_id"], "BooksByYear.author_id", "uint64Value"),
    isbn: _rawValue(row["isbn"], "BooksByYear.isbn", "textValue"),
    bookType: _rawValue(row["book_type"], "BooksByYear.book_type", "textValue"),
    title: _rawValue(row["title"], "BooksByYear.title", "textValue"),
    year: _rawValue(row["year"], "BooksByYear.year", "int32Value"),
    available: _rawValue(row["available"], "BooksByYear.available", "uint64Value"),
    tags: _rawValue(row["tags"], "BooksByYear.tags", "textValue"),
  };
}

function decodeCreateAuthorRow(row) {
  if (row === null || typeof row !== "object" || Array.isArray(row)) throw new TypeError("CreateAuthor: expected an object row");
  if (!Object.hasOwn(row, "author_id")) throw new TypeError("CreateAuthor: result row is missing column author_id");
  if (!Object.hasOwn(row, "name")) throw new TypeError("CreateAuthor: result row is missing column name");
  if (!Object.hasOwn(row, "biography")) throw new TypeError("CreateAuthor: result row is missing column biography");
  return {
    authorId: _rawValue(row["author_id"], "CreateAuthor.author_id", "uint64Value"),
    name: _rawValue(row["name"], "CreateAuthor.name", "textValue"),
    biography: _rawOptional(row["biography"], "CreateAuthor.biography", (value) => _rawValue(value, "CreateAuthor.biography", "textValue")),
  };
}

function decodeCreateBookRow(row) {
  if (row === null || typeof row !== "object" || Array.isArray(row)) throw new TypeError("CreateBook: expected an object row");
  if (!Object.hasOwn(row, "book_id")) throw new TypeError("CreateBook: result row is missing column book_id");
  if (!Object.hasOwn(row, "author_id")) throw new TypeError("CreateBook: result row is missing column author_id");
  if (!Object.hasOwn(row, "isbn")) throw new TypeError("CreateBook: result row is missing column isbn");
  if (!Object.hasOwn(row, "book_type")) throw new TypeError("CreateBook: result row is missing column book_type");
  if (!Object.hasOwn(row, "title")) throw new TypeError("CreateBook: result row is missing column title");
  if (!Object.hasOwn(row, "year")) throw new TypeError("CreateBook: result row is missing column year");
  if (!Object.hasOwn(row, "available")) throw new TypeError("CreateBook: result row is missing column available");
  if (!Object.hasOwn(row, "tags")) throw new TypeError("CreateBook: result row is missing column tags");
  return {
    bookId: _rawValue(row["book_id"], "CreateBook.book_id", "uint64Value"),
    authorId: _rawValue(row["author_id"], "CreateBook.author_id", "uint64Value"),
    isbn: _rawValue(row["isbn"], "CreateBook.isbn", "textValue"),
    bookType: _rawValue(row["book_type"], "CreateBook.book_type", "textValue"),
    title: _rawValue(row["title"], "CreateBook.title", "textValue"),
    year: _rawValue(row["year"], "CreateBook.year", "int32Value"),
    available: _rawValue(row["available"], "CreateBook.available", "uint64Value"),
    tags: _rawValue(row["tags"], "CreateBook.tags", "textValue"),
  };
}

function decodeGetBiographyRow(row) {
  if (row === null || typeof row !== "object" || Array.isArray(row)) throw new TypeError("GetBiography: expected an object row");
  if (!Object.hasOwn(row, "biography")) throw new TypeError("GetBiography: result row is missing column biography");
  return {
    biography: _rawOptional(row["biography"], "GetBiography.biography", (value) => _rawValue(value, "GetBiography.biography", "textValue")),
  };
}

export class Queries {
  #client;

  /** @param {import("@ydbjs/query").QueryClient} client */
  constructor(client) {
    if (typeof client !== "function") throw new TypeError("Queries requires a YDB QueryClient");
    this.#client = client;
  }

  async getAuthor(authorId) {
    const pending = this.#client(_GET_AUTHOR_SQL_EXEC)
      .parameter("author_id", new Uint64(_uint64(authorId, "author_id")))
    ;
    const resultSets = await pending.raw();
    if (!Array.isArray(resultSets) || !Array.isArray(resultSets[0])) throw new TypeError("GetAuthor: expected the first YDB result set to be an array");
    const rows = resultSets[0];
    return rows.length === 0 ? null : decodeGetAuthorRow(rows[0]);
  }

  async deleteBookExecResult(bookId) {
    const pending = this.#client(_DELETE_BOOK_EXEC_RESULT_SQL_EXEC)
      .parameter("book_id", new Uint64(_uint64(bookId, "book_id")))
    ;
    const resultSets = await pending;
    return undefined;
  }

  async deleteBook(bookId) {
    const pending = this.#client(_DELETE_BOOK_SQL_EXEC)
      .parameter("book_id", new Uint64(_uint64(bookId, "book_id")))
    ;
    const resultSets = await pending;
    return undefined;
  }

  async deleteBookNamedFunc(bookId) {
    const pending = this.#client(_DELETE_BOOK_NAMED_FUNC_SQL_EXEC)
      .parameter("book_id", new Uint64(_uint64(bookId, "book_id")))
    ;
    const resultSets = await pending;
    return undefined;
  }

  async deleteBookNamedSign(bookId) {
    const pending = this.#client(_DELETE_BOOK_NAMED_SIGN_SQL_EXEC)
      .parameter("book_id", new Uint64(_uint64(bookId, "book_id")))
    ;
    const resultSets = await pending;
    return undefined;
  }

  async booksByYear(year) {
    const pending = this.#client(_BOOKS_BY_YEAR_SQL_EXEC)
      .parameter("year", new Int32(_int32(year, "year")))
    ;
    const resultSets = await pending.raw();
    if (!Array.isArray(resultSets) || !Array.isArray(resultSets[0])) throw new TypeError("BooksByYear: expected the first YDB result set to be an array");
    const rows = resultSets[0];
    return rows.map(decodeBooksByYearRow);
  }

  async createAuthor(args) {
    const pending = this.#client(_CREATE_AUTHOR_SQL_EXEC)
      .parameter("author_id", new Uint64(_uint64(args.authorId, "author_id")))
      .parameter("name", new Utf8(_string(args.name, "name")))
      .parameter("biography", _optional(args.biography, "biography", new JsonType(), (item) => new Json(_json(item, "biography"))))
    ;
    const resultSets = await pending.raw();
    if (!Array.isArray(resultSets) || !Array.isArray(resultSets[0])) throw new TypeError("CreateAuthor: expected the first YDB result set to be an array");
    const rows = resultSets[0];
    return rows.length === 0 ? null : decodeCreateAuthorRow(rows[0]);
  }

  async createBook(args) {
    const pending = this.#client(_CREATE_BOOK_SQL_EXEC)
      .parameter("book_id", new Uint64(_uint64(args.bookId, "book_id")))
      .parameter("author_id", new Uint64(_uint64(args.authorId, "author_id")))
      .parameter("isbn", new Utf8(_string(args.isbn, "isbn")))
      .parameter("book_type", new Utf8(_string(args.bookType, "book_type")))
      .parameter("title", new Utf8(_string(args.title, "title")))
      .parameter("year", new Int32(_int32(args.year, "year")))
      .parameter("available", new Primitive({ value: { case: "uint64Value", value: _timestamp(args.available, "available") } }, new TimestampType()))
      .parameter("tags", new Json(_json(args.tags, "tags")))
    ;
    const resultSets = await pending.raw();
    if (!Array.isArray(resultSets) || !Array.isArray(resultSets[0])) throw new TypeError("CreateBook: expected the first YDB result set to be an array");
    const rows = resultSets[0];
    return rows.length === 0 ? null : decodeCreateBookRow(rows[0]);
  }

  async updateBook(args) {
    const pending = this.#client(_UPDATE_BOOK_SQL_EXEC)
      .parameter("title", new Utf8(_string(args.title, "title")))
      .parameter("tags", new Json(_json(args.tags, "tags")))
      .parameter("book_id", new Uint64(_uint64(args.bookId, "book_id")))
    ;
    const resultSets = await pending;
    return undefined;
  }

  async getBiography(authorId) {
    const pending = this.#client(_GET_BIOGRAPHY_SQL_EXEC)
      .parameter("author_id", new Uint64(_uint64(authorId, "author_id")))
    ;
    const resultSets = await pending.raw();
    if (!Array.isArray(resultSets) || !Array.isArray(resultSets[0])) throw new TypeError("GetBiography: expected the first YDB result set to be an array");
    const rows = resultSets[0];
    return rows.length === 0 ? null : decodeGetBiographyRow(rows[0]);
  }
}
