// Code generated by sqlc-ydb. DO NOT EDIT.
package batch.nativeapi

import tech.ydb.query.QueryTransaction
import tech.ydb.query.tools.SessionRetryContext
import tech.ydb.query.tools.QueryReader
import tech.ydb.common.transaction.TxMode
import tech.ydb.table.query.Params
import tech.ydb.table.values.PrimitiveValue
import tech.ydb.table.values.PrimitiveType
import tech.ydb.table.values.OptionalType

// The caller owns the injected executor and its lifecycle.
class Queries {
    private val client: SessionRetryContext?
    private val transaction: QueryTransaction?

    constructor(client: SessionRetryContext) {
        this.client = client
        this.transaction = null
    }

    constructor(transaction: QueryTransaction) {
        this.client = null
        this.transaction = transaction
    }

    // -- name: GetAuthor :one
    fun getAuthor(authorId: Long): GetAuthorRow? {
        val _params = Params.create()
        _params.put("\$author_id", PrimitiveValue.newUint64(authorId))
        val _query = if (transaction != null) {
            QueryReader.readFrom(transaction.createQuery(
                "SELECT author_id, name, biography FROM authors\n" +
                "WHERE author_id = \$author_id;", _params)).join().getValue()
        } else {
            client!!.supplyResult { _session ->
                QueryReader.readFrom(_session.createQuery(
                "SELECT author_id, name, biography FROM authors\n" +
                "WHERE author_id = \$author_id;", TxMode.SERIALIZABLE_RW, _params))
            }.join().getValue()
        }
        kotlin.check(_query.getResultSetCount() == 1) { "Expected one result set" }
        val _rows = _query.getResultSet(0)
        if (!_rows.next()) return null
        val _value0: Long = _rows.getColumn(0).getUint64()
        val _value1: String = _rows.getColumn(1).getText()
        val _value2: String? = if (_rows.getColumn(2).isOptionalItemPresent()) _rows.getColumn(2).getOptionalItem().getJson() else null
        return GetAuthorRow(_value0, _value1, _value2)
    }

    // -- name: DeleteBookExecResult :exec
    fun deleteBookExecResult(bookId: Long): Unit {
        val _params = Params.create()
        _params.put("\$book_id", PrimitiveValue.newUint64(bookId))
        if (transaction != null) {
            transaction.createQuery(
                "DELETE FROM books\n" +
                "WHERE book_id = \$book_id;", _params).execute().join().getStatus().expectSuccess()
        } else {
            client!!.supplyResult { _session ->
                _session.createQuery(
                "DELETE FROM books\n" +
                "WHERE book_id = \$book_id;", TxMode.SERIALIZABLE_RW, _params).execute()
            }.join().getStatus().expectSuccess()
        }
    }

    // -- name: DeleteBook :exec
    fun deleteBook(bookId: Long): Unit {
        val _params = Params.create()
        _params.put("\$book_id", PrimitiveValue.newUint64(bookId))
        if (transaction != null) {
            transaction.createQuery(
                "DELETE FROM books\n" +
                "WHERE book_id = \$book_id;", _params).execute().join().getStatus().expectSuccess()
        } else {
            client!!.supplyResult { _session ->
                _session.createQuery(
                "DELETE FROM books\n" +
                "WHERE book_id = \$book_id;", TxMode.SERIALIZABLE_RW, _params).execute()
            }.join().getStatus().expectSuccess()
        }
    }

    // -- name: DeleteBookNamedFunc :exec
    fun deleteBookNamedFunc(bookId: Long): Unit {
        val _params = Params.create()
        _params.put("\$book_id", PrimitiveValue.newUint64(bookId))
        if (transaction != null) {
            transaction.createQuery(
                "DELETE FROM books\n" +
                "WHERE book_id = \$book_id;", _params).execute().join().getStatus().expectSuccess()
        } else {
            client!!.supplyResult { _session ->
                _session.createQuery(
                "DELETE FROM books\n" +
                "WHERE book_id = \$book_id;", TxMode.SERIALIZABLE_RW, _params).execute()
            }.join().getStatus().expectSuccess()
        }
    }

    // -- name: DeleteBookNamedSign :exec
    fun deleteBookNamedSign(bookId: Long): Unit {
        val _params = Params.create()
        _params.put("\$book_id", PrimitiveValue.newUint64(bookId))
        if (transaction != null) {
            transaction.createQuery(
                "DELETE FROM books\n" +
                "WHERE book_id = \$book_id;", _params).execute().join().getStatus().expectSuccess()
        } else {
            client!!.supplyResult { _session ->
                _session.createQuery(
                "DELETE FROM books\n" +
                "WHERE book_id = \$book_id;", TxMode.SERIALIZABLE_RW, _params).execute()
            }.join().getStatus().expectSuccess()
        }
    }

    // -- name: BooksByYear :many
    fun booksByYear(year: Int): List<BooksByYearRow> {
        val _params = Params.create()
        _params.put("\$year", PrimitiveValue.newInt32(year))
        val _query = if (transaction != null) {
            QueryReader.readFrom(transaction.createQuery(
                "SELECT book_id, author_id, isbn, book_type, title, year, available, tags\n" +
                "FROM books\n" +
                "WHERE year = \$year;", _params)).join().getValue()
        } else {
            client!!.supplyResult { _session ->
                QueryReader.readFrom(_session.createQuery(
                "SELECT book_id, author_id, isbn, book_type, title, year, available, tags\n" +
                "FROM books\n" +
                "WHERE year = \$year;", TxMode.SERIALIZABLE_RW, _params))
            }.join().getValue()
        }
        kotlin.check(_query.getResultSetCount() == 1) { "Expected one result set" }
        val _rows = _query.getResultSet(0)
        val _items = ArrayList<BooksByYearRow>()
        while (_rows.next()) {
            val _value0: Long = _rows.getColumn(0).getUint64()
            val _value1: Long = _rows.getColumn(1).getUint64()
            val _value2: String = _rows.getColumn(2).getText()
            val _value3: String = _rows.getColumn(3).getText()
            val _value4: String = _rows.getColumn(4).getText()
            val _value5: Int = _rows.getColumn(5).getInt32()
            val _value6: java.time.Instant = _rows.getColumn(6).getTimestamp()
            val _value7: String = _rows.getColumn(7).getJson()
            _items.add(BooksByYearRow(_value0, _value1, _value2, _value3, _value4, _value5, _value6, _value7))
        }
        return _items
    }

    // -- name: CreateAuthor :one
    fun createAuthor(authorId: Long, name: String, biography: String?): CreateAuthorRow? {
        val _params = Params.create()
        _params.put("\$author_id", PrimitiveValue.newUint64(authorId))
        _params.put("\$name", PrimitiveValue.newText(name))
        _params.put("\$biography", if (biography == null) OptionalType.of(PrimitiveType.Json).emptyValue() else OptionalType.of(PrimitiveType.Json).newValue(PrimitiveValue.newJson(biography)))
        val _query = if (transaction != null) {
            QueryReader.readFrom(transaction.createQuery(
                "INSERT INTO authors (author_id, name, biography)\n" +
                "VALUES (\$author_id, \$name, \$biography)\n" +
                "RETURNING author_id, name, biography;", _params)).join().getValue()
        } else {
            client!!.supplyResult { _session ->
                QueryReader.readFrom(_session.createQuery(
                "INSERT INTO authors (author_id, name, biography)\n" +
                "VALUES (\$author_id, \$name, \$biography)\n" +
                "RETURNING author_id, name, biography;", TxMode.SERIALIZABLE_RW, _params))
            }.join().getValue()
        }
        kotlin.check(_query.getResultSetCount() == 1) { "Expected one result set" }
        val _rows = _query.getResultSet(0)
        if (!_rows.next()) return null
        val _value0: Long = _rows.getColumn(0).getUint64()
        val _value1: String = _rows.getColumn(1).getText()
        val _value2: String? = if (_rows.getColumn(2).isOptionalItemPresent()) _rows.getColumn(2).getOptionalItem().getJson() else null
        return CreateAuthorRow(_value0, _value1, _value2)
    }

    // -- name: CreateBook :one
    fun createBook(bookId: Long, authorId: Long, isbn: String, bookType: String, title: String, year: Int, available: java.time.Instant, tags: String): CreateBookRow? {
        val _params = Params.create()
        _params.put("\$book_id", PrimitiveValue.newUint64(bookId))
        _params.put("\$author_id", PrimitiveValue.newUint64(authorId))
        _params.put("\$isbn", PrimitiveValue.newText(isbn))
        _params.put("\$book_type", PrimitiveValue.newText(bookType))
        _params.put("\$title", PrimitiveValue.newText(title))
        _params.put("\$year", PrimitiveValue.newInt32(year))
        _params.put("\$available", PrimitiveValue.newTimestamp(available))
        _params.put("\$tags", PrimitiveValue.newJson(tags))
        val _query = if (transaction != null) {
            QueryReader.readFrom(transaction.createQuery(
                "INSERT INTO books (book_id, author_id, isbn, book_type, title, year, available, tags)\n" +
                "VALUES (\$book_id, \$author_id, \$isbn, \$book_type, \$title, \$year, \$available, \$tags)\n" +
                "RETURNING book_id, author_id, isbn, book_type, title, year, available, tags;", _params)).join().getValue()
        } else {
            client!!.supplyResult { _session ->
                QueryReader.readFrom(_session.createQuery(
                "INSERT INTO books (book_id, author_id, isbn, book_type, title, year, available, tags)\n" +
                "VALUES (\$book_id, \$author_id, \$isbn, \$book_type, \$title, \$year, \$available, \$tags)\n" +
                "RETURNING book_id, author_id, isbn, book_type, title, year, available, tags;", TxMode.SERIALIZABLE_RW, _params))
            }.join().getValue()
        }
        kotlin.check(_query.getResultSetCount() == 1) { "Expected one result set" }
        val _rows = _query.getResultSet(0)
        if (!_rows.next()) return null
        val _value0: Long = _rows.getColumn(0).getUint64()
        val _value1: Long = _rows.getColumn(1).getUint64()
        val _value2: String = _rows.getColumn(2).getText()
        val _value3: String = _rows.getColumn(3).getText()
        val _value4: String = _rows.getColumn(4).getText()
        val _value5: Int = _rows.getColumn(5).getInt32()
        val _value6: java.time.Instant = _rows.getColumn(6).getTimestamp()
        val _value7: String = _rows.getColumn(7).getJson()
        return CreateBookRow(_value0, _value1, _value2, _value3, _value4, _value5, _value6, _value7)
    }

    // -- name: UpdateBook :exec
    fun updateBook(title: String, tags: String, bookId: Long): Unit {
        val _params = Params.create()
        _params.put("\$title", PrimitiveValue.newText(title))
        _params.put("\$tags", PrimitiveValue.newJson(tags))
        _params.put("\$book_id", PrimitiveValue.newUint64(bookId))
        if (transaction != null) {
            transaction.createQuery(
                "UPDATE books\n" +
                "SET title = \$title, tags = \$tags\n" +
                "WHERE book_id = \$book_id;", _params).execute().join().getStatus().expectSuccess()
        } else {
            client!!.supplyResult { _session ->
                _session.createQuery(
                "UPDATE books\n" +
                "SET title = \$title, tags = \$tags\n" +
                "WHERE book_id = \$book_id;", TxMode.SERIALIZABLE_RW, _params).execute()
            }.join().getStatus().expectSuccess()
        }
    }

    // -- name: GetBiography :one
    fun getBiography(authorId: Long): GetBiographyRow? {
        val _params = Params.create()
        _params.put("\$author_id", PrimitiveValue.newUint64(authorId))
        val _query = if (transaction != null) {
            QueryReader.readFrom(transaction.createQuery(
                "SELECT biography FROM authors\n" +
                "WHERE author_id = \$author_id;", _params)).join().getValue()
        } else {
            client!!.supplyResult { _session ->
                QueryReader.readFrom(_session.createQuery(
                "SELECT biography FROM authors\n" +
                "WHERE author_id = \$author_id;", TxMode.SERIALIZABLE_RW, _params))
            }.join().getValue()
        }
        kotlin.check(_query.getResultSetCount() == 1) { "Expected one result set" }
        val _rows = _query.getResultSet(0)
        if (!_rows.next()) return null
        val _value0: String? = if (_rows.getColumn(0).isOptionalItemPresent()) _rows.getColumn(0).getOptionalItem().getJson() else null
        return GetBiographyRow(_value0)
    }
}
