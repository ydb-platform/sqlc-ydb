# Code generated by sqlc-ydb. DO NOT EDIT.
from __future__ import annotations
from typing import Optional, Union
from datetime import datetime
from . import models as _models
import ydb as _ydb


class Querier:
    def __init__(self, executor: Union[_ydb.QuerySessionPool, _ydb.QueryTxContext], *, retry_settings: Optional[_ydb.RetrySettings] = None):
        if retry_settings is not None and not isinstance(executor, _ydb.QuerySessionPool):
            raise ValueError("retry_settings belongs to the pool; configure retries around the whole transaction")
        self._executor = executor
        self._retry_settings = retry_settings
        if isinstance(executor, _ydb.QuerySessionPool) and retry_settings is None:
            self._retry_settings = _ydb.RetrySettings(max_retries=0)

    def _execute(self, query: str, parameters: dict):
        if isinstance(self._executor, _ydb.QuerySessionPool):
            return self._executor.execute_with_retries(query, parameters, retry_settings=self._retry_settings)
        return list(self._executor.execute(query, parameters))

    # -- name: GetAuthor :one
    def get_author(self, author_id: int) -> Optional[_models.Authors]:
        parameters = {
            "$author_id": _ydb.TypedValue(author_id, _ydb.PrimitiveType.Uint64),
        }
        result_sets = self._execute(
            ("SELECT author_id, name, biography FROM authors\n"
             "WHERE author_id = $author_id;"), parameters)
        if len(result_sets) != 1:
            raise ValueError("expected exactly one YDB result set")
        rows = result_sets[0].rows
        row = rows[0] if rows else None
        if row is None:
            return None
        return _models.Authors(
            author_id=row["author_id"],
            name=row["name"],
            biography=row["biography"],
        )

    # -- name: DeleteBookExecResult :exec
    def delete_book_exec_result(self, book_id: int) -> None:
        parameters = {
            "$book_id": _ydb.TypedValue(book_id, _ydb.PrimitiveType.Uint64),
        }
        result_sets = self._execute(
            ("DELETE FROM books\n"
             "WHERE book_id = $book_id;"), parameters)
        return None

    # -- name: DeleteBook :exec
    def delete_book(self, book_id: int) -> None:
        parameters = {
            "$book_id": _ydb.TypedValue(book_id, _ydb.PrimitiveType.Uint64),
        }
        result_sets = self._execute(
            ("DELETE FROM books\n"
             "WHERE book_id = $book_id;"), parameters)
        return None

    # -- name: DeleteBookNamedFunc :exec
    def delete_book_named_func(self, book_id: int) -> None:
        parameters = {
            "$book_id": _ydb.TypedValue(book_id, _ydb.PrimitiveType.Uint64),
        }
        result_sets = self._execute(
            ("DELETE FROM books\n"
             "WHERE book_id = $book_id;"), parameters)
        return None

    # -- name: DeleteBookNamedSign :exec
    def delete_book_named_sign(self, book_id: int) -> None:
        parameters = {
            "$book_id": _ydb.TypedValue(book_id, _ydb.PrimitiveType.Uint64),
        }
        result_sets = self._execute(
            ("DELETE FROM books\n"
             "WHERE book_id = $book_id;"), parameters)
        return None

    # -- name: BooksByYear :many
    def books_by_year(self, year: int) -> list[_models.Books]:
        parameters = {
            "$year": _ydb.TypedValue(year, _ydb.PrimitiveType.Int32),
        }
        result_sets = self._execute(
            ("SELECT book_id, author_id, isbn, book_type, title, year, available, tags\n"
             "FROM books\n"
             "WHERE year = $year;"), parameters)
        if len(result_sets) != 1:
            raise ValueError("expected exactly one YDB result set")
        rows = result_sets[0].rows
        return [_models.Books(
            book_id=row["book_id"],
            author_id=row["author_id"],
            isbn=row["isbn"],
            book_type=row["book_type"],
            title=row["title"],
            year=row["year"],
            available=row["available"],
            tags=row["tags"],
        ) for row in rows]

    # -- name: CreateAuthor :one
    def create_author(self, author_id: int, name: str, biography: Optional[str]) -> Optional[_models.Authors]:
        parameters = {
            "$author_id": _ydb.TypedValue(author_id, _ydb.PrimitiveType.Uint64),
            "$name": _ydb.TypedValue(name, _ydb.PrimitiveType.Utf8),
            "$biography": _ydb.TypedValue(biography, _ydb.OptionalType(_ydb.PrimitiveType.Json)),
        }
        result_sets = self._execute(
            ("INSERT INTO authors (author_id, name, biography)\n"
             "VALUES ($author_id, $name, $biography)\n"
             "RETURNING author_id, name, biography;"), parameters)
        if len(result_sets) != 1:
            raise ValueError("expected exactly one YDB result set")
        rows = result_sets[0].rows
        row = rows[0] if rows else None
        if row is None:
            return None
        return _models.Authors(
            author_id=row["author_id"],
            name=row["name"],
            biography=row["biography"],
        )

    # -- name: CreateBook :one
    def create_book(self, book_id: int, author_id: int, isbn: str, book_type: str, title: str, year: int, available: datetime, tags: str) -> Optional[_models.Books]:
        parameters = {
            "$book_id": _ydb.TypedValue(book_id, _ydb.PrimitiveType.Uint64),
            "$author_id": _ydb.TypedValue(author_id, _ydb.PrimitiveType.Uint64),
            "$isbn": _ydb.TypedValue(isbn, _ydb.PrimitiveType.Utf8),
            "$book_type": _ydb.TypedValue(book_type, _ydb.PrimitiveType.Utf8),
            "$title": _ydb.TypedValue(title, _ydb.PrimitiveType.Utf8),
            "$year": _ydb.TypedValue(year, _ydb.PrimitiveType.Int32),
            "$available": _ydb.TypedValue(available, _ydb.PrimitiveType.Timestamp),
            "$tags": _ydb.TypedValue(tags, _ydb.PrimitiveType.Json),
        }
        result_sets = self._execute(
            ("INSERT INTO books (book_id, author_id, isbn, book_type, title, year, available, tags)\n"
             "VALUES ($book_id, $author_id, $isbn, $book_type, $title, $year, $available, $tags)\n"
             "RETURNING book_id, author_id, isbn, book_type, title, year, available, tags;"), parameters)
        if len(result_sets) != 1:
            raise ValueError("expected exactly one YDB result set")
        rows = result_sets[0].rows
        row = rows[0] if rows else None
        if row is None:
            return None
        return _models.Books(
            book_id=row["book_id"],
            author_id=row["author_id"],
            isbn=row["isbn"],
            book_type=row["book_type"],
            title=row["title"],
            year=row["year"],
            available=row["available"],
            tags=row["tags"],
        )

    # -- name: UpdateBook :exec
    def update_book(self, title: str, tags: str, book_id: int) -> None:
        parameters = {
            "$title": _ydb.TypedValue(title, _ydb.PrimitiveType.Utf8),
            "$tags": _ydb.TypedValue(tags, _ydb.PrimitiveType.Json),
            "$book_id": _ydb.TypedValue(book_id, _ydb.PrimitiveType.Uint64),
        }
        result_sets = self._execute(
            ("UPDATE books\n"
             "SET title = $title, tags = $tags\n"
             "WHERE book_id = $book_id;"), parameters)
        return None

    # -- name: GetBiography :one
    def get_biography(self, author_id: int) -> Optional[_models.GetBiographyRow]:
        parameters = {
            "$author_id": _ydb.TypedValue(author_id, _ydb.PrimitiveType.Uint64),
        }
        result_sets = self._execute(
            ("SELECT biography FROM authors\n"
             "WHERE author_id = $author_id;"), parameters)
        if len(result_sets) != 1:
            raise ValueError("expected exactly one YDB result set")
        rows = result_sets[0].rows
        row = rows[0] if rows else None
        if row is None:
            return None
        return _models.GetBiographyRow(
            biography=row["biography"],
        )

    # -- name: CreateBooks :exec
    def create_books(self, books: list[_models.CreateBooksBooksItem]) -> None:
        parameters = {
            "$books": _ydb.TypedValue([{"book_id": item.book_id, "author_id": item.author_id, "isbn": item.isbn, "book_type": item.book_type, "title": item.title, "year": item.year, "available": item.available, "tags": item.tags} for item in books], _ydb.ListType(_ydb.StructType().add_member("book_id", _ydb.PrimitiveType.Uint64).add_member("author_id", _ydb.PrimitiveType.Uint64).add_member("isbn", _ydb.PrimitiveType.Utf8).add_member("book_type", _ydb.PrimitiveType.Utf8).add_member("title", _ydb.PrimitiveType.Utf8).add_member("year", _ydb.PrimitiveType.Int32).add_member("available", _ydb.PrimitiveType.Timestamp).add_member("tags", _ydb.PrimitiveType.Json))),
        }
        result_sets = self._execute(
            ("DECLARE $books AS List<Struct<\n"
             "    book_id: Uint64,\n"
             "    author_id: Uint64,\n"
             "    isbn: Utf8,\n"
             "    book_type: Utf8,\n"
             "    title: Utf8,\n"
             "    year: Int32,\n"
             "    available: Timestamp,\n"
             "    tags: Json\n"
             ">>;\n"
             "INSERT INTO books (\n"
             "    book_id, author_id, isbn, book_type, title, year, available, tags\n"
             ")\n"
             "SELECT\n"
             "    book_id, author_id, isbn, book_type, title, year, available, tags\n"
             "FROM AS_TABLE($books);"), parameters)
        return None
