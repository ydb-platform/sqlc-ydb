// Code generated by sqlc-ydb. DO NOT EDIT.
package batch.jdbc;

import tech.ydb.table.values.PrimitiveValue;
import tech.ydb.table.values.PrimitiveType;
import tech.ydb.table.values.OptionalType;

// The caller owns the injected client and its lifecycle.
public final class Queries {
    private final java.sql.Connection client;

    public Queries(java.sql.Connection client) {
        this.client = java.util.Objects.requireNonNull(client);
    }

    // -- name: GetAuthor :one
    public java.util.Optional<GetAuthorRow> getAuthor(long authorId) throws java.sql.SQLException {
        try (var _prepared = client.prepareStatement("""
            SELECT author_id, name, biography FROM authors
            WHERE author_id = ?;\
            """)) {
            _prepared.setObject(1, PrimitiveValue.newUint64(authorId));
            try (var _rows = _prepared.executeQuery()) {
                if (!_rows.next()) return java.util.Optional.empty();
                long _value0 = _rows.getLong(1);
                String _value1 = _rows.getString(2);
                String _value2 = _rows.getString(3);
                return java.util.Optional.of(new GetAuthorRow(_value0, _value1, _value2));
            }
        }
    }

    // -- name: DeleteBookExecResult :exec
    public void deleteBookExecResult(long bookId) throws java.sql.SQLException {
        try (var _prepared = client.prepareStatement("""
            DELETE FROM books
            WHERE book_id = ?;\
            """)) {
            _prepared.setObject(1, PrimitiveValue.newUint64(bookId));
            _prepared.execute();
        }
    }

    // -- name: DeleteBook :exec
    public void deleteBook(long bookId) throws java.sql.SQLException {
        try (var _prepared = client.prepareStatement("""
            DELETE FROM books
            WHERE book_id = ?;\
            """)) {
            _prepared.setObject(1, PrimitiveValue.newUint64(bookId));
            _prepared.execute();
        }
    }

    // -- name: DeleteBookNamedFunc :exec
    public void deleteBookNamedFunc(long bookId) throws java.sql.SQLException {
        try (var _prepared = client.prepareStatement("""
            DELETE FROM books
            WHERE book_id = ?;\
            """)) {
            _prepared.setObject(1, PrimitiveValue.newUint64(bookId));
            _prepared.execute();
        }
    }

    // -- name: DeleteBookNamedSign :exec
    public void deleteBookNamedSign(long bookId) throws java.sql.SQLException {
        try (var _prepared = client.prepareStatement("""
            DELETE FROM books
            WHERE book_id = ?;\
            """)) {
            _prepared.setObject(1, PrimitiveValue.newUint64(bookId));
            _prepared.execute();
        }
    }

    // -- name: BooksByYear :many
    public java.util.List<BooksByYearRow> booksByYear(int year) throws java.sql.SQLException {
        try (var _prepared = client.prepareStatement("""
            SELECT book_id, author_id, isbn, book_type, title, year, available, tags
            FROM books
            WHERE year = ?;\
            """)) {
            _prepared.setInt(1, year);
            try (var _rows = _prepared.executeQuery()) {
                var _items = new java.util.ArrayList<BooksByYearRow>();
                while (_rows.next()) {
                    long _value0 = _rows.getLong(1);
                    long _value1 = _rows.getLong(2);
                    String _value2 = _rows.getString(3);
                    String _value3 = _rows.getString(4);
                    String _value4 = _rows.getString(5);
                    int _value5 = _rows.getInt(6);
                    var _value6Raw = _rows.getTimestamp(7);
                    java.time.Instant _value6 = _value6Raw == null ? null : _value6Raw.toInstant();
                    String _value7 = _rows.getString(8);
                    _items.add(new BooksByYearRow(_value0, _value1, _value2, _value3, _value4, _value5, _value6, _value7));
                }
                return _items;
            }
        }
    }

    // -- name: CreateAuthor :one
    public java.util.Optional<CreateAuthorRow> createAuthor(long authorId, String name, String biography) throws java.sql.SQLException {
        try (var _prepared = client.prepareStatement("""
            INSERT INTO authors (author_id, name, biography)
            VALUES (?, ?, ?)
            RETURNING author_id, name, biography;\
            """)) {
            _prepared.setObject(1, PrimitiveValue.newUint64(authorId));
            _prepared.setString(2, name);
            _prepared.setObject(3, biography == null ? OptionalType.of(PrimitiveType.Json).emptyValue() : PrimitiveValue.newJson(biography).makeOptional());
            try (var _rows = _prepared.executeQuery()) {
                if (!_rows.next()) return java.util.Optional.empty();
                long _value0 = _rows.getLong(1);
                String _value1 = _rows.getString(2);
                String _value2 = _rows.getString(3);
                return java.util.Optional.of(new CreateAuthorRow(_value0, _value1, _value2));
            }
        }
    }

    // -- name: CreateBook :one
    public java.util.Optional<CreateBookRow> createBook(long bookId, long authorId, String isbn, String bookType, String title, int year, java.time.Instant available, String tags) throws java.sql.SQLException {
        try (var _prepared = client.prepareStatement("""
            INSERT INTO books (book_id, author_id, isbn, book_type, title, year, available, tags)
            VALUES (?, ?, ?, ?, ?, ?, ?, ?)
            RETURNING book_id, author_id, isbn, book_type, title, year, available, tags;\
            """)) {
            _prepared.setObject(1, PrimitiveValue.newUint64(bookId));
            _prepared.setObject(2, PrimitiveValue.newUint64(authorId));
            _prepared.setString(3, isbn);
            _prepared.setString(4, bookType);
            _prepared.setString(5, title);
            _prepared.setInt(6, year);
            _prepared.setObject(7, PrimitiveValue.newTimestamp(available));
            _prepared.setObject(8, PrimitiveValue.newJson(tags));
            try (var _rows = _prepared.executeQuery()) {
                if (!_rows.next()) return java.util.Optional.empty();
                long _value0 = _rows.getLong(1);
                long _value1 = _rows.getLong(2);
                String _value2 = _rows.getString(3);
                String _value3 = _rows.getString(4);
                String _value4 = _rows.getString(5);
                int _value5 = _rows.getInt(6);
                var _value6Raw = _rows.getTimestamp(7);
                java.time.Instant _value6 = _value6Raw == null ? null : _value6Raw.toInstant();
                String _value7 = _rows.getString(8);
                return java.util.Optional.of(new CreateBookRow(_value0, _value1, _value2, _value3, _value4, _value5, _value6, _value7));
            }
        }
    }

    // -- name: UpdateBook :exec
    public void updateBook(String title, String tags, long bookId) throws java.sql.SQLException {
        try (var _prepared = client.prepareStatement("""
            UPDATE books
            SET title = ?, tags = ?
            WHERE book_id = ?;\
            """)) {
            _prepared.setString(1, title);
            _prepared.setObject(2, PrimitiveValue.newJson(tags));
            _prepared.setObject(3, PrimitiveValue.newUint64(bookId));
            _prepared.execute();
        }
    }

    // -- name: GetBiography :one
    public java.util.Optional<GetBiographyRow> getBiography(long authorId) throws java.sql.SQLException {
        try (var _prepared = client.prepareStatement("""
            SELECT biography FROM authors
            WHERE author_id = ?;\
            """)) {
            _prepared.setObject(1, PrimitiveValue.newUint64(authorId));
            try (var _rows = _prepared.executeQuery()) {
                if (!_rows.next()) return java.util.Optional.empty();
                String _value0 = _rows.getString(1);
                return java.util.Optional.of(new GetBiographyRow(_value0));
            }
        }
    }

    // -- name: CreateBooks :exec
    public void createBooks(java.util.List<CreateBooksBooksItem> books) throws java.sql.SQLException {
        try (var _prepared = client.prepareStatement("""
              \s
                \s
                \s
                \s
                \s
                \s
                \s
                \s
                \s

            INSERT INTO books (
                book_id, author_id, isbn, book_type, title, year, available, tags
            )
            SELECT
                book_id, author_id, isbn, book_type, title, year, available, tags
            FROM AS_TABLE(?);\
            """)) {
            _prepared.setObject(1, tech.ydb.table.values.ListType.of(tech.ydb.table.values.StructType.of(java.util.Map.ofEntries(java.util.Map.entry("book_id", tech.ydb.table.values.PrimitiveType.Uint64), java.util.Map.entry("author_id", tech.ydb.table.values.PrimitiveType.Uint64), java.util.Map.entry("isbn", tech.ydb.table.values.PrimitiveType.Text), java.util.Map.entry("book_type", tech.ydb.table.values.PrimitiveType.Text), java.util.Map.entry("title", tech.ydb.table.values.PrimitiveType.Text), java.util.Map.entry("year", tech.ydb.table.values.PrimitiveType.Int32), java.util.Map.entry("available", tech.ydb.table.values.PrimitiveType.Timestamp), java.util.Map.entry("tags", tech.ydb.table.values.PrimitiveType.Json)))).newValue(books.stream().map(_batchItem -> tech.ydb.table.values.StructValue.of(java.util.Map.ofEntries(java.util.Map.entry("book_id", PrimitiveValue.newUint64(_batchItem.bookId())), java.util.Map.entry("author_id", PrimitiveValue.newUint64(_batchItem.authorId())), java.util.Map.entry("isbn", PrimitiveValue.newText(_batchItem.isbn())), java.util.Map.entry("book_type", PrimitiveValue.newText(_batchItem.bookType())), java.util.Map.entry("title", PrimitiveValue.newText(_batchItem.title())), java.util.Map.entry("year", PrimitiveValue.newInt32(_batchItem.year())), java.util.Map.entry("available", PrimitiveValue.newTimestamp(_batchItem.available())), java.util.Map.entry("tags", PrimitiveValue.newJson(_batchItem.tags()))))).toList()));
            _prepared.execute();
        }
    }
}
