// Code generated by sqlc-ydb. DO NOT EDIT.
package namespaces.jdbc;

import tech.ydb.table.values.PrimitiveValue;

// The caller owns the injected client and its lifecycle.
public final class Queries {
    private final java.sql.Connection client;

    public Queries(java.sql.Connection client) {
        this.client = java.util.Objects.requireNonNull(client);
    }

    // -- name: UpsertPrimaryUser :exec
    public void upsertPrimaryUser(long id, String name) throws java.sql.SQLException {
        try (var _prepared = client.unwrap(tech.ydb.jdbc.YdbConnection.class).prepareStatement("""
            PRAGMA TablePathPrefix(\"/local/sqlc_namespaces/a\");
            DECLARE $id AS Uint64;
            DECLARE $name AS Utf8;
            UPSERT INTO users (id, name) VALUES ($id, $name);\
            """, tech.ydb.jdbc.YdbPrepareMode.DATA_QUERY)) {
            _prepared.setObject("id", PrimitiveValue.newUint64(id));
            _prepared.setString("name", name);
            _prepared.execute();
        }
    }

    // -- name: UpsertSecondaryUser :exec
    public void upsertSecondaryUser(long id, String name) throws java.sql.SQLException {
        try (var _prepared = client.unwrap(tech.ydb.jdbc.YdbConnection.class).prepareStatement("""
            PRAGMA TablePathPrefix(\"/local/sqlc_namespaces/b\");
            DECLARE $id AS Uint64;
            DECLARE $name AS Utf8;
            UPSERT INTO users (id, name) VALUES ($id, $name);\
            """, tech.ydb.jdbc.YdbPrepareMode.DATA_QUERY)) {
            _prepared.setObject("id", PrimitiveValue.newUint64(id));
            _prepared.setString("name", name);
            _prepared.execute();
        }
    }

    // -- name: GetPrimaryUser :one
    public java.util.Optional<GetPrimaryUserRow> getPrimaryUser(long id) throws java.sql.SQLException {
        try (var _prepared = client.prepareStatement("""
            PRAGMA TablePathPrefix(\"/local/sqlc_namespaces/a\");
            SELECT users.`id` AS `id`, `users`.`name` AS `name` FROM users WHERE id = ?;\
            """)) {
            _prepared.setObject(1, PrimitiveValue.newUint64(id));
            try (var _rows = _prepared.executeQuery()) {
                if (!_rows.next()) return java.util.Optional.empty();
                long _value0 = _rows.getLong(1);
                String _value1 = _rows.getString(2);
                return java.util.Optional.of(new GetPrimaryUserRow(_value0, _value1));
            }
        }
    }

    // -- name: GetSecondaryUser :one
    public java.util.Optional<GetSecondaryUserRow> getSecondaryUser(long id) throws java.sql.SQLException {
        try (var _prepared = client.unwrap(tech.ydb.jdbc.YdbConnection.class).prepareStatement("""
            PRAGMA TablePathPrefix(\"/local/sqlc_namespaces/b\");
            DECLARE $id AS Uint64;
            SELECT users.`id` AS `id`, `users`.`name` AS `name` FROM users WHERE id = $id;\
            """, tech.ydb.jdbc.YdbPrepareMode.DATA_QUERY)) {
            _prepared.setObject("id", PrimitiveValue.newUint64(id));
            try (var _rows = _prepared.executeQuery()) {
                if (!_rows.next()) return java.util.Optional.empty();
                long _value0 = _rows.getLong(1);
                String _value1 = _rows.getString(2);
                return java.util.Optional.of(new GetSecondaryUserRow(_value0, _value1));
            }
        }
    }

    // -- name: FindPrimaryUsersByName :many
    public java.util.List<FindPrimaryUsersByNameRow> findPrimaryUsersByName(String name) throws java.sql.SQLException {
        try (var _prepared = client.prepareStatement("""
            PRAGMA TablePathPrefix(\"/local/sqlc_namespaces/a\");
            SELECT u.`id` AS `id`, `u`.`name` AS `name` FROM users VIEW by_name AS u WHERE u.name = ? ORDER BY u.id;\
            """)) {
            _prepared.setString(1, name);
            try (var _rows = _prepared.executeQuery()) {
                var _items = new java.util.ArrayList<FindPrimaryUsersByNameRow>();
                while (_rows.next()) {
                    long _value0 = _rows.getLong(1);
                    String _value1 = _rows.getString(2);
                    _items.add(new FindPrimaryUsersByNameRow(_value0, _value1));
                }
                return _items;
            }
        }
    }

    // -- name: CompareUserNames :many
    public java.util.List<CompareUserNamesRow> compareUserNames() throws java.sql.SQLException {
        try (var _prepared = client.prepareStatement("""
            PRAGMA TablePathPrefix(\"/local/sqlc_namespaces/a\");
            SELECT a.id AS id, a.name AS primary_name, b.name AS secondary_name
            FROM users AS a
            LEFT JOIN `/local/sqlc_namespaces/b/users` AS b ON a.id = b.id
            ORDER BY a.id;\
            """)) {
            try (var _rows = _prepared.executeQuery()) {
                var _items = new java.util.ArrayList<CompareUserNamesRow>();
                while (_rows.next()) {
                    long _value0 = _rows.getLong(1);
                    String _value1 = _rows.getString(2);
                    String _value2 = _rows.getString(3);
                    _items.add(new CompareUserNamesRow(_value0, _value1, _value2));
                }
                return _items;
            }
        }
    }
}
