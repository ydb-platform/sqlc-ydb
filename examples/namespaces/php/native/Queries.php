<?php
// Code generated by sqlc-ydb. DO NOT EDIT.
declare(strict_types=1);

namespace Namespaces\Native;

require_once __DIR__ . '/YdbRuntime.php';

use Closure;
use UnexpectedValueException;
use Ydb\Table\ExecuteQueryResult;
use Ydb\Table\TransactionControl;
use Ydb\Type\PrimitiveTypeId;
use YdbPlatform\Ydb\Retry\RetryParams;
use YdbPlatform\Ydb\Session;
use YdbPlatform\Ydb\Table;

final class Queries
{
    private ?Session $session = null;
    private ?string $txId = null;

    public function __construct(
        private readonly Table $table,
        private readonly bool $idempotent = false,
        private readonly ?Closure $configure = null,
        private readonly ?RetryParams $retryParams = null,
    )
    {
        if (PHP_INT_SIZE !== 8) {
            throw new \LogicException('sqlc-ydb generated PHP code requires a 64-bit PHP runtime');
        }
    }

    /** Bind to a caller-owned transaction; this helper never commits or retries it. */
    public function withTx(Session $session, string $txId): self
    {
        if ($this->session !== null) {
            throw new \LogicException('Queries is already bound to a transaction; call withTx on an unbound helper');
        }
        if ($txId === '') {
            throw new \InvalidArgumentException('Transaction ID must not be empty');
        }
        $queries = clone $this;
        $queries->session = $session->take();
        $queries->txId = $txId;
        return $queries;
    }

    private function execute(Closure $operation): ExecuteQueryResult
    {
        if ($this->session !== null) {
            return $operation($this->session);
        }
        return $this->table->retrySession($operation, $this->idempotent, $this->retryParams);
    }

    // -- name: UpsertPrimaryUser :exec
    public function upsertPrimaryUser(UpsertPrimaryUserParams $params): void
    {
        $parameters = [
            '$id' => YdbValueCodec::typedUint64($params->id, 'id'),
            '$name' => YdbValueCodec::typedUtf8($params->name, 'name'),
        ];

        $this->execute(function (Session $session) use ($parameters): ExecuteQueryResult {
            $query = $session->newQuery(<<<'SQLC_YDB_YQL'
                PRAGMA TablePathPrefix("/local/sqlc_namespaces/a");
                DECLARE $id AS Uint64;
                DECLARE $name AS Utf8;
                UPSERT INTO users (id, name) VALUES ($id, $name);
                SQLC_YDB_YQL)
                ->parameters($parameters)
                ->keepInCache(count($parameters) > 0);
            if ($this->txId !== null) {
                $query->txControl(new TransactionControl(['tx_id' => $this->txId]));
                $txControl = $query->getRequestData()['tx_control']->serializeToString();
            } else {
                $query->beginTx('serializable_read_write');
            }
            if ($this->configure !== null) {
                ($this->configure)($query);
            }
            if ($this->txId !== null && $query->getRequestData()['tx_control']->serializeToString() !== $txControl) {
                throw new \LogicException('configure must not change transaction control on a transaction-bound Queries');
            }

            return (new YdbRawExecutor($this->table))->execute($session, $query, $this->txId === null);
        });
    }

    // -- name: UpsertSecondaryUser :exec
    public function upsertSecondaryUser(UpsertSecondaryUserParams $params): void
    {
        $parameters = [
            '$id' => YdbValueCodec::typedUint64($params->id, 'id'),
            '$name' => YdbValueCodec::typedUtf8($params->name, 'name'),
        ];

        $this->execute(function (Session $session) use ($parameters): ExecuteQueryResult {
            $query = $session->newQuery(<<<'SQLC_YDB_YQL'
                PRAGMA TablePathPrefix("/local/sqlc_namespaces/b");
                DECLARE $id AS Uint64;
                DECLARE $name AS Utf8;
                UPSERT INTO users (id, name) VALUES ($id, $name);
                SQLC_YDB_YQL)
                ->parameters($parameters)
                ->keepInCache(count($parameters) > 0);
            if ($this->txId !== null) {
                $query->txControl(new TransactionControl(['tx_id' => $this->txId]));
                $txControl = $query->getRequestData()['tx_control']->serializeToString();
            } else {
                $query->beginTx('serializable_read_write');
            }
            if ($this->configure !== null) {
                ($this->configure)($query);
            }
            if ($this->txId !== null && $query->getRequestData()['tx_control']->serializeToString() !== $txControl) {
                throw new \LogicException('configure must not change transaction control on a transaction-bound Queries');
            }

            return (new YdbRawExecutor($this->table))->execute($session, $query, $this->txId === null);
        });
    }

    // -- name: GetPrimaryUser :one
    public function getPrimaryUser(string $id): ?GetPrimaryUserRow
    {
        $parameters = [
            '$id' => YdbValueCodec::typedUint64($id, 'id'),
        ];

        $result = $this->execute(function (Session $session) use ($parameters): ExecuteQueryResult {
            $query = $session->newQuery(<<<'SQLC_YDB_YQL'
                PRAGMA TablePathPrefix("/local/sqlc_namespaces/a");
                SELECT users.`id` AS `id`, `users`.`name` AS `name` FROM users WHERE id = $id;
                SQLC_YDB_YQL)
                ->parameters($parameters)
                ->keepInCache(count($parameters) > 0);
            if ($this->txId !== null) {
                $query->txControl(new TransactionControl(['tx_id' => $this->txId]));
                $txControl = $query->getRequestData()['tx_control']->serializeToString();
            } else {
                $query->beginTx('serializable_read_write');
            }
            if ($this->configure !== null) {
                ($this->configure)($query);
            }
            if ($this->txId !== null && $query->getRequestData()['tx_control']->serializeToString() !== $txControl) {
                throw new \LogicException('configure must not change transaction control on a transaction-bound Queries');
            }

            return (new YdbRawExecutor($this->table))->execute($session, $query, $this->txId === null);
        });

        $rows = $this->decodeRows(
            $result,
            'GetPrimaryUser',
            [
                ['id', PrimitiveTypeId::UINT64, false],
                ['name', PrimitiveTypeId::UTF8, false],
            ],
            static fn($items): GetPrimaryUserRow => new GetPrimaryUserRow(
                YdbValueCodec::uint64($items->offsetGet(0), 'GetPrimaryUser.id'),
                YdbValueCodec::utf8($items->offsetGet(1), 'GetPrimaryUser.name'),
            ),
        );

        return $rows[0] ?? null;
    }

    // -- name: GetSecondaryUser :one
    public function getSecondaryUser(string $id): ?GetSecondaryUserRow
    {
        $parameters = [
            '$id' => YdbValueCodec::typedUint64($id, 'id'),
        ];

        $result = $this->execute(function (Session $session) use ($parameters): ExecuteQueryResult {
            $query = $session->newQuery(<<<'SQLC_YDB_YQL'
                PRAGMA TablePathPrefix("/local/sqlc_namespaces/b");
                DECLARE $id AS Uint64;
                SELECT users.`id` AS `id`, `users`.`name` AS `name` FROM users WHERE id = $id;
                SQLC_YDB_YQL)
                ->parameters($parameters)
                ->keepInCache(count($parameters) > 0);
            if ($this->txId !== null) {
                $query->txControl(new TransactionControl(['tx_id' => $this->txId]));
                $txControl = $query->getRequestData()['tx_control']->serializeToString();
            } else {
                $query->beginTx('serializable_read_write');
            }
            if ($this->configure !== null) {
                ($this->configure)($query);
            }
            if ($this->txId !== null && $query->getRequestData()['tx_control']->serializeToString() !== $txControl) {
                throw new \LogicException('configure must not change transaction control on a transaction-bound Queries');
            }

            return (new YdbRawExecutor($this->table))->execute($session, $query, $this->txId === null);
        });

        $rows = $this->decodeRows(
            $result,
            'GetSecondaryUser',
            [
                ['id', PrimitiveTypeId::UINT64, false],
                ['name', PrimitiveTypeId::UTF8, false],
            ],
            static fn($items): GetSecondaryUserRow => new GetSecondaryUserRow(
                YdbValueCodec::uint64($items->offsetGet(0), 'GetSecondaryUser.id'),
                YdbValueCodec::utf8($items->offsetGet(1), 'GetSecondaryUser.name'),
            ),
        );

        return $rows[0] ?? null;
    }

    /** @return list<FindPrimaryUsersByNameRow> */
    // -- name: FindPrimaryUsersByName :many
    public function findPrimaryUsersByName(string $name): array
    {
        $parameters = [
            '$name' => YdbValueCodec::typedUtf8($name, 'name'),
        ];

        $result = $this->execute(function (Session $session) use ($parameters): ExecuteQueryResult {
            $query = $session->newQuery(<<<'SQLC_YDB_YQL'
                PRAGMA TablePathPrefix("/local/sqlc_namespaces/a");
                SELECT u.`id` AS `id`, `u`.`name` AS `name` FROM users VIEW by_name AS u WHERE u.name = $name ORDER BY u.id;
                SQLC_YDB_YQL)
                ->parameters($parameters)
                ->keepInCache(count($parameters) > 0);
            if ($this->txId !== null) {
                $query->txControl(new TransactionControl(['tx_id' => $this->txId]));
                $txControl = $query->getRequestData()['tx_control']->serializeToString();
            } else {
                $query->beginTx('serializable_read_write');
            }
            if ($this->configure !== null) {
                ($this->configure)($query);
            }
            if ($this->txId !== null && $query->getRequestData()['tx_control']->serializeToString() !== $txControl) {
                throw new \LogicException('configure must not change transaction control on a transaction-bound Queries');
            }

            return (new YdbRawExecutor($this->table))->execute($session, $query, $this->txId === null);
        });

        $rows = $this->decodeRows(
            $result,
            'FindPrimaryUsersByName',
            [
                ['id', PrimitiveTypeId::UINT64, false],
                ['name', PrimitiveTypeId::UTF8, false],
            ],
            static fn($items): FindPrimaryUsersByNameRow => new FindPrimaryUsersByNameRow(
                YdbValueCodec::uint64($items->offsetGet(0), 'FindPrimaryUsersByName.id'),
                YdbValueCodec::utf8($items->offsetGet(1), 'FindPrimaryUsersByName.name'),
            ),
        );

        return $rows;
    }

    /** @return list<CompareUserNamesRow> */
    // -- name: CompareUserNames :many
    public function compareUserNames(): array
    {
        $parameters = [
        ];

        $result = $this->execute(function (Session $session) use ($parameters): ExecuteQueryResult {
            $query = $session->newQuery(<<<'SQLC_YDB_YQL'
                PRAGMA TablePathPrefix("/local/sqlc_namespaces/a");
                SELECT a.id AS id, a.name AS primary_name, b.name AS secondary_name
                FROM users AS a
                LEFT JOIN `/local/sqlc_namespaces/b/users` AS b ON a.id = b.id
                ORDER BY a.id;
                SQLC_YDB_YQL)
                ->parameters($parameters)
                ->keepInCache(count($parameters) > 0);
            if ($this->txId !== null) {
                $query->txControl(new TransactionControl(['tx_id' => $this->txId]));
                $txControl = $query->getRequestData()['tx_control']->serializeToString();
            } else {
                $query->beginTx('serializable_read_write');
            }
            if ($this->configure !== null) {
                ($this->configure)($query);
            }
            if ($this->txId !== null && $query->getRequestData()['tx_control']->serializeToString() !== $txControl) {
                throw new \LogicException('configure must not change transaction control on a transaction-bound Queries');
            }

            return (new YdbRawExecutor($this->table))->execute($session, $query, $this->txId === null);
        });

        $rows = $this->decodeRows(
            $result,
            'CompareUserNames',
            [
                ['id', PrimitiveTypeId::UINT64, false],
                ['primary_name', PrimitiveTypeId::UTF8, false],
                ['secondary_name', PrimitiveTypeId::UTF8, true],
            ],
            static fn($items): CompareUserNamesRow => new CompareUserNamesRow(
                YdbValueCodec::uint64($items->offsetGet(0), 'CompareUserNames.id'),
                YdbValueCodec::utf8($items->offsetGet(1), 'CompareUserNames.primary_name'),
                YdbValueCodec::optionalUtf8($items->offsetGet(2), 'CompareUserNames.secondary_name'),
            ),
        );

        return $rows;
    }

    /**
     * @param array<int, array{0: string, 1: int, 2: bool}> $expectedColumns
     * @return list<object>
     */
    private function decodeRows(ExecuteQueryResult $result, string $query, array $expectedColumns, Closure $decode): array
    {
        $sets = $result->getResultSets();
        if (count($sets) !== 1) {
            throw new UnexpectedValueException(sprintf('%s: expected one YDB result set, got %d', $query, count($sets)));
        }
        $set = $sets->offsetGet(0);
        if ($set->getTruncated()) {
            throw new UnexpectedValueException($query . ': YDB result is truncated; use a bounded query or pagination');
        }

        $columns = $set->getColumns();
        if (count($columns) !== count($expectedColumns)) {
            throw new UnexpectedValueException(sprintf('%s: expected %d result columns, got %d', $query, count($expectedColumns), count($columns)));
        }
        foreach ($expectedColumns as $index => [$name, $typeId, $optional]) {
            $column = $columns->offsetGet($index);
            if ($column->getName() !== $name) {
                throw new UnexpectedValueException(sprintf('%s: result column %d is %s, expected %s', $query, $index, $column->getName(), $name));
            }
            YdbValueCodec::assertType($column->getType(), $typeId, $optional, $query . '.' . $name);
        }
        $rows = [];
        foreach ($set->getRows() as $rowIndex => $row) {
            $items = $row->getItems();
            if (count($items) !== count($expectedColumns)) {
                throw new UnexpectedValueException(sprintf('%s: row %d has %d values, expected %d', $query, $rowIndex, count($items), count($expectedColumns)));
            }
            $rows[] = $decode($items);
        }
        return $rows;
    }
}
