# Code generated by sqlc-ydb. DO NOT EDIT.
from __future__ import annotations
from typing import Optional
from . import models as _models
import ydb as _ydb


class Querier:
    def __init__(self, connection):
        self._connection = connection

    # -- name: UpsertPrimaryUser :exec
    def upsert_primary_user(self, id: int, name: str) -> None:
        parameters = {
            "$id": (id, _ydb.PrimitiveType.Uint64),
            "$name": (name, _ydb.PrimitiveType.Utf8),
        }
        cursor = self._connection.cursor()
        try:
            cursor.execute(
                ("PRAGMA TablePathPrefix(\"/local/sqlc_namespaces/a\");\n"
                 "DECLARE $id AS Uint64;\n"
                 "DECLARE $name AS Utf8;\n"
                 "UPSERT INTO users (id, name) VALUES ($id, $name);"),
                parameters,
            )
            return None
        finally:
            cursor.close()

    # -- name: UpsertSecondaryUser :exec
    def upsert_secondary_user(self, id: int, name: str) -> None:
        parameters = {
            "$id": (id, _ydb.PrimitiveType.Uint64),
            "$name": (name, _ydb.PrimitiveType.Utf8),
        }
        cursor = self._connection.cursor()
        try:
            cursor.execute(
                ("PRAGMA TablePathPrefix(\"/local/sqlc_namespaces/b\");\n"
                 "DECLARE $id AS Uint64;\n"
                 "DECLARE $name AS Utf8;\n"
                 "UPSERT INTO users (id, name) VALUES ($id, $name);"),
                parameters,
            )
            return None
        finally:
            cursor.close()

    # -- name: GetPrimaryUser :one
    def get_primary_user(self, id: int) -> Optional[_models.LocalSqlcNamespacesAUsers]:
        parameters = {
            "$id": (id, _ydb.PrimitiveType.Uint64),
        }
        cursor = self._connection.cursor()
        try:
            cursor.execute(
                ("PRAGMA TablePathPrefix(\"/local/sqlc_namespaces/a\");\n"
                 "SELECT users.`id` AS `id`, `users`.`name` AS `name` FROM users WHERE id = $id;"),
                parameters,
            )
            row = cursor.fetchone()
            if row is None:
                return None
            return _models.LocalSqlcNamespacesAUsers(
                id=row[0],
                name=row[1],
            )
        finally:
            cursor.close()

    # -- name: GetSecondaryUser :one
    def get_secondary_user(self, id: int) -> Optional[_models.LocalSqlcNamespacesBUsers]:
        parameters = {
            "$id": (id, _ydb.PrimitiveType.Uint64),
        }
        cursor = self._connection.cursor()
        try:
            cursor.execute(
                ("PRAGMA TablePathPrefix(\"/local/sqlc_namespaces/b\");\n"
                 "DECLARE $id AS Uint64;\n"
                 "SELECT users.`id` AS `id`, `users`.`name` AS `name` FROM users WHERE id = $id;"),
                parameters,
            )
            row = cursor.fetchone()
            if row is None:
                return None
            return _models.LocalSqlcNamespacesBUsers(
                id=row[0],
                name=row[1],
            )
        finally:
            cursor.close()

    # -- name: FindPrimaryUsersByName :many
    def find_primary_users_by_name(self, name: str) -> list[_models.LocalSqlcNamespacesAUsers]:
        parameters = {
            "$name": (name, _ydb.PrimitiveType.Utf8),
        }
        cursor = self._connection.cursor()
        try:
            cursor.execute(
                ("PRAGMA TablePathPrefix(\"/local/sqlc_namespaces/a\");\n"
                 "SELECT u.`id` AS `id`, `u`.`name` AS `name` FROM users VIEW by_name AS u WHERE u.name = $name ORDER BY u.id;"),
                parameters,
            )
            rows = cursor.fetchall()
            return [_models.LocalSqlcNamespacesAUsers(
                id=row[0],
                name=row[1],
            ) for row in rows]
        finally:
            cursor.close()

    # -- name: CompareUserNames :many
    def compare_user_names(self) -> list[_models.CompareUserNamesRow]:
        parameters = {
        }
        cursor = self._connection.cursor()
        try:
            cursor.execute(
                ("PRAGMA TablePathPrefix(\"/local/sqlc_namespaces/a\");\n"
                 "SELECT a.id AS id, a.name AS primary_name, b.name AS secondary_name\n"
                 "FROM users AS a\n"
                 "LEFT JOIN `/local/sqlc_namespaces/b/users` AS b ON a.id = b.id\n"
                 "ORDER BY a.id;"),
                parameters,
            )
            rows = cursor.fetchall()
            return [_models.CompareUserNamesRow(
                id=row[0],
                primary_name=row[1],
                secondary_name=row[2],
            ) for row in rows]
        finally:
            cursor.close()
