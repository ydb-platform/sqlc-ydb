// Code generated by sqlc-ydb. DO NOT EDIT.
package namespaces.jdbc

import tech.ydb.table.values.PrimitiveValue
import tech.ydb.table.values.PrimitiveType
import tech.ydb.table.values.OptionalType

// The caller owns the injected client and its lifecycle.
class Queries(private val client: java.sql.Connection) {

    // -- name: UpsertPrimaryUser :exec
    fun upsertPrimaryUser(id: Long, name: String): Unit {
        client.unwrap(tech.ydb.jdbc.YdbConnection::class.java).prepareStatement(
            "PRAGMA TablePathPrefix(\"/local/sqlc_namespaces/a\");\n" +
            "DECLARE \$id AS Uint64;\n" +
            "DECLARE \$name AS Utf8;\n" +
            "UPSERT INTO users (id, name) VALUES (\$id, \$name);", tech.ydb.jdbc.YdbPrepareMode.DATA_QUERY).use { _prepared ->
            _prepared.setObject("id", PrimitiveValue.newUint64(id))
            _prepared.setString("name", name)
            _prepared.execute()
        }
    }

    // -- name: UpsertSecondaryUser :exec
    fun upsertSecondaryUser(id: Long, name: String): Unit {
        client.unwrap(tech.ydb.jdbc.YdbConnection::class.java).prepareStatement(
            "PRAGMA TablePathPrefix(\"/local/sqlc_namespaces/b\");\n" +
            "DECLARE \$id AS Uint64;\n" +
            "DECLARE \$name AS Utf8;\n" +
            "UPSERT INTO users (id, name) VALUES (\$id, \$name);", tech.ydb.jdbc.YdbPrepareMode.DATA_QUERY).use { _prepared ->
            _prepared.setObject("id", PrimitiveValue.newUint64(id))
            _prepared.setString("name", name)
            _prepared.execute()
        }
    }

    // -- name: GetPrimaryUser :one
    fun getPrimaryUser(id: Long): GetPrimaryUserRow? {
        client.prepareStatement(
            "PRAGMA TablePathPrefix(\"/local/sqlc_namespaces/a\");\n" +
            "SELECT users.`id` AS `id`, `users`.`name` AS `name` FROM users WHERE id = ?;").use { _prepared ->
            _prepared.setObject(1, PrimitiveValue.newUint64(id))
            _prepared.executeQuery().use { _rows ->
                if (!_rows.next()) return null
                val _value0: Long = _rows.getLong(1)
                val _value1: String = _rows.getString(2)
                return GetPrimaryUserRow(_value0, _value1)
            }
        }
    }

    // -- name: GetSecondaryUser :one
    fun getSecondaryUser(id: Long): GetSecondaryUserRow? {
        client.unwrap(tech.ydb.jdbc.YdbConnection::class.java).prepareStatement(
            "PRAGMA TablePathPrefix(\"/local/sqlc_namespaces/b\");\n" +
            "DECLARE \$id AS Uint64;\n" +
            "SELECT users.`id` AS `id`, `users`.`name` AS `name` FROM users WHERE id = \$id;", tech.ydb.jdbc.YdbPrepareMode.DATA_QUERY).use { _prepared ->
            _prepared.setObject("id", PrimitiveValue.newUint64(id))
            _prepared.executeQuery().use { _rows ->
                if (!_rows.next()) return null
                val _value0: Long = _rows.getLong(1)
                val _value1: String = _rows.getString(2)
                return GetSecondaryUserRow(_value0, _value1)
            }
        }
    }

    // -- name: FindPrimaryUsersByName :many
    fun findPrimaryUsersByName(name: String): List<FindPrimaryUsersByNameRow> {
        client.prepareStatement(
            "PRAGMA TablePathPrefix(\"/local/sqlc_namespaces/a\");\n" +
            "SELECT u.`id` AS `id`, `u`.`name` AS `name` FROM users VIEW by_name AS u WHERE u.name = ? ORDER BY u.id;").use { _prepared ->
            _prepared.setString(1, name)
            _prepared.executeQuery().use { _rows ->
                val _items = ArrayList<FindPrimaryUsersByNameRow>()
                while (_rows.next()) {
                    val _value0: Long = _rows.getLong(1)
                    val _value1: String = _rows.getString(2)
                    _items.add(FindPrimaryUsersByNameRow(_value0, _value1))
                }
                return _items
            }
        }
    }

    // -- name: CompareUserNames :many
    fun compareUserNames(): List<CompareUserNamesRow> {
        client.prepareStatement(
            "PRAGMA TablePathPrefix(\"/local/sqlc_namespaces/a\");\n" +
            "SELECT a.id AS id, a.name AS primary_name, b.name AS secondary_name\n" +
            "FROM users AS a\n" +
            "LEFT JOIN `/local/sqlc_namespaces/b/users` AS b ON a.id = b.id\n" +
            "ORDER BY a.id;").use { _prepared ->
            _prepared.executeQuery().use { _rows ->
                val _items = ArrayList<CompareUserNamesRow>()
                while (_rows.next()) {
                    val _value0: Long = _rows.getLong(1)
                    val _value1: String = _rows.getString(2)
                    val _value2: String? = _rows.getString(3)
                    _items.add(CompareUserNamesRow(_value0, _value1, _value2))
                }
                return _items
            }
        }
    }
}
