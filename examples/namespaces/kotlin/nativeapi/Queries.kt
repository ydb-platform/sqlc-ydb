// Code generated by sqlc-ydb. DO NOT EDIT.
package namespaces.nativeapi

import tech.ydb.query.QueryTransaction
import tech.ydb.query.tools.SessionRetryContext
import tech.ydb.query.tools.QueryReader
import tech.ydb.common.transaction.TxMode
import tech.ydb.table.query.Params
import tech.ydb.table.values.PrimitiveValue
import tech.ydb.table.values.PrimitiveType
import tech.ydb.table.values.OptionalType

// The caller owns the injected executor and its lifecycle.
class Queries {
    private val client: SessionRetryContext?
    private val transaction: QueryTransaction?

    constructor(client: SessionRetryContext) {
        this.client = client
        this.transaction = null
    }

    constructor(transaction: QueryTransaction) {
        this.client = null
        this.transaction = transaction
    }

    // -- name: UpsertPrimaryUser :exec
    fun upsertPrimaryUser(id: Long, name: String): Unit {
        val _params = Params.create()
        _params.put("\$id", PrimitiveValue.newUint64(id))
        _params.put("\$name", PrimitiveValue.newText(name))
        if (transaction != null) {
            transaction.createQuery(
                "PRAGMA TablePathPrefix(\"/local/sqlc_namespaces/a\");\n" +
                "DECLARE \$id AS Uint64;\n" +
                "DECLARE \$name AS Utf8;\n" +
                "UPSERT INTO users (id, name) VALUES (\$id, \$name);", _params).execute().join().getStatus().expectSuccess()
        } else {
            client!!.supplyResult { _session ->
                _session.createQuery(
                    "PRAGMA TablePathPrefix(\"/local/sqlc_namespaces/a\");\n" +
                    "DECLARE \$id AS Uint64;\n" +
                    "DECLARE \$name AS Utf8;\n" +
                    "UPSERT INTO users (id, name) VALUES (\$id, \$name);", TxMode.SERIALIZABLE_RW, _params).execute()
            }.join().getStatus().expectSuccess()
        }
    }

    // -- name: UpsertSecondaryUser :exec
    fun upsertSecondaryUser(id: Long, name: String): Unit {
        val _params = Params.create()
        _params.put("\$id", PrimitiveValue.newUint64(id))
        _params.put("\$name", PrimitiveValue.newText(name))
        if (transaction != null) {
            transaction.createQuery(
                "PRAGMA TablePathPrefix(\"/local/sqlc_namespaces/b\");\n" +
                "DECLARE \$id AS Uint64;\n" +
                "DECLARE \$name AS Utf8;\n" +
                "UPSERT INTO users (id, name) VALUES (\$id, \$name);", _params).execute().join().getStatus().expectSuccess()
        } else {
            client!!.supplyResult { _session ->
                _session.createQuery(
                    "PRAGMA TablePathPrefix(\"/local/sqlc_namespaces/b\");\n" +
                    "DECLARE \$id AS Uint64;\n" +
                    "DECLARE \$name AS Utf8;\n" +
                    "UPSERT INTO users (id, name) VALUES (\$id, \$name);", TxMode.SERIALIZABLE_RW, _params).execute()
            }.join().getStatus().expectSuccess()
        }
    }

    // -- name: GetPrimaryUser :one
    fun getPrimaryUser(id: Long): GetPrimaryUserRow? {
        val _params = Params.create()
        _params.put("\$id", PrimitiveValue.newUint64(id))
        val _query = if (transaction != null) {
            QueryReader.readFrom(transaction.createQuery(
                "PRAGMA TablePathPrefix(\"/local/sqlc_namespaces/a\");\n" +
                "SELECT users.`id` AS `id`, `users`.`name` AS `name` FROM users WHERE id = \$id;", _params)).join().getValue()
        } else {
            client!!.supplyResult { _session ->
                QueryReader.readFrom(_session.createQuery(
                    "PRAGMA TablePathPrefix(\"/local/sqlc_namespaces/a\");\n" +
                    "SELECT users.`id` AS `id`, `users`.`name` AS `name` FROM users WHERE id = \$id;", TxMode.SERIALIZABLE_RW, _params))
            }.join().getValue()
        }
        kotlin.check(_query.getResultSetCount() == 1) { "Expected one result set" }
        val _rows = _query.getResultSet(0)
        if (!_rows.next()) return null
        val _value0: Long = _rows.getColumn(0).getUint64()
        val _value1: String = _rows.getColumn(1).getText()
        return GetPrimaryUserRow(_value0, _value1)
    }

    // -- name: GetSecondaryUser :one
    fun getSecondaryUser(id: Long): GetSecondaryUserRow? {
        val _params = Params.create()
        _params.put("\$id", PrimitiveValue.newUint64(id))
        val _query = if (transaction != null) {
            QueryReader.readFrom(transaction.createQuery(
                "PRAGMA TablePathPrefix(\"/local/sqlc_namespaces/b\");\n" +
                "DECLARE \$id AS Uint64;\n" +
                "SELECT users.`id` AS `id`, `users`.`name` AS `name` FROM users WHERE id = \$id;", _params)).join().getValue()
        } else {
            client!!.supplyResult { _session ->
                QueryReader.readFrom(_session.createQuery(
                    "PRAGMA TablePathPrefix(\"/local/sqlc_namespaces/b\");\n" +
                    "DECLARE \$id AS Uint64;\n" +
                    "SELECT users.`id` AS `id`, `users`.`name` AS `name` FROM users WHERE id = \$id;", TxMode.SERIALIZABLE_RW, _params))
            }.join().getValue()
        }
        kotlin.check(_query.getResultSetCount() == 1) { "Expected one result set" }
        val _rows = _query.getResultSet(0)
        if (!_rows.next()) return null
        val _value0: Long = _rows.getColumn(0).getUint64()
        val _value1: String = _rows.getColumn(1).getText()
        return GetSecondaryUserRow(_value0, _value1)
    }

    // -- name: FindPrimaryUsersByName :many
    fun findPrimaryUsersByName(name: String): List<FindPrimaryUsersByNameRow> {
        val _params = Params.create()
        _params.put("\$name", PrimitiveValue.newText(name))
        val _query = if (transaction != null) {
            QueryReader.readFrom(transaction.createQuery(
                "PRAGMA TablePathPrefix(\"/local/sqlc_namespaces/a\");\n" +
                "SELECT u.`id` AS `id`, `u`.`name` AS `name` FROM users VIEW by_name AS u WHERE u.name = \$name ORDER BY u.id;", _params)).join().getValue()
        } else {
            client!!.supplyResult { _session ->
                QueryReader.readFrom(_session.createQuery(
                    "PRAGMA TablePathPrefix(\"/local/sqlc_namespaces/a\");\n" +
                    "SELECT u.`id` AS `id`, `u`.`name` AS `name` FROM users VIEW by_name AS u WHERE u.name = \$name ORDER BY u.id;", TxMode.SERIALIZABLE_RW, _params))
            }.join().getValue()
        }
        kotlin.check(_query.getResultSetCount() == 1) { "Expected one result set" }
        val _rows = _query.getResultSet(0)
        val _items = ArrayList<FindPrimaryUsersByNameRow>()
        while (_rows.next()) {
            val _value0: Long = _rows.getColumn(0).getUint64()
            val _value1: String = _rows.getColumn(1).getText()
            _items.add(FindPrimaryUsersByNameRow(_value0, _value1))
        }
        return _items
    }

    // -- name: CompareUserNames :many
    fun compareUserNames(): List<CompareUserNamesRow> {
        val _params = Params.create()
        val _query = if (transaction != null) {
            QueryReader.readFrom(transaction.createQuery(
                "PRAGMA TablePathPrefix(\"/local/sqlc_namespaces/a\");\n" +
                "SELECT a.id AS id, a.name AS primary_name, b.name AS secondary_name\n" +
                "FROM users AS a\n" +
                "LEFT JOIN `/local/sqlc_namespaces/b/users` AS b ON a.id = b.id\n" +
                "ORDER BY a.id;", _params)).join().getValue()
        } else {
            client!!.supplyResult { _session ->
                QueryReader.readFrom(_session.createQuery(
                    "PRAGMA TablePathPrefix(\"/local/sqlc_namespaces/a\");\n" +
                    "SELECT a.id AS id, a.name AS primary_name, b.name AS secondary_name\n" +
                    "FROM users AS a\n" +
                    "LEFT JOIN `/local/sqlc_namespaces/b/users` AS b ON a.id = b.id\n" +
                    "ORDER BY a.id;", TxMode.SERIALIZABLE_RW, _params))
            }.join().getValue()
        }
        kotlin.check(_query.getResultSetCount() == 1) { "Expected one result set" }
        val _rows = _query.getResultSet(0)
        val _items = ArrayList<CompareUserNamesRow>()
        while (_rows.next()) {
            val _value0: Long = _rows.getColumn(0).getUint64()
            val _value1: String = _rows.getColumn(1).getText()
            val _value2: String? = _rows.getColumn(2).getText()
            _items.add(CompareUserNamesRow(_value0, _value1, _value2))
        }
        return _items
    }
}
