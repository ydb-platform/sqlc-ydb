// Code generated by sqlc-ydb. DO NOT EDIT.
#include "queries.hpp"

#include <ydb-cpp-sdk/client/params/params.h>
#include <ydb-cpp-sdk/client/result/result.h>
#include <ydb-cpp-sdk/client/types/status/status.h>

#include <stdexcept>
#include <utility>

namespace namespaces::native {

// -- name: UpsertPrimaryUser :exec
void Queries::UpsertPrimaryUser(std::uint64_t id, const std::string& name) const {
    const auto sqlc_execute = [&](NYdb::NQuery::TSession sqlc_session, const NYdb::NQuery::TTxControl& sqlc_tx) -> NYdb::TStatus {
        auto sqlc_params = NYdb::TParamsBuilder()
            .AddParam("$id").Uint64(id).Build()
            .AddParam("$name").Utf8(name).Build()
            .Build();
        auto sqlc_result = sqlc_session.ExecuteQuery(
            "PRAGMA TablePathPrefix(\"/local/sqlc_namespaces/a\");\n"
            "DECLARE $id AS Uint64;\n"
            "DECLARE $name AS Utf8;\n"
            "UPSERT INTO users (id, name) VALUES ($id, $name);",
            sqlc_tx,
            sqlc_params,
            this->execute_settings_
        ).GetValueSync();
        return sqlc_result;
    };
    const auto sqlc_status = this->transaction_ != nullptr
        ? sqlc_execute(this->transaction_->GetSession(), NYdb::NQuery::TTxControl::Tx(*this->transaction_))
        : this->client_->RetryQuerySync([&](NYdb::NQuery::TSession sqlc_session) -> NYdb::TStatus {
            return sqlc_execute(
                std::move(sqlc_session),
                NYdb::NQuery::TTxControl::BeginTx(this->tx_settings_).CommitTx()
            );
        }, this->retry_settings_);
    NYdb::NStatusHelpers::ThrowOnError(sqlc_status);
}

// -- name: UpsertSecondaryUser :exec
void Queries::UpsertSecondaryUser(std::uint64_t id, const std::string& name) const {
    const auto sqlc_execute = [&](NYdb::NQuery::TSession sqlc_session, const NYdb::NQuery::TTxControl& sqlc_tx) -> NYdb::TStatus {
        auto sqlc_params = NYdb::TParamsBuilder()
            .AddParam("$id").Uint64(id).Build()
            .AddParam("$name").Utf8(name).Build()
            .Build();
        auto sqlc_result = sqlc_session.ExecuteQuery(
            "PRAGMA TablePathPrefix(\"/local/sqlc_namespaces/b\");\n"
            "DECLARE $id AS Uint64;\n"
            "DECLARE $name AS Utf8;\n"
            "UPSERT INTO users (id, name) VALUES ($id, $name);",
            sqlc_tx,
            sqlc_params,
            this->execute_settings_
        ).GetValueSync();
        return sqlc_result;
    };
    const auto sqlc_status = this->transaction_ != nullptr
        ? sqlc_execute(this->transaction_->GetSession(), NYdb::NQuery::TTxControl::Tx(*this->transaction_))
        : this->client_->RetryQuerySync([&](NYdb::NQuery::TSession sqlc_session) -> NYdb::TStatus {
            return sqlc_execute(
                std::move(sqlc_session),
                NYdb::NQuery::TTxControl::BeginTx(this->tx_settings_).CommitTx()
            );
        }, this->retry_settings_);
    NYdb::NStatusHelpers::ThrowOnError(sqlc_status);
}

// -- name: GetPrimaryUser :one
std::optional<GetPrimaryUserRow> Queries::GetPrimaryUser(std::uint64_t id) const {
    std::optional<NYdb::TResultSet> sqlc_result_set;
    const auto sqlc_execute = [&](NYdb::NQuery::TSession sqlc_session, const NYdb::NQuery::TTxControl& sqlc_tx) -> NYdb::TStatus {
        auto sqlc_params = NYdb::TParamsBuilder()
            .AddParam("$id").Uint64(id).Build()
            .Build();
        auto sqlc_result = sqlc_session.ExecuteQuery(
            "PRAGMA TablePathPrefix(\"/local/sqlc_namespaces/a\");\n"
            "SELECT users.`id` AS `id`, `users`.`name` AS `name` FROM users WHERE id = $id;",
            sqlc_tx,
            sqlc_params,
            this->execute_settings_
        ).GetValueSync();
        if (sqlc_result.IsSuccess()) {
            if (sqlc_result.GetResultSets().size() != 1) {
                throw std::runtime_error("expected exactly one result set");
            }
            sqlc_result_set = sqlc_result.GetResultSet(0);
        }
        return sqlc_result;
    };
    const auto sqlc_status = this->transaction_ != nullptr
        ? sqlc_execute(this->transaction_->GetSession(), NYdb::NQuery::TTxControl::Tx(*this->transaction_))
        : this->client_->RetryQuerySync([&](NYdb::NQuery::TSession sqlc_session) -> NYdb::TStatus {
            return sqlc_execute(
                std::move(sqlc_session),
                NYdb::NQuery::TTxControl::BeginTx(this->tx_settings_).CommitTx()
            );
        }, this->retry_settings_);
    NYdb::NStatusHelpers::ThrowOnError(sqlc_status);
    if (!sqlc_result_set) {
        throw std::runtime_error("GetPrimaryUser: successful query returned no result set");
    }
    NYdb::TResultSetParser sqlc_parser(*sqlc_result_set);
    if (!sqlc_parser.TryNextRow()) {
        return std::nullopt;
    }
    GetPrimaryUserRow sqlc_row{
        sqlc_parser.ColumnParser("id").GetUint64(),
        sqlc_parser.ColumnParser("name").GetUtf8(),
    };
    return sqlc_row;
}

// -- name: GetSecondaryUser :one
std::optional<GetSecondaryUserRow> Queries::GetSecondaryUser(std::uint64_t id) const {
    std::optional<NYdb::TResultSet> sqlc_result_set;
    const auto sqlc_execute = [&](NYdb::NQuery::TSession sqlc_session, const NYdb::NQuery::TTxControl& sqlc_tx) -> NYdb::TStatus {
        auto sqlc_params = NYdb::TParamsBuilder()
            .AddParam("$id").Uint64(id).Build()
            .Build();
        auto sqlc_result = sqlc_session.ExecuteQuery(
            "PRAGMA TablePathPrefix(\"/local/sqlc_namespaces/b\");\n"
            "DECLARE $id AS Uint64;\n"
            "SELECT users.`id` AS `id`, `users`.`name` AS `name` FROM users WHERE id = $id;",
            sqlc_tx,
            sqlc_params,
            this->execute_settings_
        ).GetValueSync();
        if (sqlc_result.IsSuccess()) {
            if (sqlc_result.GetResultSets().size() != 1) {
                throw std::runtime_error("expected exactly one result set");
            }
            sqlc_result_set = sqlc_result.GetResultSet(0);
        }
        return sqlc_result;
    };
    const auto sqlc_status = this->transaction_ != nullptr
        ? sqlc_execute(this->transaction_->GetSession(), NYdb::NQuery::TTxControl::Tx(*this->transaction_))
        : this->client_->RetryQuerySync([&](NYdb::NQuery::TSession sqlc_session) -> NYdb::TStatus {
            return sqlc_execute(
                std::move(sqlc_session),
                NYdb::NQuery::TTxControl::BeginTx(this->tx_settings_).CommitTx()
            );
        }, this->retry_settings_);
    NYdb::NStatusHelpers::ThrowOnError(sqlc_status);
    if (!sqlc_result_set) {
        throw std::runtime_error("GetSecondaryUser: successful query returned no result set");
    }
    NYdb::TResultSetParser sqlc_parser(*sqlc_result_set);
    if (!sqlc_parser.TryNextRow()) {
        return std::nullopt;
    }
    GetSecondaryUserRow sqlc_row{
        sqlc_parser.ColumnParser("id").GetUint64(),
        sqlc_parser.ColumnParser("name").GetUtf8(),
    };
    return sqlc_row;
}

// -- name: FindPrimaryUsersByName :many
std::vector<FindPrimaryUsersByNameRow> Queries::FindPrimaryUsersByName(const std::string& name) const {
    std::optional<NYdb::TResultSet> sqlc_result_set;
    const auto sqlc_execute = [&](NYdb::NQuery::TSession sqlc_session, const NYdb::NQuery::TTxControl& sqlc_tx) -> NYdb::TStatus {
        auto sqlc_params = NYdb::TParamsBuilder()
            .AddParam("$name").Utf8(name).Build()
            .Build();
        auto sqlc_result = sqlc_session.ExecuteQuery(
            "PRAGMA TablePathPrefix(\"/local/sqlc_namespaces/a\");\n"
            "SELECT u.`id` AS `id`, `u`.`name` AS `name` FROM users VIEW by_name AS u WHERE u.name = $name ORDER BY u.id;",
            sqlc_tx,
            sqlc_params,
            this->execute_settings_
        ).GetValueSync();
        if (sqlc_result.IsSuccess()) {
            if (sqlc_result.GetResultSets().size() != 1) {
                throw std::runtime_error("expected exactly one result set");
            }
            sqlc_result_set = sqlc_result.GetResultSet(0);
        }
        return sqlc_result;
    };
    const auto sqlc_status = this->transaction_ != nullptr
        ? sqlc_execute(this->transaction_->GetSession(), NYdb::NQuery::TTxControl::Tx(*this->transaction_))
        : this->client_->RetryQuerySync([&](NYdb::NQuery::TSession sqlc_session) -> NYdb::TStatus {
            return sqlc_execute(
                std::move(sqlc_session),
                NYdb::NQuery::TTxControl::BeginTx(this->tx_settings_).CommitTx()
            );
        }, this->retry_settings_);
    NYdb::NStatusHelpers::ThrowOnError(sqlc_status);
    if (!sqlc_result_set) {
        throw std::runtime_error("FindPrimaryUsersByName: successful query returned no result set");
    }
    NYdb::TResultSetParser sqlc_parser(*sqlc_result_set);
    std::vector<FindPrimaryUsersByNameRow> sqlc_rows;
    sqlc_rows.reserve(sqlc_result_set->RowsCount());
    while (sqlc_parser.TryNextRow()) {
        sqlc_rows.push_back(FindPrimaryUsersByNameRow{
            sqlc_parser.ColumnParser("id").GetUint64(),
            sqlc_parser.ColumnParser("name").GetUtf8(),
        });
    }
    return sqlc_rows;
}

// -- name: CompareUserNames :many
std::vector<CompareUserNamesRow> Queries::CompareUserNames() const {
    std::optional<NYdb::TResultSet> sqlc_result_set;
    const auto sqlc_execute = [&](NYdb::NQuery::TSession sqlc_session, const NYdb::NQuery::TTxControl& sqlc_tx) -> NYdb::TStatus {
        auto sqlc_result = sqlc_session.ExecuteQuery(
            "PRAGMA TablePathPrefix(\"/local/sqlc_namespaces/a\");\n"
            "SELECT a.id AS id, a.name AS primary_name, b.name AS secondary_name\n"
            "FROM users AS a\n"
            "LEFT JOIN `/local/sqlc_namespaces/b/users` AS b ON a.id = b.id\n"
            "ORDER BY a.id;",
            sqlc_tx,
            this->execute_settings_
        ).GetValueSync();
        if (sqlc_result.IsSuccess()) {
            if (sqlc_result.GetResultSets().size() != 1) {
                throw std::runtime_error("expected exactly one result set");
            }
            sqlc_result_set = sqlc_result.GetResultSet(0);
        }
        return sqlc_result;
    };
    const auto sqlc_status = this->transaction_ != nullptr
        ? sqlc_execute(this->transaction_->GetSession(), NYdb::NQuery::TTxControl::Tx(*this->transaction_))
        : this->client_->RetryQuerySync([&](NYdb::NQuery::TSession sqlc_session) -> NYdb::TStatus {
            return sqlc_execute(
                std::move(sqlc_session),
                NYdb::NQuery::TTxControl::BeginTx(this->tx_settings_).CommitTx()
            );
        }, this->retry_settings_);
    NYdb::NStatusHelpers::ThrowOnError(sqlc_status);
    if (!sqlc_result_set) {
        throw std::runtime_error("CompareUserNames: successful query returned no result set");
    }
    NYdb::TResultSetParser sqlc_parser(*sqlc_result_set);
    std::vector<CompareUserNamesRow> sqlc_rows;
    sqlc_rows.reserve(sqlc_result_set->RowsCount());
    while (sqlc_parser.TryNextRow()) {
        sqlc_rows.push_back(CompareUserNamesRow{
            sqlc_parser.ColumnParser("id").GetUint64(),
            sqlc_parser.ColumnParser("primary_name").GetUtf8(),
            sqlc_parser.ColumnParser("secondary_name").GetOptionalUtf8(),
        });
    }
    return sqlc_rows;
}

}  // namespace namespaces::native
