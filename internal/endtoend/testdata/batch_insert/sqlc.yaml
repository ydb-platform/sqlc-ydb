version: "2"
sql:
  - engine: ydb
    schema: schema.sql
    queries: queries.sql
    gen:
      go:
        package: batch
        out: db
        sql_package: ydb
        emit_interface: true
      typescript:
        out: typescript
        runtime: ydb
