# Code generated by sqlc-ydb. DO NOT EDIT.
from __future__ import annotations
from typing import Iterable, Optional
from . import models
import ydb
from sqlalchemy import text
from sqlalchemy.engine import Connection

SQL_GET_AUTHOR = """-- name\\: GetAuthor \\:one
DECLARE $id AS Uint64;
SELECT * FROM authors WHERE id = :id;"""


def _typed(value, typ):
    return (value, typ)


class Querier:
    def __init__(self, connection: Connection):
        self._connection = connection

    def get_author(self, id: int) -> Optional[models.Author]:
        parameters = {"id": _typed(id, ydb.PrimitiveType.Uint64)}
        result = self._connection.execute(text(SQL_GET_AUTHOR), parameters)
        try:
            rows = result.fetchall()
        finally:
            result.close()
        if not rows:
            return None
        row = rows[0]
        return models.Author(
            id=row._mapping["id"],
            name=row._mapping["name"],
        )
